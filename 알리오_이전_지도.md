# 알리오 이전 지도 (2026-09-26)

수집기를 시트 → DB 로 옮기는 첫 경로입니다. **옮기기 전에 지도만 만들었습니다.**
코드는 아직 한 줄도 안 썼습니다.

---

## 1. 알리오 경로가 쓰는 함수 전부

`수집기_지도.md` 의 줄 번호는 오래됐습니다(밤새 500줄이 들어갔습니다). 오늘 다시 찾은 것입니다.

| 함수 | 줄 | 하는 일 | node 로 옮길 때 |
|---|---|---|---|
| `collectJobs` | 10358 | **알리오 본체.** 목록 훑기 → 직군 판정 → 상세 열기 → 줄 만들기 → 시트 | 새로 씀 |
| `fetchJobPage_` | 10626 | 목록 한 쪽. 3번까지 다시 시도 | 그대로 (fetch) |
| `callJobApi_` | 10645 | **POST** 로 부름. 인증키만 감싸지 않음 | 그대로 |
| `alioDetail_` | 14728 | 상세 (`data.go.kr/1051000/recruitment/detail?sn=`) | 그대로 (GET) |
| `alioJob_` | 14749 | 상세에서 직군 뽑기 — steps → 자격 → **PDF** 순 | ⚠ PDF 가 걸림 |
| `alioCookie_` | 6329 | 포털 세션 쿠키 받기 (첨부용) | **옮겨서 됨 (확인)** |
| `alioFile_` | 6352 | 쿠키+Referer 로 첨부 받기 | **옮겨서 됨 (확인)** |
| `nidPdfText_` | — | 첨부 PDF → 글자. **Drive `convert:true` (OCR)** | ⚠ **여기가 막힙니다** |
| `matchJob_` | 10578 | 글에서 직군 찾기 (작업/물리/공통) | `tools/` 로 옮겨 한 벌 |
| `notOurs_` | 9610 | 남의 자리 공고 버리기 | 〃 |
| `multiRole_` | 9581 | 여러 자리 통합 공고인가 | 〃 |
| `mixedTitle_` | 9626 | 제목 괄호에 다른 자리가 섞였나 → 「포함」 | 〃 |
| `titleOtherOnly_` | — | 제목이 남의 자리뿐인가 | 〃 |
| `sameJob_` / `jobKeys_` | 10118 | 다른 경로로 이미 들어온 같은 공고인가 | DB 질의로 바꿈 |
| `fmtDate_` | 10618 | `20260926` → `2026.09.26` | 그대로 |
| `cut_` | — | 글 자르기 | 그대로 |
| `parseSchedule_` | — | 심사방법 글에서 전형 일정 뽑기 | 그대로 |
| `decodeEntities_` | — | `&amp;` `&#xD;` 풀기 | 그대로 |
| `sortRows_` / `sortJob_` | 3389 / 3228 | **네 갈래 문지기** | `tools/sort-rule.mjs` 에 이미 있음 |
| `mailNewJobs` / `holdMail_` | — | 새 공고·보류 메일 | 나중에 (DB 알림으로) |
| `orgMark_` | 16525 | 기관별 「됨/안 됨」 기록 | DB 표로 |
| `collectLog_` | — | 수집 기준선 기록 | DB 표로 |

**안 옮기는 것** — `alio_done`(속성에 열어본 공고 기억) · 쪽 이어가기 · 6분 예산 나누기.
node 에는 6분 한도가 없어 한 번에 다 돕니다 (아래 ③ 참고).

---

## 2. 시트 칸 ↔ DB 칸

`collectJobs` 가 만드는 줄은 27칸입니다 (`JCOLS`).

| # | 시트 칸 | 알리오 항목 | DB 칸 (`job_posts`) |
|---|---|---|---|
| 0 | 공고ID | `recrutPblntSn` | `id` |
| 1 | 수집일 | 오늘 | `collected_at` |
| 2 | 기관명 | `instNm` | `org_name` |
| 3 | 공고명 | `recrutPbancTtl` | `title` |
| 4 | 채용구분 | `recrutSeNm` | `hire_type` |
| 5 | 고용형태 | `hireTypeNmLst` | `employ_type` |
| 6 | 근무지 | `workRgnNmLst` | `work_place` → `sido`·`sgg` |
| 7 | 학력 | `acbgCondNmLst` | `edu` |
| 8 | 인원 | `ourNope` 또는 `recrutNope` | `headcount` |
| 9 | 접수시작 | `pbancBgngYmd` | `apply_from` |
| 10 | 접수마감 | `pbancEndYmd` | `apply_to` |
| 11 | 링크 | `srcUrl` | `url` |
| 12 | 직군 | 판정 결과 | `job_group` |
| 13 | 형태 | 「포함」 또는 빈칸 | `form` |
| 14 | 숨김 | — | `hidden` |
| 15 | 알림 | — | `notify` |
| 16 | 일정 | `scrnprcdrMthdExpln` 에서 뽑음 | `detail.일정` |
| 17 | 전형방법 | `scrnprcdrMthdExpln` | `detail.전형방법` |
| 18 | 지원자격 | `aplyQlfcCn` | `detail.지원자격` |
| 19 | 우대사항 | `prefCn` / `prefCondCn` | `detail.우대사항` |
| 20 | 결격사유 | `disqlfcRsn` | `detail.결격사유` |
| 21~24 | 자격증·제출서류·접수방법·문의처 | 안 채움 | `detail.*` |
| 25 | 연봉 | **알리오엔 없음** | — |
| 26 | 기관홈 | `srcUrl` | `detail.기관홈` |
| 27 | 보류 | `hold` 면 Y | `hold` |
| 28 | 보류사유 | `why` | `evidence.보류사유` |

**새로 채울 것** (지금 안 남는 것)
`source = '알리오(새)'` · `evidence.직군근거`(steps/자격/pdf/제목) · `evidence.걸린단어` ·
기관 붙이기 결과(`org_public`) · 수집 시각.

---

## 3. 알리오 특유의 것 — 전부 직접 두드려 확인했습니다

### ① 목록은 POST — **node 에서 됩니다**

```
POST https://opendata.alio.go.kr/new/v1/recruit/list.do?serviceKey=…&pageNo=1&numOfRows=100&ongoingYn=Y
  → HTTP 200 · 224,049자
  접수중 500건 · 5쪽 · **2초**
```

인증키는 이미 인코딩돼 있어 **다시 감싸면 안 됩니다** (`callJobApi_` 의 `rawKey`).
IP 차단 없습니다. GitHub Actions 에서도 될 것으로 봅니다(같은 공개 API).

**gas 는 이걸 100초 예산 안에서 쪼개 돌고 있습니다. node 는 2초입니다.**

항목 30개 —
`recrutPblntSn · instNm · recrutPbancTtl · srcUrl · pbancBgngYmd · pbancEndYmd ·
aplyQlfcCn · prefCn · prefCondCn · disqlfcRsn · workRgnNmLst · acbgCondNmLst ·
hireTypeNmLst · recrutSeNm · recrutNope · scrnprcdrMthdExpln · ncsCdNmLst ·
steps · files · ongoingYn · decimalDay · …`

### ② 상세 — GET · 이름표는 `sn`

```
GET https://apis.data.go.kr/1051000/recruitment/detail?serviceKey=…&sn=<공고번호>
```

`recrutPblntSn` 이 아니라 **`sn`** 입니다 (gas 주석에 적혀 있고 확인했습니다).

### ③ 상세를 몇 건이나 열어야 하나 — **467/500건**

```
제목만으로 직군 확정        2건
목록 전체를 봐도 확정        6건
남의 자리라 버림           25건
상세를 열어야 하는 것      467건   ← 이것이 gas 가 며칠씩 걸리는 까닭
```

gas 는 한 번에 최대 900건, 400ms 씩 쉬며 예산 안에서만 엽니다. 로그에
「이 속도면 N번 더 · 약 X일」 이 찍히는 이유입니다.
**node 는 한 번에 다 열 수 있습니다** (6분 한도 없음).

### ④ 대괄호 병원명 · 본사 이름 — **이미 DB 가 풀고 있습니다**

알리오는 본사 이름으로 올립니다 (「근로복지공단」 + 제목 「[태백병원] …」).
이건 수집기가 아니라 `org_public()` 이 풉니다 — 4번째 칸(제목 대괄호).
**새 수집기는 그 함수를 그대로 부르면 됩니다.** 새로 만들 것 없습니다.

같은 시도 분원은 0번째 칸(원문 주소 호스트)이 풉니다 — 칠곡경북대 5건.

### ⑤ ⚠ 첨부 공고문 — **여기가 유일한 걸림돌입니다**

**첨부 받기는 옮겨집니다.** gas 의 `alioCookie_` 를 node 로 그대로 옮겨 확인했습니다 —

```
① https://opendata.alio.go.kr/new/ 를 두드려 쿠키 받기
     302 → 쿠키 2개 (JSESSIONID · SCOUTER) → 200
② 그 쿠키 + Referer 로 첨부 내려받기
     HTTP 200 · 420,117바이트 · 앞 5글자 「%PDF-」   ← 진짜 PDF
```

쿠키 없이 받으면 **포털 첫 화면 8,333바이트 HTML** 이 옵니다 (맨몸·UA만·UA+Referer 셋 다).

**막히는 것은 PDF 에서 글자를 뽑는 자리입니다.**

gas 는 Drive `convert:true` 로 **OCR** 합니다. node 에는 그게 없습니다.
이 컴퓨터에 있는 `pdftotext` 로 시험해 보니 —

```
의료 기관 공고문 PDF 12건을 받아 pdftotext 로 읽기
  글자 PDF   0건
  스캔 그림  12건      ← 한글이 한 자도 안 나옵니다
  (첫 PDF: 글꼴 0개 · 그림 9개 · 쪽 0개 — 순수 스캔본)
```

국립중앙의료원 · 분당서울대 · 경북대 · 전남대 · 경상국립대 전부 스캔본이었습니다.
**「글자 PDF 면 pdftotext 로 된다」 는 길은 없습니다.**

세 갈래 중 하나를 정하셔야 합니다 —

| 길 | 드는 것 | 잃는 것 |
|---|---|---|
| ㉮ **PDF 는 보류함으로** | 없음. 지금 규칙 그대로(「못 가린 공고는 보류함」) | gas 가 OCR 로 건지던 것을 사람이 봄 |
| ㉯ node 에서 OCR | tesseract + 한국어 자료. 한 건에 수 초 | 무겁고 느림. GitHub Actions 시간 |
| ㉰ Drive OCR 을 계속 씀 | 구글 서비스 계정 열쇠 + Drive API | gas 를 벗어나는 뜻이 반감 |

**저는 ㉮ 를 권합니다** — [3] 사흘 대조에서 「PDF 때문에만 갈린 건수」가 몇 건인지
숫자로 나옵니다. 적으면 그대로 두고, 많으면 그때 ㉯·㉰ 를 정하면 됩니다.
지금 정하지 않아도 [2] 를 시작할 수 있습니다.

### ⑥ steps 는 생각보다 안 쓰입니다

gas 주석은 「steps 에 직군이 또렷이 적혀 있다」 고 합니다. 그런데 오늘 의료 공고
40건의 상세를 열어보니 —

```
steps 로 풀린 것      0건
자격 글로 풀린 것      0건
PDF 만 있는 것       22건
한글 첨부뿐          17건
첨부 없음             1건
```

**지금 접수중인 공고에서는 steps 가 한 건도 안 풀었습니다.**
기관마다 채워 넣는 정도가 다른 것으로 보입니다. 규칙을 옮길 때
「steps 가 주력」 이라고 전제하면 안 됩니다.

---

## 4. 기준선 — 지금 알리오가 내는 것

이 숫자와 새 수집기를 사흘 대조합니다.

```
표에 남은 알리오 공고        60건 (전부 최근 30일)
  회원 목록                57건
  보류함                    0건
  숨김                      3건
  직군이 정해진 것           58건
    작업치료사               27건
    물리치료사               25건
    직군 빈칸                 2건
쓰레기통(알리오)              1건
가장 오래된 것  2026-08-27 · 가장 최근  2026-09-23
최근 7일에 들어온 기관 — 경북대학교병원 · 국민건강보험공단 일산병원 · 대한적십자사
```

> ※ 「보류함 0건」 은 알리오 보류가 없다는 뜻이 아니라, 지금 표에 남은 것 중
> 보류가 없다는 뜻입니다. gas 는 보류를 따로 메일로 보내고 관리자가 치웁니다.

---

## 5. [2] 를 시작하기 전에 정할 것

1. **PDF 를 어떻게 할지** — ㉮ 보류함 / ㉯ node OCR / ㉰ Drive OCR
   (㉮ 로 시작하고 [3] 에서 숫자를 보는 것을 권합니다)
2. `source` 값을 **`알리오(새)`** 로 쓸지 — DB 는 지금 `AL` 두 글자를 씁니다.
   빨간 줄(`route_health`)과 경로지도가 `source` 로 갈래를 셉니다.
   **`AL2` 처럼 짧은 값**이 기존 표와 어울립니다. 화면 이름만 「알리오(새)」 로.
3. 열쇠 — `JOB_API.KEY`(알리오 목록) · `ALIO_D.KEY`(상세) 둘 다 GitHub Secrets 로.
   지금은 `gas/wage.js` 안에 박혀 있습니다.
