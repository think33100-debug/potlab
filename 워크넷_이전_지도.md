# 워크넷 이전 지도 (2026-09-26)

알리오 다음 경로입니다. **지도만 만들었습니다. 수집기 코드는 한 줄도 안 썼습니다.**
표에 적은 숫자는 전부 오늘 직접 두드려 받은 것입니다 (집 IP · node).

---

## 1. 워크넷 경로가 쓰는 함수 전부

| 함수 | 줄 | 하는 일 | node 로 옮길 때 |
|---|---|---|---|
| `collectWorknet` | 2627 | **본체.** 8갈래 목록 → 합집합 → 판정 → 상세 → 줄 만들기 → 시트 | 새로 씀 |
| `workUrl_` | 2006 | 주소 만들기. 인증키는 UUID 라 감싸도 안전 | 그대로 |
| `wnFetch_` | 2601 | 두드리기. **4번까지 다시 시도** | ⚠ 아래 ② |
| `wnParseDetail_` | 5015 | 상세 XML → 항목표. 같은 이름이 또 오면 ` \| ` 로 이음 | 그대로 |
| `wnJudge_` | 5363 | **상세 본문으로 직군 가리기** (①②③④ 규칙) | `tools/` 로 옮겨 한 벌 |
| `wnCut_` | 5004 | 제목이 30자에서 잘렸나 | 그대로 |
| `wnFullTitle_` | 5010 | 상세의 `wantedTitle` 로 온전한 제목 되찾기 | 그대로 |
| `wnDate_` | 2586 | `26-09-27` → `2026.09.27` | 그대로 |
| `wnPay_` | 2593 | `월급 240~240만원` 한 줄 | 그대로 |
| `wnKind_` | 2471 | 기관 종별 가리기 (심평원 → 업종 신고 → 이름 추정) | DB 의 `org_public` 로 바꿈 |
| `wnMatcher_` | 2321 | 기관명·주소로 심평원 표에 붙이기 (4단계) | **DB 가 이미 함** (`org_group_mv`) |
| `wnVerdictSheet_`/`wnVerdicts_` | 2617 / 2623 | 「WN판정」 시트 — 한 번 버린 공고는 다시 안 엶 | DB 표 `wn_verdicts` (이미 있음) |
| `xmlItems_` | 1550 | `<wanted>` 덩어리 나누기 | 그대로 |
| `matchJob_`·`notOurs_`·`mixedTitle_`·`titleOtherOnly_` | — | 공통 규칙 | `tools/gas-rules.mjs` 에 **이미 있음** |
| `sameJob_`·`jobKeys_` | 10118 / 10132 | 다른 경로로 이미 들어왔나 | DB 질의로 |
| `regionNorm_` | 6361 | `경기도`→`경기` · `전남광주 광산구`→`광주 광산구` | 그대로 |
| `hireLabel_` | 3163 | 고용형태 말 다듬기 | 그대로 |
| `sortRows_`/`sortJob_` | 3389 / 3228 | **네 갈래 문지기** | `tools/sort-rule.mjs` 에 이미 있음 |
| `collectLog_`·`collectTally_`·`collectLater_` | 20984~ | 기준선 기록 | DB 표로 |
| `mailFresh_`·`orgMark_` | 10645 / 16858 | 새 공고 메일 · 기관 표시 | 나중에 |
| `fixWnTitles` | 4937 | 이미 담긴 잘린 제목 고치기 (한 번 쓰는 것) | 안 옮김 |
| `rejudgeWorknet` | 5389 | 이미 담긴 줄 다시 판정 | 안 옮김 |

**안 옮기는 것** — `wn_more`(예산이 모자라 못 연 것을 다음 깨어남에 이어감) ·
6분 예산 쪼개기 · 조사용 `surveyWorknet*` 넷.
node 에는 6분 한도가 없어 한 번에 다 돕니다 (아래 ③).

---

## 2. 시트 칸 ↔ DB 칸

`collectWorknet` 이 채우는 자리만 적습니다. `JCOLS` 순서, 다리(`tools/copy_jobs.js` `toJobPost`) 대응입니다.

| 시트 칸 | 자리 | 워크넷 어디서 | DB 칸 |
|---|---|---|---|
| 공고ID | 0 | `'WN' + wantedAuthNo` | `id` (앞 두 글자가 `source`) |
| 수집일 | 1 | 오늘 | `collected_at` |
| 기관명 | 2 | `company` | `org_name` |
| 공고명 | 3 | `title` → **상세 `wantedTitle`** | `title` |
| 채용구분 | 4 | `career` | `hire_type` |
| 고용형태 | 5 | `hireLabel_(empTpNm‖empTpCd)` | `employ_type` |
| 근무지 | 6 | `regionNorm_(region)` | `work_place` → `sido`·`sgg` |
| 학력 | 7 | `minEdubg` | `edu` |
| 인원 | 8 | `recruitCnt ‖ collectCnt` | `headcount` |
| 접수시작 | 9 | `wnDate_(regDt)` | `apply_from` |
| 접수마감 | 10 | `wnDate_(closeDt)` | `apply_to` |
| 링크 | 11 | `wantedInfoUrl` | `url` |
| 직군 | 12 | 직종코드 ‖ 제목 ‖ `wnJudge_` | `job_group` |
| 형태 | 13 | `mixedTitle_` 이면 `포함` | `form` |
| 보류 · 보류사유 | 27 · 28 | `wnJudge_` 가 「보류」 이거나 `WN_HOLD_ORG` | `hold` · `detail` |
| 공고일 | 29 | `wnDate_(regDt)` | `posted_at` |
| 기관종별 · 분류근거 | 30 · 31 | `wnKind_` | `org_kind` · `evidence` |
| 직군근거 | 32 | 판정 근거 한 줄 | `evidence` |
| 일정(23) | 23 | `채용시까지` 면 안내 한 줄 | `detail` |
| 연봉(25) | 25 | `wnPay_` | `detail` |
| 기관홈(26) | 26 | `wantedMobileInfoUrl` | `detail` |

`WN판정` 시트는 이미 DB 표 `wn_verdicts` 로 넘어가 있습니다 (다리가 옮깁니다).

---

## 3. 직접 두드려 확인한 것

### ① 목록도 상세도 한 주소 — `callTp` 로 가릅니다

```
https://www.work24.go.kr/cm/openApi/call/wk/callOpenApiSvcInfo210L01.do
  authKey=<UUID> & returnType=XML
  callTp=L  startPage=1  display=100        ← 목록 (display 최대 100)
  callTp=D  wantedAuthNo=K18…  infoSvc=VALIDATION   ← 상세
```

`GET` 입니다 (알리오처럼 POST 가 아닙니다). 인증키는 UUID 라 감싸도 안전합니다.

### ② `wnFetch_` 의 4번 재시도는 **구글 때문입니다 — node 에서는 필요 없습니다**

gas 주석에 「같은 주소가 어느 때는 263건을 주고 어느 때는 Address unavailable」
이라고 적혀 있습니다. 구글 쪽 출구 IP 가 요청마다 달라서, 워크넷이 막아둔 IP 를
만나면 실패하는 것입니다.

집 IP · node 에서 **19번 두드려 한 번도 안 막혔습니다.**
25곳 병원 홈페이지와 같은 이야기입니다 — 막는 건 구글 IP 입니다.

> 다만 GitHub Actions 도 남의 클라우드 IP 입니다. **거기서도 되는지는 아직 안 봤습니다.**
> 새 수집기 dry 를 GitHub 에서 한 번 돌려 확인해야 합니다 (알리오 때 0건 났던 것과 같은 자리).

### ③ 8갈래 합집합 — **334건 · 요청 19번 · 15초**

| 질의 | total | 새로 들어온 것 | 누적 |
|---|---:|---:|---:|
| 직종 306500 | 267 | 267 | 267 |
| 면허 6006184 (물리치료사) | 187 | 4 | 271 |
| 면허 6006189 (작업치료사) | 142 | 3 | 274 |
| 면허 6099839 (정신건강작업치료사) | 1 | 0 | 274 |
| kw 작업치료 | 205 | **38** | 312 |
| kw 작업치료사 | 199 | 3 | 315 |
| kw 물리치료 | 221 | **16** | 331 |
| kw 물리치료사 | 231 | 3 | 334 |

여덟 갈래가 다 값을 합니다. 특히 **낱말 검색이 57건을 더 찾습니다** — 직종코드로만
받으면 그만큼 놓칩니다. 면허 셋은 8건밖에 안 보태지만 요청 3번이라 그대로 둡니다.

gas 는 6분 예산 안에서 돌기 때문에 「예산의 절반을 넘기면 거기까지만」 이라는 줄이
있습니다 — 뒤쪽 낱말 검색이 잘릴 수 있습니다. node 는 15초라 전부 받습니다.

### ④ 상세를 몇 건이나 열어야 하나 — **334건 중 82건 + 잘린 제목 131건**

```
jobsCd 가 3065xx (바로 담기)     252건
그 밖 (상세를 열어 wnJudge_)      82건
제목이 30자 이상 (잘린 것)       131건   ← 직종코드로 담는 길도 한 번 열어야 함
```

상세 한 번이 0.4초 대기 + 왕복이라, 겹치는 것을 빼면 **170건 안팎 · 2분 남짓**입니다.
gas 는 이걸 6분 안에 못 끝내 `wn_more` 로 이어가고 있습니다.

### ⑤ 30자 잘린 제목 — 상세에 온전한 것이 있습니다 (어제 고친 그대로)

```
목록(30자) ... 보령시 취창업 박람회] 주간보호 물리치료사 구인
상세(33자) [2026년 보령시 취창업 박람회] 주간보호 물리치료사 구인

목록(30자) ...장) 작업치료사 또는 물리치료사선생님 채용합니다.
상세(34자) 다온요양원(확장) 작업치료사 또는 물리치료사선생님 채용합니다.
```

목록 100건 중 **39건이 30자**였습니다 (합집합 334건 중 131건).
`wnFullTitle_` 은 **더 길 때만** 바꿉니다 — 상세가 비어 와도 제목을 잃지 않습니다.
그리고 **판정보다 먼저** 바꿔야 잘린 자리의 직군 말을 `wnJudge_` 가 봅니다.

#### ⚠ 새 수집기에서 고칠 것 두 가지 (2026-09-26 에 소급하며 드러났습니다)

**① `wnCut_` 이 「30자 이상」 을 잘린 것으로 봅니다 — 아닙니다.**
워크넷 목록 제목은 30자를 넘어서도 옵니다. 62자짜리도 그대로 옵니다.

```
목록(62자) [수원일자리 박람회 참여 업체] 함께하실 노인요양시설 내 전문요양실 물리치료사 또는 작업치료사 선생님 모십니다.
상세(62자) (글자 하나까지 같습니다)
```

DB 로 세어보면 **「30자 이상」 161건 중 진짜 잘린 것은 86건뿐**이고,
그 86건은 **정확히 「…로 끝나는 것」과 같은 집합**입니다.
나머지 75건은 그냥 제목이 긴 것입니다 — 상세를 열 필요가 없습니다.

→ 새 수집기의 잘림 판정은 **`/\.\.\.$|…$/` 하나면 됩니다.** 길이는 안 봅니다.
   (gas 는 동결이라 안 고쳤습니다. 상세를 헛되이 75번 더 여는 것뿐이라 해는 없습니다)

**② 워크넷이 상세에서 제목을 자르지는 않습니다.**
잘린 채 아직 접수중인 30건의 상세를 전부 열어봤습니다 —
**상세 제목이 「…」로 끝난 것은 0건**입니다. 상세가 없어진 8건을 빼면 22건 다 온전했습니다.

```
K161132609110060  (30자) 주야간보호센터 물리치료사 또는 작업치료사를 모집합니다.
K180012609040024  (30자) 물리치료사 오후근무 가능하신 분 모십니다( 일 4시간)
K141212609110028  (29자) [구미 옥계] 물리치료사 채용 (달빛노인주간보호센터)
```

→ **「제목 일부」 같은 표시를 붙일 필요가 없습니다.** 상세만 열면 온전한 제목이 옵니다.
   못 고치는 것은 **마감이 지나 상세가 사라진 것**뿐입니다 (`정보가 존재하지 않습니다 · 006`).

### ⑥ 항목 — 목록 25개 · 상세 57개 (61개가 아니었습니다)

목록 (25개):
```
wantedAuthNo company busino indTpNm title salTpNm sal minSal maxSal region
holidayTpNm minEdubg career regDt closeDt infoSvc wantedInfoUrl
wantedMobileInfoUrl smodifyDtm zipCd strtnmCd basicAddr detailAddr empTpCd jobsCd
```

상세 (57개):
```
wantedAuthNo corpNm reperNm totPsncnt capitalAmt yrSalesAmt indTpCdNm busiCont
corpAddr homePg busiSize jobsNm wantedTitle relJobsNm jobCont receiptCloseDt
empTpNm collectPsncnt salTpNm enterTpNm eduNm forLang major certificate
mltsvcExcHope compAbl pfCond etcPfCond selMthd rcptMthd submitDoc etcHopeCont
workRegion nearLine workdayWorkhrCont fourIns retirepay etcWelfare disableCvntl
dtlRecrContUrl jobsCd minEdubgIcd maxEdubgIcd regionCd empTpCd enterTpCd salTpCd
staAreaRegionCd lineCd staNmCd exitNoCd walkDistCd attachFileUrl srchKeywordNm
empChargerDpt contactTelno chargerFaxNo
```

### ⑦ ⚠ **gas 는 상세 57개 중 7개만 씁니다** (CLAUDE.md 4번)

쓰는 것 — `wantedTitle` `jobsNm` `relJobsNm` `certificate` `etcPfCond` `pfCond` `jobCont`

안 쓰는데 값이 차 있는 것 (상세 20건을 열어 셌습니다):

| 항목 | 20건 중 | 무엇 |
|---|---:|---|
| `dtlRecrContUrl` | **20** | 워크넷 공고 상세 화면 주소 |
| `srchKeywordNm` | 13 | 공고에 붙은 검색 낱말 |
| `attachFileUrl` | **5** | **공고문 첨부 파일** |
| `selMthd`·`rcptMthd`·`submitDoc` | — | 전형방법·접수방법·제출서류 (상세 화면 칸들) |
| `collectPsncnt`·`empTpNm`·`receiptCloseDt` | — | 인원·고용형태·마감 (목록 것보다 정확할 수 있음) |
| `contactTelno`·`empChargerDpt` | — | 문의처 |
| `corpAddr`·`homePg`·`indTpCdNm` | — | 기관 주소·홈페이지·업종 |

**`attachFileUrl` 은 내려받지 못했습니다.** 두 번 두드려 실패해서 멈췄습니다 (CLAUDE.md 3번).

```
http://www.work.go.kr/framework/filedownload/keisDownload.do?filePathName=9SiDEb4ZtdZyoTn9
  그냥          → HTTP 500 · 0바이트
  UA+Referer   → HTTP 200 · 2,373바이트 text/html   ← 파일이 아니라 화면
  work24 쪽    → HTTP 404
```

알리오 첨부와 같은 모양입니다 — **포털 세션이 필요해 보입니다.** [확인 필요]
알리오는 `alioCookie_` 로 풀었으니 같은 길이 있을 수 있습니다. 다만 **지금은 급하지 않습니다**
— 아래 4번을 보시면 워크넷 첨부를 읽어야 할 1층 공고가 거의 없습니다.

`selMthd`·`rcptMthd`·`submitDoc`·`contactTelno` 는 **첨부 없이 바로 상세 칸에 들어갑니다.**
지금 워크넷 공고의 상세 화면이 알리오보다 휑한 이유가 이것입니다. 새 수집기에서 채웁니다.

---

## 4. 기준선 — 지금 워크넷이 내는 것

DB `job_posts where source='WN'` (2026-09-26):

```
전체 413건 (전부 최근 30일)
  회원 목록   380
  보류함        7
  숨김         26
  쓰레기통     13 (job_trash)

직군   물리치료사 179 · 작업치료사 114 · 공통 101 · 빈칸 19
제목이 정확히 30자(잘린 채 담긴 것)  86건   ← fixWnTitles 를 아직 다 못 돌린 자리
```

기관 종별로:

| 기관종별 | 전체 | 회원목록 | 보류 | 숨김 |
|---|---:|---:|---:|---:|
| 요양원 | 148 | 135 | 3 | 10 |
| 요양원·주야간보호 | 89 | 82 | 0 | 7 |
| 요양병원 | 43 | 42 | 0 | 1 |
| **공공·복지기관** | **42** | 38 | 1 | 3 |
| 병원 | 26 | 26 | 0 | 0 |
| 의원 | 24 | 24 | 0 | 0 |
| 기타 | 20 | 16 | 1 | 3 |
| 재활센터 | 10 | 6 | 2 | 2 |
| **종합병원** | **6** | 6 | 0 | 0 |
| 병원·의원 | 3 | 3 | 0 | 0 |
| 한방병원 | 2 | 2 | 0 | 0 |

---

## 5. 그래서 — **워크넷은 1층 경로가 아닙니다**

**413건 중 1층(공공·종합·대학)은 48건 (12%)** 입니다. 공공·복지기관 42 + 종합병원 6.
나머지 365건(88%)은 요양원·요양병원·의원·재활 — 2층입니다.

CLAUDE.md 의 판단 기준 그대로 재면:

- 「이게 공공·종합·대학 커버리지를 올리는가?」 → **12% 만.**
- 다만 그 42곳(공공·복지기관 — 치매안심센터·복지관·보건소류)은 **다른 경로에 잘 안 나옵니다.**
  알리오는 공공기관 경영정보 대상만, 나라일터는 공무원 자리만 냅니다.
  워크넷은 **작은 공공·복지기관을 잡는 유일한 그물**입니다.

그리고 워크넷은 **이미 잘 돌고 있습니다.** 413건 · 보류 7건뿐입니다.
클린아이처럼 74% 를 못 가리는 상태가 아닙니다.

### 옮겨서 실제로 나아지는 것

| | 지금 (gas) | 옮기면 |
|---|---|---|
| 8갈래를 다 받나 | 예산 절반을 넘기면 잘림 | 15초라 항상 다 받음 |
| 상세 열기 | 6분에 못 끝내 `wn_more` 로 이어감 | 2분에 끝남 |
| 막힘 | 구글 IP 라 「Address unavailable」 · 4번 재시도 | 집/Actions IP — 확인 필요 |
| 잘린 제목 | 86건이 잘린 채 남아 있음 | 처음부터 온전히 |
| 상세 칸 | 57개 중 7개만 씀 | 전형방법·접수방법·제출서류·문의처를 채움 |

**막히지도 않고 빠지지도 않는 경로라, 옮겨서 얻는 게 알리오만큼 크지 않습니다.**
얻는 건 ① 예산에 잘리지 않음 ② 상세 칸이 찬다 — 둘입니다.

---

## 6. [4] 로 가기 전에 정하실 것

1. **워크넷을 지금 옮길까요, 나라일터를 먼저 할까요.**
   [1] 에서 정하신 순서는 「워크넷 → 나라일터」 였습니다. 위 숫자를 보시고
   바꾸실 수 있습니다 — 나라일터는 100% 공공이라 1층 비중이 워크넷의 8배입니다.
2. **`attachFileUrl` 을 파고들까요.** 세션이 필요해 보입니다. 1층 공고가 48건뿐이라
   미뤄도 될 것 같습니다.
3. **잘린 제목 86건** — 이미 담긴 것은 안 고쳐집니다 (CLAUDE.md 9번).
   새 수집기를 붙일 때 `fixWnTitles` 를 한 번 더 돌리거나, 새 수집기가 덮어쓰게 합니다.

옮기기로 하시면 [4] 에서 — `tools/collect-worknet.mjs` → dry → 사흘 대조,
알리오와 같은 차례로 합니다. `wnJudge_` 는 `tools/gas-rules.mjs` 에 얹습니다 (규칙 한 벌).
