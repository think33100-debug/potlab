## 이 프로젝트의 목표

**공공기관·종합병원·대학병원 채용공고를 하나도 안 빼고 올린다.**
많이 안 뽑는 곳이라도 하나 빠지면 "여기 오면 다 있다"가 무너진다.

대상은 셋 중 하나라도 걸리면 포함한다.
1. 나라·지자체가 운영하는 모든 기관 (국립대병원·국립병원·지방의료원·보훈·산재·
   보건소·치매안심센터·국립센터류·지자체 직영 복지관/재활원, 시립·도립·군립 요양시설)
2. 심평원 종별이 상급종합 또는 종합병원
3. 이름에 대학병원이 들어가면 종별 무관

재활·요양은 기본으로 올리되 중요도가 낮다. 나중에 유료화할 자리다.

## 일하는 순서

1. 대상 기관 목록을 만든다
2. 우리가 긁는 곳과 대조해 구멍을 센다
3. 구멍을 메운다 — API 우선 → 안 되면 홈페이지 긁기 →
   그래도 안 되면 기관 채용 알림을 받아 봇이 1차 검증하고 사람이 확인
4. 대상 몇 곳을 보고 있는지 화면에 밝힌다

## 목표에 비춘 판단 기준

- 작업을 시작하기 전에 묻는다. **"이게 공공·종합·대학 커버리지를 올리는가?"**
  아니면 뒤로 미룬다.
- 요양원·주간보호 분류를 정교하게 다듬는 데 시간을 쓰지 않는다.
- 커버리지는 느낌이 아니라 숫자로 말한다.
  "대상 N곳 중 최근 30일 공고가 들어온 곳 M곳"
- 구멍을 메우기 전에 한 곳으로 품을 재본다. 재기 전에 전체 계획을 세우지 않는다.
- 전부 자동일 필요는 없다. 안 되는 곳은 「손으로 확인할 곳」 목록에 넣고
  마지막 확인일을 남긴다. **빠지지 않는 것이 자동화보다 중요하다.**
- 재활·요양 공고에는 조회수를 남긴다. 나중에 값을 매길 근거가 된다.

---

# POT JOB 작업 지침

2026년 9월 6~8일 네 번의 작업에서 실제로 일어난 실수를 바탕으로 만들었습니다.
새 대화를 시작할 때 이 파일을 먼저 읽히면 같은 실수를 줄일 수 있습니다.

---

## 세션을 넘어 반복된 실수

같은 실수가 하루 이틀 만에 다시 나왔습니다. 이게 제일 큰 문제입니다.

| 실수 | 언제 |
|---|---|
| 진단 함수를 만들면서 원문을 안 찍음 | 세션 3, 세션 4 |
| 주소·명세를 짐작으로 만듦 | 세션 3, 세션 4 (4회) |
| 이미 있는 이름과 충돌 | 세션 1, 세션 4 |
| 블록을 통째로 갈아끼우다 다른 함수를 날림 | 세션 1, 세션 4 |
| 뒤로가기 자리를 다시 안 쌓음 | 세션 2, 세션 4 |
| 원인을 확인하지 않고 고침 | 세션 2, 세션 4 (다섯 번 헛짚음) |
| API 기능을 다 안 보고 「안 된다」 함 | 세션 4 (알리오·나라일터 둘 다) |

---

## 1. 원인을 확인하지 않고 고치지 않기

세션 4에서 새로고침 유지 하나를 다섯 번 헛짚었습니다.

```
1회  wipe() 가 해시를 지우는 줄            → 아님
2회  KEEP_HASH 선언 위치                  → 아님
3회  openFromHash 시점                    → 아님
4회  tab('home') 이 주소를 밀어버림         ← 진짜 원인
5회  화면만 열고 자료를 안 불러옴            → 또 다른 문제
```

세션 2에서도 확인 주소를 두 번 틀리게 드렸습니다. 스크립트가 죽어 있어 그 검사에 닿지도 못했는데 그걸 몰랐습니다.

### 지킬 것

- 고치기 전에 "이게 원인이라는 증거가 무엇인가" 를 먼저 답할 것
- 같은 증상을 두 번 고쳐서 안 되면, 세 번째는 코드를 만지지 말고 진단부터
- 좋은 진단의 예: `history.pushState` 를 가로채 호출 순서를 전부 기록 → `push /` 한 줄에서 원인이 드러남

---

## 2. 진단 함수는 원문을 찍을 것

세션 3과 4에서 같은 실수를 했습니다.
확인용 함수를 만들면서 정작 응답 원문을 안 찍어, 진단 도구가 진단을 못 했습니다.

### 지킬 것

- 진단 함수는 응답 원문 500~1,200자를 반드시 포함
- 항목 이름 목록(`Object.keys`)을 함께 찍을 것
- "몇 건 왔다"만 찍는 진단은 쓸모없음

---

## 3. 주소·명세를 짐작으로 만들지 않기

네 번 지어냈습니다.

- 광역치매센터 17곳 주소를 규칙으로 생성 → `seoul.nid.or.kr` 은 없는 주소
- 고용24 서비스 번호를 후보로 두드림 → 못 찾음
- 온통청년 `/opi/` 경로 → 2025년 개편으로 바뀐 것
- 세션 3에서도 지어낸 주소에 `[Unverified]` 만 붙이고 넘어감

### 지킬 것

- 주소 하나를 확인했다고 나머지를 규칙으로 만들지 말 것
- 후보를 두 번 두드려 실패하면 멈추고 명세를 요청할 것
- `[Unverified]` 는 면죄부가 아님. 확인할 수 있으면 확인부터

실제로 해결된 방식 — 온통청년·고용24 모두 세중님이 명세 캡처 한 장을 주셔서 끝났습니다.

---

## 4. API 는 신청한 기능을 전부 확인하고 시작하기

같은 실수를 두 번 했습니다.

**알리오** — 「의료기술직」 으로 뭉뚱그린 공고를 못 가려 보류함이 넘쳤습니다.
「자동은 어렵습니다」 라고 포기하려 했는데, 세중님이
「상식적으로 알리오가 저걸 분류 못 한다는 게 이상하잖아」 라고 하셨습니다.

명세를 다시 보니 이랬습니다.

```
쓰던 것     /list        목록만
안 쓰던 것   /detail      「채용전형단계, 채용첨부파일 메타정보」
```

`/detail` 을 열자 `steps` 에 직군이 나뉘어 있었습니다.

```
의료기술직(물리치료사)  ← 여기 다 적혀 있었음
일반(작업치료사)-(1)    ← 인원까지
```

API 한 번이면 끝날 일을 화면 긁기·PDF 읽기로 헤맸습니다.

**나라일터** — 그러고 나서 또 같은 상태였습니다.
신청한 6개 중 3개만 쓰고 있었습니다.

```
쓰던 것     getList · getItem · getItemFile
안 쓰던 것   getItemPosition · getClsfList · getInsttList
```

`getItemPosition` = 「검색 고유값에 해당하는 채용직급을 조회」 —
알리오의 `steps` 와 같은 것인데 안 보고 있었습니다.

### 지킬 것

새 API 를 붙이기 전에

1. 신청 화면의 「상세기능」 표를 전부 적어둘 것. 하나도 빠짐없이
2. 각 기능의 설명을 끝까지 읽을 것.
   「첨부파일 메타정보」 「채용직급 조회」 같은 말이 답인 경우가 많습니다
3. 참고문서(코드 정의서·활용가이드)를 열어볼 것.
   첨부유형 `A=공고문 B=지원서 C=직무기술서` 도 거기 있었습니다
4. 그다음에 응답 원문을 찍는 진단 함수

막혔다고 느낄 때 가장 먼저 할 일

> 「내가 이 API 의 기능을 전부 쓰고 있나?」

화면을 긁거나 PDF 를 읽기 전에 이걸 먼저 물어야 합니다.
공공기관이 목록만 주고 상세를 안 줄 리가 없습니다.

「자동은 어렵습니다」 라고 말하기 전에

1. 신청한 기능 목록을 다시 볼 것
2. 안 쓰는 기능이 있으면 반드시 두드려볼 것
3. 명세서를 다시 읽을 것
4. 그래도 없으면 그때 다른 길을 찾을 것

포기가 빠르면 세중님이 대신 찾아내야 합니다. 그건 제 몫입니다.

---

## 5. 응답을 보고 코드를 쓰기

세션 4에서 네 번 놓쳤습니다.

| 놓친 것 | 실제 |
|---|---|
| 첨부가 `href` 에 있는 줄 | `onclick` 안, 작은따옴표 |
| PDF 를 못 읽는 줄 | `&amp;` 를 안 풀어 HTML 을 받고 있었음 |
| 첨부 주소가 없는 줄 | `/getItemFile` 이 기능 목록에 있었음 |
| 직군만 필요한 줄 | 고용형태·인원·자격이 같은 글에 있었음 |

세션 3에서는 `rgPlain_` 을 쓰려다 `<2026년 한가위 정담장터>` 가 통째로 사라질 뻔했습니다. 꺾쇠를 태그로 오해하는 함수였습니다.

### 지킬 것

- 응답 원문을 찍는 진단 함수 → 항목 확인 → 그다음에 코드
- XML 응답은 `&amp;` `&#38;` 를 반드시 풀 것
- 받은 파일이 정말 그 형식인지 확인 (PDF 는 앞 5글자가 `%PDF-`)
- 신청한 API 의 기능 목록을 끝까지 읽을 것
- 기존 함수를 재활용할 때 그 함수가 무엇을 하는지 먼저 읽을 것

---

## 6. 이미 있는 것을 먼저 찾기

**세션 1** — `FSHEET` 를 쓰려 했는데 1318줄에서 건의사항 시트가 이미 쓰고 있었습니다. 중복 선언이라 스크립트 전체가 안 돌았습니다.

**세션 4** — `drawOrg` 가 이미 있는데 서버를 다른 이름(`x.state` vs `x.ok`)으로 만들었습니다. 탭 8개 중 `openHold` 에만 "눌린 표시" 줄을 빼먹었습니다.

### 지킬 것

- 새 상수·함수 이름은 반드시 `grep` 으로 먼저 검색
- 화면과 서버를 잇는 항목 이름은 한쪽을 열어보고 다른 쪽을 맞출 것
- 비슷한 기능이 여럿이면(탭 8개 등) 전부 훑어 빠진 것을 찾을 것

---

## 7. 코드 블록을 통째로 갈아끼우지 않기

**세션 1** — `collectTick` 주석부터 `setupJobTrigger` 까지를 잘라내고 새 블록을 넣었는데, 그 사이에 함수 14개가 들어 있었습니다. 초임·정리·시트 만들기가 통째로 날아갔습니다.

**세션 4** — 편집 중 파일을 한 번 깨뜨렸다가 되살렸습니다. 별첨 21개가 1개로 줄어든 적도 있습니다.

### 지킬 것

- 큰 블록 교체 대신 작은 문자열 치환을 쓸 것
- 잘라낼 범위 안에 다른 함수가 있는지 먼저 확인
- 편집 후 핵심 문자열이 실제로 들어갔는지 확인 — 여러 편집 중 하나가 실패하면 앞부분도 저장이 안 됨
- 파일 크기·줄 수가 예상과 맞는지 볼 것

---

## 8. Apps Script 특유의 함정

**밑줄로 끝나는 함수는 실행 목록에 안 뜹니다.** (세션 1)
`initPaySheet_` 를 만들어놓고 "실행하세요"라고 안내했는데 목록에 없었습니다.
→ 세중님이 직접 실행할 함수는 밑줄 없이 지을 것

**캐시가 옛 자료를 물고 있습니다.** (모든 세션에서 가장 자주)
`payMap_` 은 6시간, 목록은 21600초 캐시합니다.
→ 자료를 바꾼 뒤에는 `refreshAll` 안내를 반드시 붙일 것

**한 번 실행은 6분까지.**
→ 오래 걸리는 작업은 어디까지 했는지 기억했다가 이어가는 방식으로

**주소 길이 한도.** 한글은 주소에서 9배로 부풉니다.
→ 긴 요청은 600자씩 조각내어 JSONP 로. form+iframe 은 응답을 못 읽어서 안 됨

**PDF 읽기** — Drive `convert: true` 만. `mimeType` 을 지정하면 충돌

**`.hwp` 는 못 읽음** → 보류함으로

---

## 9. 이미 쌓인 자료는 안 고쳐집니다

세션 1에서 지적받은 것 — 수집기를 고쳐도 이미 시트에 쌓인 자료는 그대로입니다. 언어재활사 공고가 남아 있었는데 치우는 코드를 안 만들었습니다.

세션 4에서도 같은 일이 있었습니다. PDF 뽑기를 넣었는데 이미 담긴 공고는 그대로였습니다.

### 지킬 것

- 수집 규칙을 고치면 "이미 담긴 것은 어떻게 할지" 를 함께 말할 것
- 지우고 다시 받는 함수를 같이 만들 것 (`cleanNidRows` 같은)
- 안내할 때 시트를 비우는 단계를 반드시 포함

---

## 10. 확인 없이 단정하지 않기

세 번 틀렸습니다.

- 경쟁사가 "급여 정보 없다" → 실제로 연봉정보 게시판이 있었음
- "우리만 직업정보제공사업 신고" → 확인할 방법 없음
- "하루 트래픽 1,000건" → 실제 10,000건. 이 때문에 하루 60건만 열고 있었음

세션 4 — 지원서 그림에 「회원 34명이 올린 병원별 초임」 을 그렸습니다. 실제 코드는 「직군 구분 없는 기관 전체 평균 · 공시 자료」였습니다.

### 지킬 것

- 남(경쟁사·기관)에 대한 단정은 직접 확인한 것만
- 한도·제한 수치는 승인 화면에서 확인
- 그림·문서를 만들 때 실제 코드를 열어 확인하고 그릴 것
- 지원서·홍보물은 더 엄격하게. 심사에서 확인되면 전체 신뢰를 잃음

---

## 11. 배포 탓하지 않기

같은 문제를 세 번 "서버 배포 안 하신 것 같다" 고 했습니다. 실제 원인은 주소 길이 한도였습니다.

### 지킬 것

- 배포·캐시·업로드를 원인으로 지목하려면 근거를 함께 댈 것
- 근거가 없으면 화면에 진짜 이유가 뜨게 만들 것
- 좋은 예: `app.js 를 못 불러왔습니다 / 다리 a24 / GitHub 확인`
- 나쁜 예: `서버에 연결하지 못했습니다`

---

## 12. 시험은 실제와 같은 조건에서

`jsdom` 으로 통과했는데 실제 브라우저에서 안 된 일이 있었습니다.
`jsdom` 은 스크립트를 다 읽은 뒤 실행해서 선언 순서 문제가 안 드러납니다.

### 지킬 것

- 시험이 통과했다고 "된다"고 말하지 말 것. "이 조건에서는 통과했다"
- 시험 방식이 실제와 어떻게 다른지 먼저 밝힐 것
- `node --check` 는 실행 순서 문제를 못 잡음

---

## 13. 매번 지킬 것 (체크리스트)

### 코드를 고친 뒤

```
1. node --check                문법
2. audit.py                    API 등록 · 함수 존재 · style/script 섞임
3. check1.py                   tab() 라우팅 · 버튼 함수
4. 새 이름을 grep 으로 검색      중복 선언 방지
5. 화면↔서버 항목 이름 일치
6. 버전 올리기                  SERVER_VER · APP_VER · app.js?v=
7. 끝줄바꿈
8. 편집이 실제로 들어갔는지 확인
```

### 새 API 를 붙일 때

```
1. 신청 화면의 「상세기능」 을 전부 적기   ← 하나도 빠뜨리지 말 것
2. 각 기능 설명을 끝까지 읽기
3. 참고문서(코드 정의서·활용가이드) 열어보기
4. 응답 원문을 찍는 진단 함수
5. 항목 이름을 눈으로 확인
6. 그다음에 수집 코드
7. API_LIST 에 한 줄 추가
```

### 이미 쓰던 API 가 막혔을 때

```
1. 신청한 기능 중 안 쓰는 게 있나?      ← 이것부터
2. 명세서를 다시 읽었나?
3. 요청 방법이 맞나? (GET/POST · 이름표 · 키)
4. 그다음에 화면 긁기·PDF 읽기 같은 다른 길
```

### 안내할 때

```
· 올릴 파일을 정확히 나열 (Wage.gs 는 "새 버전 배포" 명시)
· 시트를 비워야 하면 그 단계도
· refreshAll 필요 여부
· 실행할 함수를 번호 순서로
```

---

## 14. 세중님 쪽에서 도와주시면 좋을 것

**로그는 통째로**
잘린 로그 때문에 두 번 헛짚었습니다. 길면 나눠서라도 전부 주시는 게 빠릅니다.

**증상을 구체적으로**
"안 된다"보다 "이 문구가 뜬다" 가 훨씬 빠릅니다. 캡처 한 장이 대화 열 번을 줄입니다.

**명세·승인 화면 캡처**
온통청년·고용24·중앙치매센터 모두 캡처 하나로 해결됐습니다.

**"몇 번째다"라고 말씀해주세요**
그 말을 듣고 접근을 바꿔 원인을 찾았습니다. 두 번 이상이면 알려주세요.

**실행 결과를 알려주세요**
"됐다"도 중요합니다. 안 되는 것만 말씀하시면 제가 뭘 고쳤는지 확신을 못 합니다.

---

## 15. 이 프로젝트 자료의 성격

- 공고 제목에 직군이 없는 경우가 많음 → 제목 → 상세 → 첨부 PDF 순으로
- 여러 직군 통합 공고는 인원이 우리 직군 인원이 아님
- 마감 지난 공고·결과 발표 공고는 회원에게 보여주지 않음
- 못 가린 공고는 버리지 말고 보류함으로. 관리자가 확인 후 올림
- 확실하지 않은 숫자(경쟁률 등)는 만들지 않음
- 출처와 한계를 화면에 밝힘 (예: "직군 구분 없는 기관 전체 평균")
- 쓰는 API 10곳 · 못 붙인 곳 3곳 — 관리자 → 기관 현황 탭에서 볼 수 있음
