# POT JOB 인수인계

2026년 9월 8일 기준 · 서버 v243 · 화면 v243 · 다리 a24

---

## 1. 지금 무엇이 돌아가는가

**주소** potjob.co.kr (GitHub Pages) + Apps Script(Wage.gs) + JSONP(app.js)

**파일 4개**
```
Wage.gs      서버. Apps Script 에 붙여넣고 「새 버전 배포」
index.html   회원 화면. GitHub 에 올림
admin.html   관리자 화면. GitHub 에 올림
app.js       화면↔서버 다리. GitHub 에 올림
```

**주의** — `app.js` 를 고치면 `index.html`·`admin.html` 의 `app.js?v=숫자` 를
둘 다 올려야 합니다. 안 그러면 옛 파일을 물고 있습니다.

---

## 2. 쓰는 공공데이터 10곳

| 어디 | 무엇 | 함수 |
|---|---|---|
| 재정경제부 공공기관 채용정보 | 중앙 공공기관 공고 · 전형단계 · 첨부 | `collectJobs` |
| 클린아이 잡플러스 | 지방공공기관 · 지방의료원 | `collectJobs2` |
| 나라일터 | 지자체 · 보건소 | `collectJobs3` |
| 중앙치매센터 | 광역·치매안심센터 (화면 긁기) | `collectNid` |
| 온통청년 | 청년정책 2,745건 | `pullYouth` |
| VMS | 봉사활동 | `volList` |
| 사회서비스 바우처 | 발달재활 기관 37,661곳 | `pullVoucherLocalAll` |
| 심평원 | 병원 15,332곳 | 병원 자료 |
| 국가자격 시험일정 | 시험 일정 | `testExam` |
| 카카오 | 로그인 | `kakaoExchange` |

**못 붙인 곳 3곳** — 워크넷(명세 못 찾음) · 전문연구정보센터(서버 죽음) · 고용24 3종(서비스 번호 못 찾음)

관리자 → **기관 현황** 탭에서 볼 수 있습니다.

---

## 3. 공고를 어떻게 가려내는가

이게 이 프로젝트의 핵심이자 가장 어려운 부분입니다.

**문제** — 공고 제목에 직군이 없는 경우가 대부분입니다.
```
「2026년 제4차 정규직 공개채용 공고」   ← 51명 뽑는데 작업치료사 1명 포함
「지역사회중심재활사업 기간제근로자 채용」  ← 직군이 첨부에만
```

**지금 쓰는 순서**
```
1. 제목에 직군 있나
2. 없으면 상세(API)를 열어봄
   · 알리오 → steps 에 직군이 나뉘어 있음 「의료기술직(물리치료사)」
   · 나라일터 → 상세 본문
3. 그래도 없으면 첨부 공고문 PDF 를 읽음
   · 알리오 → atchFileType 이 'A' 인 것 (A=공고문 B=지원서 C=직무기술서)
   · 나라일터 → /getItemFile
   · 치매센터 → onclick 안의 download.aspx
   · PDF 는 Drive convert:true 로 글자를 뽑음
4. 못 가리면 보류함 → 관리자가 공고문 보고 결정
```

**직군별 인원도 뽑습니다.** `steps` 제목이 「일반(물리치료사)-(2)」 이면 2명입니다.
전체 인원(51명)이 아니라 우리 직군 인원을 보여줍니다.

---

## 4. 한글(.hwp) 읽기 — 새로 붙인 것

`.hwp` 는 OLE 복합문서라 본문이 압축돼 있습니다. Apps Script 로는 못 풉니다.
Drive 도 `.hwp` 를 못 엽니다.

**그런데 「PrvText」(미리보기 글) 는 압축 없이 UTF-16LE 로 들어 있습니다.**
문서 앞부분 1,000자쯤이라 제목·모집분야가 대개 여기 있습니다.

그래서 **파일 전체를 바이트로 훑어 UTF-16LE 패턴을 찾습니다.**
```
작업치료 = 91 C7 C5 C5 58 CE CC B8
물리치료 = 3C BB AC B9 58 CE CC B8
```

**함수** `hwpFind_(blob)` · `hwpJob_(url)` · `testHwp()`

**아직 실제 파일로 확인 안 했습니다.** `testHwp` 를 돌려서 되는지 봐야 합니다.
- 되면 → `collectNid` 와 `collectJobs3` 에 붙이면 한글 공고도 자동으로 가려집니다
- 안 되면 → 미리보기가 없는 파일이거나 본문에만 있는 경우입니다

**못 찾았다고 「없다」고 단정하면 안 됩니다.** 압축된 본문은 안 걸리니까요.
찾으면 담고, 못 찾으면 보류함으로 보내야 합니다.

---

## 5. 지금 제일 큰 문제 셋

### ① 한글 첨부 (가장 중요)

치매센터·보건소 공고의 상당수가 `.hwp` 로만 올립니다.
용산구·세종광역·구로구가 그래서 못 들어왔습니다.

4번의 방법이 통하면 대부분 풀립니다. **`testHwp` 부터 돌려보세요.**

안 되면 대안 셋
- **Cloud Run 에 파이썬 변환 서버** — `pyhwp` 라이브러리로 hwp→텍스트.
  월 몇천 원. 가장 확실하지만 서버를 하나 더 관리해야 합니다
- **한컴독스 API** — 있는지 확인 안 됨
- **hwpx 만 지원** — 신형 한글(.hwpx)은 ZIP+XML 이라 `Utilities.unzip` 으로 열립니다.
  다만 아직 .hwp 가 훨씬 많습니다

### ② 워크넷 채용정보

**민간 병원·요양원·발달재활센터 공고가 통째로 빠져 있습니다.**
지금은 공공기관 공고만 들어옵니다. 회원 대부분이 민간에서 일하는데요.

고용24에서 명세를 못 찾아 막혔습니다.
- 고용24 마이페이지에서 요청 주소 확인
- 또는 공공데이터포털에서 「워크넷 채용정보」 검색해 따로 신청

### ③ 알림 발송기

`pushPending` 을 부르는 곳이 우리 파일에 없습니다. **별도 서버가 있는 구조**로 보입니다.
`알림 보낸 것 17건` 이라 예전에 돌긴 했는데, 지금도 도는지 확인이 안 됐습니다.

가오픈 전에 **`testNoti`** 로 확인하시고, 회원이 「알림 받기」를 켠 뒤 실제로
알림이 오는지 봐야 합니다.

---

## 6. 앞으로의 방향

**가까이 (가오픈 전)**
- `testHwp` 로 한글 읽기 확인 → 되면 붙이기
- `collectJobs` 를 여러 번 돌려 첫 바퀴 완주 (12,000건)
- 알림이 실제로 가는지 확인
- 보류함 정리 (`reviewHold`)

**중간 (가오픈 후 1~2주)**
- 워크넷 뚫기 — 민간 공고가 없으면 반쪽입니다
- 회원 반응 보고 화면 다듬기
- SNS 자동 발행 (지금은 메일+드라이브로 받아 손으로 올림)

**멀리**
- Supabase 이전 — 회원이 늘면 Apps Script 로는 버겁습니다
- 네이티브 앱 — PWA 는 뒤로가기를 못 막습니다
- 병원 리뷰 · 면접 후기 — 커뮤니티가 자리 잡은 뒤

---

## 7. 자주 쓰는 함수

**수집**
```
collectAllJobs   전부 한 번에 (6분 제한으로 중간에 끊길 수 있음)
collectJobs      알리오 (운영계정 · 하루 10만건)
collectJobs2     잡플러스
collectJobs3     나라일터 (이어서 돌기)
collectNid       치매센터
pullYouth        청년정책 (이어서 돌기)
```

**정리**
```
reviewHold       보류함을 다시 자동 판별
cleanNidRows     치매센터 공고 지우고 다시 받기
resetAlio        알리오 처음부터 다시
resetJob3        나라일터 처음부터 다시
refreshAll       캐시 비우기 ← 자료 바꾼 뒤 꼭
```

**확인**
```
testNoti         알림이 갈 상태인지
testHwp          한글 읽기가 되는지
testNidRows      치매센터 공고 상태
testCommon       「공통」으로 잘못 담긴 공고
whyMissed('...')  놓친 공고의 원인
testMail         메일 상태
sendHoldMailAgain 보류함 메일 다시 보내기
```

---

## 8. 반드시 지킬 것

**Apps Script 함정**
- 밑줄(`_`)로 끝나는 함수는 실행 목록에 안 뜸
- 한 번 실행 6분 → 이어서 돌기 방식으로
- 캐시 6시간 → 자료 바꾸면 `refreshAll`
- 주소 길이 한도 → 한글은 9배로 부풂. 긴 요청은 600자씩 조각내기
- PDF 는 Drive `convert: true` 만 (`mimeType` 지정하면 충돌)

**자료를 다룰 때**
- XML 응답은 `&amp;` 를 반드시 풀 것 (안 풀면 HTML 이 옴)
- 받은 파일이 진짜 그 형식인지 확인 (PDF 는 앞 5글자가 `%PDF-`)
- 수집 규칙을 고치면 **이미 쌓인 자료는 안 바뀜** → 지우고 다시 받는 함수도 같이
- 확실하지 않은 숫자(경쟁률 등)는 만들지 않음
- 출처와 한계를 화면에 밝힘

**회원에게**
- 못 가린 공고는 버리지 말고 보류함으로
- 마감 지난 공고·결과 발표 공고는 안 보여줌
- 여러 직군 통합 공고는 인원이 우리 직군 인원이 아님을 알림

---

## 9. 남은 잔가지

- **날짜 오류** — 어느 화면인지 확인 필요. 제보만 있고 재현 못 함
- 알리오 첨부 내려받기가 HTML 을 돌려줌 (`testAtch` 로 확인 가능)
- 청년정책 시트를 다시 받아야 링크·마감일이 채워짐
- 지원서용 화면 캡처 (실제 앱에서)
- 운영계정 활용사례 등록 (`POTJOB_활용사례_신청서.md` 참고)

---

## 10. 작업할 때

`POTJOB_작업지침.md` 를 새 대화 시작할 때 먼저 읽히세요.
네 세션에서 반복된 실수 21회를 정리해뒀습니다.

특히 이것들이 반복됐습니다.
- 원인을 확인하지 않고 고침 (한 문제를 다섯 번 헛짚음)
- 주소·명세를 짐작으로 만듦
- 진단 함수를 만들면서 원문을 안 찍음
- 이미 있는 이름과 충돌
