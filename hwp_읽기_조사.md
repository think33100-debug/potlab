# .hwp 를 읽을 수 있나 — 조사 (2026-09-26)

**조사만 했습니다. 읽는 기능은 안 만들었습니다.**

`.hwp` 는 지금 「못 읽음 → 보류함」 으로 처리하고 있습니다 (CLAUDE.md 8번).
정말 못 읽는지 실제 파일로 확인했습니다.

---

## 답 — **읽힙니다. 라이브러리 없이.**

클린아이 의료 공고의 `.hwp` 첨부 4건을 받아 시험했습니다. **4건 다 나왔습니다.**

| 기관 | 꼴 | 크기 | 뽑힌 한글 |
|---|---|---:|---:|
| 부산의료원 (사회복지사) | HWP 5.x (OLE) | 125,440 | 4,588자 |
| 부산의료원 (기록물관리) | HWP 5.x (OLE) | 65,024 | 2,279자 |
| 대구의료원 (감사실장) | HWP 5.x (OLE) | 53,248 | 2,234자 |
| 경기도의료원 이천병원 (간호사) | HWP 5.x (OLE) | 110,592 | 3,502자 |

뽑힌 글 예 (부산의료원) —

```
공고 제2026-32호 부산광역시의료원 직원 채용 공고 채용분야 및 채용인원
구 분 채용부문 채용인원 공고(접수)기간 비 고 기간제 사회복지사 …
근무지 발달 및 뇌병변 치료지원센터 상세업무 및 근무형태는 직무설명서 참고
근무기간 채용일로부터 1년 … 응시자격 구 분 주 요 내 용 자 격 …
```

**채용분야·인원·근무지·자격이 다 들어 있습니다.** 직군을 가리기에 충분합니다.

---

## 어떻게 읽었나

### 라이브러리는 셋 다 실패했습니다

| | 결과 |
|---|---|
| `hwp.js` | 못 씀 |
| `@ohah/hwpjs` | 못 씀 |
| `node-hwp` | 못 씀 |

### 된 것 — **UTF-16LE 로 그냥 훑기**

HWP 5.x 는 OLE 복합문서(`d0 cf 11 e0`)이고, 글은 **UTF-16LE** 로 들어 있습니다.
스트림이 눌려 있지 않으면 파일을 통째로 `utf16le` 로 읽어 한글 토막만 주워도 나옵니다.

```js
const t = buf.toString('utf16le');
const 글 = (t.match(/[가-힣][가-힣\s0-9A-Za-z().,·~\-]{1,}/g) || []).join(' ');
```

눌린 경우(`78 01/9c/da` zlib 머리)도 대비해 풀어서 훑는 길을 같이 뒀는데,
**시험한 4건은 전부 맨손 쪽이 이겼습니다** — 안 눌려 있었습니다.

표는 칸 순서대로 이어 붙습니다 (`구 분 채용부문 채용인원`).
표 모양은 잃지만 **낱말은 다 남습니다.** 직군 판정에는 문제가 없습니다.

> **한계** — 눌린 파일은 아직 실제로 못 만나봤습니다. 만나면 zlib 쪽이 받습니다.
> `.hwpx`(zip)는 아직 시험 안 했습니다. 클린아이 의료 95건 중 1건뿐입니다.

---

## 첨부를 내려받는 길 (클린아이)

API 의 `URL` 은 첨부 직링크가 아니라 **공고 화면 주소**입니다. 두 걸음입니다.

```
① 공고 화면 HTML 에서
     fn_FileDown('<UPLOAD_FILENAME>','<ORIGINAL_FILENAME>','<FILE_PATH>')
② POST https://job.cleaneye.go.kr/file/FileDownload.do
     UPLOAD_FILENAME · ORIGINAL_FILENAME · FILE_PATH
     Referer: 공고 화면 주소
   → 파일 원본
```

`FILE_PATH` 는 파이프(`|`)로 나뉜 값이고, 세 인자를 다 넘겨야 파일이 옵니다.
하나라도 빠지면 0바이트가 옵니다.
**세션 쿠키는 필요 없었습니다** (알리오와 다릅니다).

---

## 그래서 — 이게 클린아이 판단을 바꿉니다

`클린아이_이전_지도.md` 에서 「제목만으로는 74% 를 못 가린다」 며 미루기를 권했습니다.
**그 74% 의 답이 첨부에 있습니다.**

오늘 다시 센 것 (17개 시도 전부):

```
클린아이 전체 360건 · 그중 의료 95건
  95건 전부 첨부가 있습니다
    .hwp   48건
    .hwpx   1건
    .pdf   44건
    없음    2건
```

**`.pdf` 44건은 이미 읽습니다** (알리오에 붙인 Drive OCR · `tools/ocr/`).
**`.hwp` 49건은 위 방법이면 읽힙니다.**
→ 첨부를 읽으면 **95건 중 93건**의 본문이 손에 들어옵니다.

그리고 `.hwp` 를 쓰는 곳이 어디인지 보면 —

```
부산의료원 · 대구의료원 · 경기도의료원 · 영월의료원 · 홍성의료원 · 공주의료원
군산의료원 · 진안군의료원 · 제주의료원 · 서귀포의료원 …
```

**전부 지방의료원 — 1층입니다.** 「많이 안 뽑는 곳이라도 하나 빠지면 안 된다」 는
바로 이런 곳입니다.

---

## 정하실 것

1. **클린아이를 미루는 판단을 되돌릴까요.** 첨부를 읽으면 의료 95건 중 93건이
   풀립니다. 미루기를 권했던 근거(74% 못 가림)가 없어졌습니다.
2. **`.hwp` 읽기를 어디에 둘까요.** `tools/ocr/` 옆에 `tools/hwp.mjs` 한 벌로 두면
   클린아이·치매센터·기관 홈페이지가 다 씁니다. 라이브러리를 안 붙이니 가볍습니다.
3. 순서는 [1] 에서 「워크넷 → 나라일터 → 클린아이…」 로 정하셨습니다. 그대로 갈지,
   클린아이를 올릴지.
