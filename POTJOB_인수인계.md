# POTJOB 인수인계

2026년 9월 20일 기준.

이 파일 하나만 읽고 따라올 수 있게 썼습니다. 프로젝트를 처음 보는 사람을 전제합니다.

POTJOB 은 **작업치료사·물리치료사를 위한 채용공고 모음 + 급여 통계 + 커뮤니티**입니다.
공공기관·종합병원·대학병원 채용공고를 하나도 빠뜨리지 않는 것이 목표입니다.

---

# ⚠ 먼저 읽을 것 셋

읽고 넘기지 말고, 회원을 받기 전에 처리해야 하는 것들입니다.

## ① 보안 — service_role 키가 노출됐습니다

Supabase 의 `service_role` 키가 **작업 중 채팅에 그대로 찍혔습니다.**

이 키는 **RLS(행 단위 보안)를 통째로 우회합니다.** 이 키를 가진 사람은
회원의 급여·스펙·채팅을 전부 읽고 고치고 지울 수 있습니다. 회원 권한 설정과 무관합니다.

**회원을 받기 전에 반드시 교체하십시오.**
Supabase 대시보드 → Project Settings → API → `service_role` 재발급.
재발급하면 그 키를 쓰는 도구(`tools/load_orgs.js` · `tools/copy_jobs.js`)의
`.env.local` 도 같이 바꿔야 합니다.

같이 처리할 것 둘.

- **`ADMIN_KEY` 변경** — 옛 쪽 관리자 화면 열쇠입니다. `gas/wage.js` 의
  `rotateAdminKey()` 함수에 새 값을 넣고 실행한 뒤 다시 비웁니다.
  코드에 열쇠를 남기지 않기 위한 절차입니다
- **인증키 한 곳으로 모으기** — 지금 공공데이터 인증키 13개가
  `gas/wage.js` 코드 안에 값 그대로 박혀 있습니다 (아래 9번 참고)

## ② 워크넷 약속 — 안 지키면 승인이 취소됩니다

워크넷(고용24) 채용정보 API 승인 신청서에 약속한 것이 있습니다.
**지키면 좋은 것이 아니라 어기면 승인이 취소될 수 있는 것**입니다.

- 받은 공고를 **임의로 고치지 않는다**
- 재가공해서 **팔지 않는다**
- **출처를 밝힌다**
- 원문 보기와 지원은 **고용24 원본 링크로 연결한다**

지금 공고 상세 화면의 「원문 공고 열기」가 그 링크입니다.
이 단추를 없애거나 우리 쪽에서 지원을 받게 바꾸면 약속을 어기는 것입니다.

워크넷에서 온 공고는 **628건 중 377건**으로 제일 많습니다. 승인이 끊기면 절반이 빕니다.

## ③ 안성병원 건 — 어떻게 내려갔는지 모릅니다

「경기도의료원 안성병원 360도 통합돌봄도시 간호사(재활치료실/수탁사업)채용 재공고」
(공고ID `CE79580`) 가 회원 화면에서 내려가 있습니다.

그런데 **손으로 내리는 목록 아홉 건에 이 공고가 없습니다.**
`gas/wage.js` 의 `hideCommonJunk()` 에 박힌 목록에도, 다른 어디에도 없습니다.

**누가 어떻게 내렸는지 모릅니다.** 알아내지 못했고, 추측해서 적지 않았습니다.
같은 일이 다른 공고에도 일어났을 수 있습니다.

지금은 `job_hides` 표에 「이미 내려져 있던 것」이라는 묶음으로 따로 적어 뒀습니다.

---

# 1. 지금 어디까지 왔나

**옛 쪽과 새 쪽이 둘 다 돌고 있습니다.** 이게 지금 상태를 이해하는 열쇠입니다.

```
옛 쪽 (회원이 쓰는 곳)          새 쪽 (만드는 중)
potjob.co.kr                   potjob-web.vercel.app
GitHub Pages + Apps Script     Next.js + Supabase
구글 시트가 자료 창고            PostgreSQL 이 자료 창고
```

**옛 쪽을 끄지 않았습니다.** 지금도 30분마다 공고를 모으고 회원이 씁니다.
새 쪽은 화면을 다시 만드는 중이고, 자료는 **한 번 복사해 온 상태**입니다 —
새 공고가 실시간으로 새 쪽에 들어오지는 않습니다.

## 옛 쪽

| | |
|---|---|
| 주소 | potjob.co.kr (GitHub Pages) |
| 서버 | Google Apps Script — `gas/wage.js` 19,879줄 |
| 다리 | `app.js` (JSONP) |
| 자료 | 구글 시트 33장 |

공고를 모으는 일은 **전부 옛 쪽이** 합니다. 새 쪽에는 수집기가 없습니다.

## 새 쪽

| | |
|---|---|
| 주소 | https://potjob-web.vercel.app |
| 화면 | Next.js 16 (App Router) · `web/` 폴더 |
| 자료 | Supabase (프로젝트 `qbnxycavokzcdonlfdat`) |
| 배포 | GitHub `main` 에 올리면 Vercel 이 자동으로 |

## 새 쪽에 지금 들어 있는 것

```
표 38개 · 전부 RLS 켜짐
기관 자료   62,749줄   (병원·요양·복지시설 등 9종)
공고         628건     (그중 회원에게 보이는 것 443건)
회원           4명
급여 등록      1줄      ← 옛 자료라 새 통계에서 뺍니다
스펙 등록      1줄
어학 등록      0줄
커뮤니티 글    0건
```

**숫자가 이렇게 적은 것은 아직 안 연 상태이기 때문입니다.**
급여 통계는 3명이 안 되면 안 보여주게 되어 있어서, 지금 화면에는
「아직 집계 전」이 뜹니다. 그게 맞는 동작입니다.

---

# 2. 사람과 회사·신고번호

**확인하지 못했습니다.**

지금 저장소의 어느 문서에도 대표자·사업자등록번호·직업정보제공사업 신고번호가
적혀 있지 않습니다. 코드에도 없습니다.

**추측해서 적지 않았습니다.** 다음 사람이 채워 주십시오.

```
대표자
사업자등록번호
직업정보제공사업 신고번호
연락처
```

참고로, 옛 작업 기록에 **「우리만 직업정보제공사업 신고」라고 단정했다가
확인할 방법이 없어 물러선 일**이 있습니다. 남(경쟁사)에 대한 단정은
직접 확인한 것만 적는다는 원칙이 그때 생겼습니다.

---

# 3. 접속 정보와 폴더 구조

## 어디에 무엇이 있나

```
potlab/
  web/          새 쪽 Next.js 앱          ← 지금 주로 고치는 곳
  gas/wage.js   옛 쪽 Apps Script 서버    ← 수집기가 여기 있음
  tools/        자료를 옮기는 도구 (Node)
  data/         원본 CSV (심평원·장기요양·복지시설 등)
  potjob-brand/ 로고·아이콘·색 명세
  scripts/
```

## 문서

| 파일 | 무엇 |
|---|---|
| `POTJOB_인수인계.md` | **이 파일.** 먼저 읽으세요 |
| `POTJOB_작업지침.md` | 반복된 실수 정리. 새 대화를 열 때 같이 읽히면 좋습니다 |
| `수집기_지도.md` | `gas/wage.js` 의 함수·시트·트리거 지도 |
| `수집기_옮길때_고칠것.md` | 수집기를 옮길 때 같이 고칠 네 가지 |
| `supabase단계기록.md` | 표를 만든 순서와 되돌리는 법 |
| `supabase설계.md` | 표 설계 |
| `현황.md` | 옛 쪽 전체 정리 (876줄) |
| `teamsparta.md` | 색·글꼴·여백 규칙 |
| `CLAUDE.md` | 이 프로젝트의 목표와 판단 기준 |

## 새 쪽 열쇠

`web/.env.local` 에 있습니다. **저장소에 안 올라갑니다.**

```
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
```

`anon` 키는 브라우저에 나가도 되는 열쇠입니다 — 무엇을 읽을 수 있는지는
DB 쪽 RLS 가 정하지, 키가 정하지 않습니다.
`service_role` 키는 절대 `web/` 에 두지 않습니다 (위 ⚠① 참고).

---

# 4. Supabase — 표·RLS·함정

## 표 38개

| 무리 | 표 |
|---|---|
| 회원 | `profiles` · `admins` · `notification_settings` · `push_devices` |
| 공고 | `job_posts` · `job_attachments` · `job_post_edits` · `job_events` · `job_stars` · `job_hides` |
| 급여·스펙 | `salary_records` · `student_specs` · `spec_langs` |
| 커뮤니티 | `posts` · `comments` · `post_images` · `post_likes` · `reports` |
| 채팅 | `chat_rooms` · `chat_members` · `chat_messages` |
| 기관 자료 | `hospitals` · `ltc_facilities` · `welfare_facilities` · `welfare_centers` · `public_hospitals` · `mental_centers` · `dementia_centers` · `dementia_safe_centers` · `dev_rehab_orgs` |
| 수집 | `collectable_orgs` · `drop_rules` · `wn_verdicts` · `collector_state` · `seen_postings` |
| 화면 | `home_blocks` · `site_settings` · `page_hits` |

**대조용 한 장** `org_directory` 62,749줄 — 기관 자료 아홉 표를 한 장으로 합친 것.
검색 화면은 이름+지역으로 묶은 `org_group_mv`(55,338곳)를 봅니다.

## RLS — 무엇을 어떻게 막고 있나

**회원은 표에 직접 못 씁니다.** 급여·스펙·어학은 전부 `save_my_survey()` 라는
함수 하나로만 들어갑니다. 함수가 「1년에 2번」을 세고, 넣을 당시의 역할을 박고,
세 표에 한꺼번에 담습니다.

**왜 이렇게 했나** — 화면에서만 막으면 요청을 직접 만들어 뚫립니다.
실제로 회원이 `profiles` 의 `survey_edits`(고친 횟수)를 직접 0 으로 되돌릴 수 있는
구멍이 있었고, 칸 단위 권한으로 막았습니다.

| 표 | 회원이 볼 수 있는 것 |
|---|---|
| `job_posts` | 숨김·보류가 아닌 공고. 출처·수집일 칸은 **관리자만** |
| `salary_records` · `student_specs` · `spec_langs` | **자기 것만** |
| `posts` · `comments` | 전부 (숨긴 것 빼고) |
| `chat_*` | 자기가 들어간 방만 |
| `job_hides` · `drop_rules` · `collector_state` | **관리자만** 또는 아무도 |

## 함정 — 한 번씩 당한 것들

### GRANT 를 빠뜨리면 RLS 까지 안 먹습니다 (세 번 걸림)

RLS 는 **「이미 닿을 수 있는 것」을 좁히는** 장치입니다.
표 권한(GRANT)이 없으면 규칙을 아무리 잘 짜도 `permission denied for table …` 이 납니다.

- 처음에 `anon`·`authenticated` 에 GRANT 를 안 줘서 전부 막혔습니다
- 그다음 `service_role` 을 빠뜨려서 기관 자료 적재가 403 으로 막혔습니다
- **표 단위 GRANT 가 있으면 칸 단위 REVOKE 가 무시됩니다.** `profiles` 에
  표 단위 UPDATE 권한이 있어서, 칸 단위로 막아둔 `survey_edits` 가 뚫렸습니다.
  표 단위를 회수하고 칸 단위로 다시 준 뒤에야 막혔습니다

**표를 새로 만들 때마다 GRANT 를 같이 주십시오.**

### 규칙 안의 함수는 anon 에게도 열려 있어야 합니다

`is_admin()` · `in_room()` 은 RLS 규칙 안에서 불립니다.
규칙 안의 함수는 **질의를 던진 사람의 권한**으로 불리기 때문에, EXECUTE 를 뺏으면
규칙이 통째로 막힙니다. Supabase 점검에 WARN 으로 뜨지만 **고치면 안 됩니다.**

### 규칙이 하나도 없는 표 둘은 일부러입니다

`collector_state` · `seen_postings` 는 RLS 를 켜고 규칙을 안 만들어
`service_role` 만 닿습니다. 점검에 INFO 로 뜨는데 고칠 것이 아닙니다.

### 그 밖에 겪은 것

- **`select *` 를 쓰면 안 되는 자리가 있습니다.** `job_posts` 의 출처·수집일 칸은
  회원에게 권한이 없어, `select('*')` 를 쓰면 통째로 거절당합니다. 칸을 적어야 합니다
- **SQL 함수는 느려질 수 있습니다.** `p_x is null or col = p_x` 꼴을 쓰면
  Postgres 가 일반 계획을 세워 전체를 훑습니다 (2,249ms). 같은 질의를
  동적 SQL(`execute … using`)로 바꿨더니 127ms 가 됐습니다
- **무거운 집계는 미리 계산해 둡니다.** 62,749줄을 네 번 훑는 함수가 시간 초과로
  죽어서, `org_facet_mv` · `org_group_mv` 두 장으로 미리 만들어 뒀습니다.
  원본을 다시 부으면 `refresh_org_directory()` 를 돌려야 합니다
- **`numeric` 반올림.** `3.8/4.5*4.5` 가 `3.79999` 가 되어 점수가 한 칸 떨어졌습니다.
  `round(g, 4)` 로 막았습니다
- **사진 파일은 글을 지워도 안 지워집니다.** `on delete cascade` 는 DB 줄만 지웁니다.
  주인 없는 파일을 치우는 일이 **아직 없습니다** (`tools/clean_orphan_files.js` 는 손으로 돌립니다)

---

# 5. 인증 — 카카오·네이버·애플

로그인은 **Supabase Auth** 가 맡습니다.

| 제공자 | 상태 | 부르는 이름 |
|---|---|---|
| 카카오 | 됨 | `kakao` |
| 네이버 | 됨 | **`custom:naver`** |
| 애플 | 안 됨 — 열쇠를 아직 안 받았습니다 | `apple` |

## 카카오 — 프로필 사진 동의를 끄면 로그인이 막힙니다

카카오 개발자 콘솔의 동의항목에서 **프로필 사진을 끄면 `KOE205` 로 로그인이 막힙니다.**

우리는 카카오 프로필 사진을 안 씁니다 — 커뮤니티에 카카오 사진과 이메일이
뜨면 안 된다는 것이 운영 방침입니다. 아바타는 이모지·색이거나 회원이 직접 올린 사진만 씁니다.

**그런데 Supabase 에는 Scopes 를 적는 칸이 없어 코드로 뺄 수가 없습니다.**
그래서 **동의항목은 켜 둔 채로 값을 안 쓰는** 방식으로 갑니다.
받아오되 `profiles` 에 안 담습니다.

동의항목을 끄면 로그인이 통째로 막히니 **끄지 마십시오.**

## 네이버 — 이름을 잘못 부르면 400

Supabase 에서 네이버는 기본 제공자가 아니라 **custom 제공자**로 켜져 있습니다.

```
signInWithOAuth({ provider: 'custom:naver' })   ← 이렇게 불러야 302
signInWithOAuth({ provider: 'naver' })          ← 400 "Unsupported provider"
```

**더 중요한 것** — 네이버 식별자는 **pairwise** 입니다.
같은 사람이라도 **앱 등록이 달라지면 다른 식별자**가 옵니다.

즉 **네이버 앱을 새로 만들면 기존 회원이 전부 남남이 됩니다.**
로그인은 되는데 자기 글도 급여도 없는 새 사람이 됩니다.
네이버 앱 등록 정보는 옮기더라도 **같은 앱을 유지**해야 합니다.

## 로그인한 뒤 어디로 가나

```
로그인 콜백 → profiles 줄이 있나?
  있으면  → 원래 보려던 곳 (sessionStorage 의 potjob.after-login) 또는 홈
  없으면  → /welcome 으로 가입 계속. 자리는 지우지 않고 남겨둠
              가입을 마치면 그때 원래 자리로
```

**자리를 남겨두는 이유** — 공유 링크를 보고 들어온 사람이 가입을 마친 뒤
홈으로 떨어지면, 뭘 보러 왔는지 잊고 그냥 나갑니다.
실제로 그렇게 되어 있었고 고쳤습니다.

## 가입 순서

```
① 약관 동의 → ② 직군·역할 → ③ 닉네임 → ④ 사진 → ⑤ 급여·스펙
```

**직군·역할이 닉네임보다 앞인 이유** — 두 번 누르면 끝나고 타자가 없어 마찰이 없습니다.
그리고 뒤의 모든 화면이 여기서 갈립니다 (설문이 9장이냐 6장이냐, 어느 커뮤니티 방이 보이냐).
닉네임은 겹치면 다시 지어야 해서 마찰이 제일 큰 칸이라 뒤로 뺐습니다.

**주의** — `profiles` 줄은 닉네임 칸에서 생깁니다 (`nickname` 이 NOT NULL).
그래서 ②에서 고른 직군·역할을 화면 상태로 들고 갔다가 ③의 INSERT 에 같이 담습니다.
줄이 없는데 UPDATE 를 부르면 **0줄이 바뀌고 오류도 안 나면서 조용히 사라집니다.**

---

# 6. 만든 화면 목록

| 주소 | 무엇 | 로그인 |
|---|---|---|
| `/` | 홈. 배너·카테고리·큰배너(관리자가 고침) + 랜딩 | 없어도 |
| `/jobs` | 공고 목록·검색 | 없어도 (맛보기) |
| `/jobs/[id]` | 공고 상세 · 공유 · 마감 안내 | 없어도 |
| `/pay` | 월급 확인 — 조건별 통계 | 「내 위치」는 회원만 |
| `/spec` | 스펙쌓기 — 점수와 또래 비교 | 회원만 |
| `/orgs` | 병원정보 찾기 (55,338곳) | 없어도 |
| `/community` · `/community/[ch]` | 커뮤니티 (어두운 바탕) | 읽기는 없어도 |
| `/post/[id]` | 글 상세 · 댓글 · 공유 | 읽기는 없어도 |
| `/community/write` | 글쓰기 | 회원만 |
| `/me` | 내 정보 · 급여·스펙 고치기 | 회원만 |
| `/welcome` | 가입 5단계 | 로그인 뒤 |
| `/login` · `/auth/callback` | 로그인 | — |
| `/terms/[doc]` | 약관 | — |
| `/soon` | 준비 중 | — |
| `/admin` · `/admin/jobs` · `/admin/stats` · `/admin/reset` | 관리자 | 관리자만 |
| `/api/og/job/[id]` | 카톡 미리보기 그림 (화면 아님) | — |

## 화면 만들 때 지키는 것

- **밝은 바탕이 기본.** 커뮤니티만 어둡고, 홈은 기기가 어두운 모드여도 밝게 고정합니다.
  **왜** — 홈 본문은 종이색으로 고정인데 탑바·탭바만 어두워지면 위아래가 따로 놉니다
- **색·글꼴·여백은 `teamsparta.md`** 를 따릅니다. `web/app/globals.css` 의
  `@theme` 가 그 값을 담고 있습니다. **여기서 값을 바꾸지 말고 그쪽 문서를 고치십시오**
- **빨강(#FF3B30) 위 흰 글자는 대비가 3.55:1** 입니다. 큰 굵은 글자(16px 이상 bold)와
  로고에만 씁니다. 본문에 쓰면 안 읽힙니다
- **이모지를 안 씁니다.** 라인 아이콘을 `components/icon.tsx` 에서 이름으로 꺼내 씁니다.
  이름은 Lucide 이름 그대로이고, 없는 이름이 오면 점 하나로 떨어집니다
- **말투** — 「~해요」 「~에요」 「~입니다」 「~죠」 를 섞습니다.
  **「~예요」는 쓰지 않습니다.** 끝을 한 가지로 통일하지 않습니다 —
  「~합니다」만 이어지면 딱딱하고 티가 납니다. 오류·경고만 예외로 또렷하게

---

# 7. 중요한 원칙과 그 이유

**모든 줄에 「왜」가 붙어 있습니다. 이유를 모르면 다음 사람이 되돌립니다.**

## 자료를 다룰 때

**확실하지 않은 숫자는 만들지 않습니다.**
왜 — 경쟁률·평균 같은 숫자를 지어내면 회원이 그걸 믿고 결정합니다.
지원서나 홍보물에서 틀린 게 드러나면 전체 신뢰를 잃습니다.

**3명이 안 되면 통계를 안 보여줍니다.**
왜 — 두 명의 평균은 통계가 아니라 누구 한 명 이야기입니다. 본인이 누군지도 드러납니다.
화면에서 가리는 게 아니라 **DB 함수가 값 자체를 안 내보냅니다.**

**출처와 한계를 화면에 밝힙니다.**
왜 — 「직군 구분 없는 기관 전체 평균」을 「회원이 올린 병원별 초임」처럼 적었다가
사실과 다른 것을 발견한 일이 있습니다. 숫자마다 무엇을 센 것인지 적습니다.

**수집 규칙을 고치면 이미 쌓인 자료는 안 바뀝니다.**
왜 — 수집기를 고쳐도 시트·DB 에 이미 들어간 줄은 그대로입니다.
언어재활사 공고가 남아 있었는데 치우는 코드를 안 만들어 계속 떠 있었습니다.
**규칙을 고칠 때는 「이미 담긴 것을 어떻게 할지」를 같이 정하십시오.**

## 공고를 다룰 때

**못 가린 공고는 버리지 않고 보류함으로.**
왜 — 버리면 다시 볼 수 없습니다. 보류함에 두면 관리자가 공고문을 보고 결정합니다.
목표가 「하나도 안 빠지는 것」이라 버리는 쪽이 더 위험합니다.

**마감일이 비어 있으면 마감으로 치지 않습니다.**
왜 — 마감일이 없는 게 아니라 **못 읽어온 것**입니다.
지금 628건 중 81건이 그렇습니다. 마감으로 처리하면 멀쩡한 공고가 통째로 막힙니다.

**마감된 공고는 목록에서 거르되 상세에서는 보여줍니다.**
왜 — 이 둘은 다른 상황입니다.
목록에서 마감 공고를 보여주면 헛걸음이 됩니다. 그래서 거릅니다.
그런데 **공유 링크는 보낸 날 바로 안 눌립니다. 며칠 뒤에 열립니다.**
그 사이 마감되면 받은 사람이 빈 화면을 보고 링크가 죽습니다.
상세는 이미 주소를 아는 사람이 오는 곳이라, 내용을 보여주고 위에 띠로 알립니다.

띠에는 「알림 받기」를 답니다 — 다음을 위한 것입니다.
아래에는 「비슷한 공고 보기」를 따로 둡니다 — 지금 당장을 위한 것입니다.
**하나만 두면 한쪽이 막힙니다.**

**여러 직군 통합 공고는 인원이 우리 직군 인원이 아닙니다.**
왜 — 「51명 채용」인데 작업치료사는 1명인 공고가 있습니다.
알리오의 `steps` 에 「일반(물리치료사)-(2)」처럼 직군별 인원이 적혀 있어 그걸 씁니다.

## 개인정보

**이름·연락처·이메일·다니는 병원 이름을 받지 않습니다.**
왜 — 급여를 올리는 일이라 신원이 드러나면 아무도 안 올립니다.
받는 것은 닉네임(본명 아니어도 됨)·권역·출생연도·연차·병원유형·급여뿐입니다.

**커뮤니티에 카카오 프로필 사진과 이메일이 뜨면 안 됩니다.**
왜 — 회원이 동의한 적 없는 노출입니다. 아바타는 이모지·색이거나 직접 올린 사진만.

**방문자를 세되 개인을 추적하지 않습니다.**
왜 — 날짜별 숫자만 있으면 운영에 충분합니다.

## 급여

**연 총소득 기준으로 셉니다.**
왜 — 월급만 비교하면 상여가 큰 곳과 작은 곳이 뒤집힙니다.
고정 월급·추가 수당·연간 상여·당직·주말을 다 넣어 연 단위로 환산합니다.

**세전으로 적었는지 계산한 것인지 DB 에 남깁니다** (`pay_basis`).
왜 — 나중에 자료를 믿을지 말지의 근거가 됩니다.

**2026년 4대보험 요율은 직접 확인한 값만 씁니다.** 소득세는 간이세액표라
**근사치이고, 화면에 「대략이에요」라고 밝힙니다.**
왜 — 짐작한 요율로 실수령을 보여주면 회원이 그걸 믿고 협상합니다.

---

# 8. 숫자 — 쓰는 것과 버린 것

## 쓰는 것

| 숫자 | 무엇 | 출처 |
|---|---|---|
| **15,332** | 등록된 병원 | 심평원 2026Q2 |
| **11,071** | 작업치료사 | 심평원 2026Q2 (`sum(ot)`) |
| **54,880** | 물리치료사 | 심평원 2026Q2 (`sum(pt)`) |
| **65,951** | 치료사 합계 | 위 둘을 **읽어서 더함** |
| **55,338** | 기관 | 이름+지역으로 묶은 수 |

**치료사 합계를 미리 더해 두지 않습니다.**
왜 — 한쪽 자료만 갱신되면 합계가 조용히 어긋납니다. 화면에서 두 값을 읽어 더합니다.

**화면의 숫자는 하나도 코드에 박혀 있지 않습니다.** `home_stats()` 라는 DB 함수가
그때그때 셉니다. 심평원 자료판(`2026Q2`)도 DB 에서 읽어서, 분기가 바뀌면
화면 표기도 따라 바뀝니다.

## 버린 것 — 다시 쓰지 마십시오

| 숫자 | 어디 쓰였나 | 왜 버렸나 |
|---|---|---|
| **664,465** | 「이만큼의 자료를 모았습니다」 | **출처를 못 찾았습니다.** 어떤 조합으로도 안 나옵니다 |
| **12,407** | 병원 수 | 실제는 15,332 |
| **9,876** | 작업치료사 | 실제는 11,071 |
| **52,598** | 물리치료사 | 실제는 54,880 |
| **15,333** | 등록된 병원 | 실제는 15,332 (1 차이) |
| **「세후 220만 원」** | 초봉 예시 | **현장 경험에 기댄 추정치.** 우리가 센 값이 아닙니다 |

지원서에도 이 숫자들의 근거가 없습니다.
**코드에 남아 있지 않은 것을 `grep` 으로 확인했습니다.**

「세후 220만 원」 대신 **우리가 모은 중위값만** 씁니다.
지금은 표본이 1줄이라 화면에 「아직 집계 전」이 뜹니다.

## 기관 수가 왜 여럿인가

같은 자료를 **다른 단위로 센 것**입니다. 셋 다 맞는 숫자인데 뜻이 다릅니다.

| 숫자 | 무엇 |
|---|---|
| **62,749** | 자료 표의 **줄 수**. 한 기관이 여러 자료에 들어 있으면 여러 줄 |
| **55,338** | **이름+지역으로 묶은 수.** 병원정보 찾기 화면이 보여주는 수 |
| **47,787** | **이름만** 중복 제거. 전국에 같은 이름이면 한 곳으로 셈 |
| 5.2만 | 지원서에 적힌 값 — **어느 것과도 안 맞습니다. 근거 없음** |

**왜 겹치나** — 요양병원은 심평원 자료에도 있고 장기요양 자료에도 있습니다.
분류별 줄 수는 장기요양 30,595 · 병원 15,332 · 복지시설 14,710 · 나머지 2,112 입니다.

---

# 9. 수집기 현황과 이전 계획

자세한 것은 `수집기_지도.md` 에 있습니다. 여기는 요약입니다.

## 규모

```
gas/wage.js        19,879줄 · 함수 656개
수집에 이어진 것      391개 · 12,791줄
인증키             코드 안 13곳 + 스크립트 속성 3곳
시트               33장 (그중 새 쪽에 대응 없는 것 13장)
트리거              8개
6분 쪼개기           8군데
```

## 어디서 공고를 받아오나

| 어디 | 무엇 | 함수 | 지금 몇 건 |
|---|---|---|---:|
| 워크넷(고용24) | 민간 병원·요양원·주간보호 | `collectWorknet` | 377 |
| 병원 홈페이지 게시판 약 200곳 | 대학병원·종합병원 | `collectHosp` | 131 |
| 알리오(공공기관 경영정보) | 중앙 공공기관 | `collectJobs` | 58 |
| 나라일터 | 지자체·보건소 | `collectJobs3` | 47 |
| 중앙치매센터 | 광역·치매안심센터 (화면 긁기) | `collectNid` | 8 |
| 클린아이 잡플러스 | 지방공공기관·지방의료원 | `collectJobs2` | 7 |

**워크넷이 제일 많습니다.** 2026-09-08 문서에는 「못 붙인 곳」으로 적혀 있었는데
그 뒤에 뚫렸습니다. 지금은 회원이 보는 공고의 절반 넘게가 여기서 옵니다
(그래서 위 ⚠② 의 약속이 중요합니다).

공고 말고 다른 것도 받습니다 — 심평원 병원 15,332곳 · 사회서비스 바우처 ·
온통청년 청년정책 · VMS 봉사활동 · 국가자격 시험일정.
관리자 → **기관 현황** 탭에서 무엇이 붙었고 무엇이 안 붙었는지 볼 수 있습니다.

## 도는 순서

수집은 `collectTick` 이 **30분마다**, 병원 게시판은 `hospTick` 이 **1시간마다 따로** 돕니다.

```
평일 · 7~19시에만
① 알리오 100초 → ② 조회수 → ③ (9·14·18시만) 잡플러스 50초 + 치매센터 40초
→ ④ 워크넷 60~120초 → ⑤ 나라일터 남은 것 전부 → ⑥ 탭 분류 → ⑦ 버림 규칙
상한 300초
```

**300초인 이유** — 한 번 실행이 6분(360초)까지입니다.
예산이 끝나도 열던 상세는 끝까지 가서 20~30초를 더 쓰기 때문에 60초를 남겨 뒀습니다.

## 6분 때문에 쪼갠 것 / 원래 쪼개야 하는 것

**시간 때문에 쪼갠 것** — Supabase 로 가면 없어도 됩니다.
예산 나누기 전체 · 워크넷 이어가기(`wn_more`) · 병원 게시판 200곳 나눠 돌기(`hs_next`) ·
나라일터 중단(`job3_at`) · 치매센터(`NID_DEADLINE`) · 무거운 것을 하루 3번으로 미룬 것.

**상대 쪽 사정이라 남겨야 하는 것**
- API 쪽 나누기 — 알리오 120쪽 · 나라일터 100건 · 워크넷 100건.
  **상대가 한 번에 주는 양이 정해져 있습니다**
- 나라일터 `job3_done` — 한 번 연 공고를 또 열지 않기 위한 기록.
  **시간이 아니라 상대 서버에 대한 예의입니다**
- 병원 게시판 `hs_dt_tried` — 같은 글의 상세를 반복해 두드리지 않기 위한 것
- 알리오 쿠키 — 첨부를 받으려면 세션을 먼저 받아야 합니다

## 공고를 어떻게 가려내나

**제목에 직군이 없는 경우가 대부분입니다.** 그래서 단계로 봅니다.

```
1. 제목에 직군이 있나
2. 없으면 상세(API)를 열어봄
     알리오    → steps 에 직군이 나뉘어 있음 「의료기술직(물리치료사)」
     나라일터  → 상세 본문
3. 그래도 없으면 첨부 공고문을 읽음
     알리오    → atchFileType 이 'A' 인 것 (A=공고문 B=지원서 C=직무기술서)
     나라일터  → /getItemFile
     치매센터  → onclick 안의 download.aspx
     PDF 는 Drive convert:true 로 글자를 뽑음
4. 못 가리면 보류함 → 관리자가 공고문 보고 결정
```

**API 가 막혔다고 느끼면 먼저 「신청한 기능을 전부 쓰고 있나」를 물으십시오.**
알리오에서 `/list` 만 쓰다가 「자동은 어렵습니다」라고 포기할 뻔했는데,
신청서에 `/detail` 이 있었고 거기에 직군이 다 적혀 있었습니다.
나라일터도 신청한 6개 중 3개만 쓰고 있었습니다.

## 이전 계획 5단계

**원칙 — 옛 쪽을 끄지 않습니다. 같은 결과가 나올 때까지 둘 다 돌립니다.**
왜 — 수집기가 멈추면 회원에게 공고가 안 갑니다. 한쪽이 틀렸을 때
어느 쪽이 맞는지 견줄 기준이 없으면 되돌릴 수도 없습니다.

```
0단계  기준선   지금 옛 쪽이 무엇을 담는지 기록해 둡니다.
                이게 없으면 새 쪽이 맞는지 틀린지 알 수 없습니다

1단계  API 하나씩   알리오 → 나라일터 → 워크넷 순서로 새 쪽에 붙입니다.
                   첨부를 안 읽는 것부터

2단계  대조        같은 날 같은 공고가 양쪽에 들어왔는지 셉니다.
                   숫자가 맞을 때까지 새 쪽은 담기만 하고 회원에게 안 보입니다

3단계  판별·보류함   직군 판정과 보류함을 새 쪽으로. 규칙도 여기서 고칩니다
                   (`수집기_옮길때_고칠것.md` 의 네 가지)

4단계  갈아타기     회원 화면이 새 쪽을 보게 합니다.
                   옛 쪽은 한동안 그대로 두고 대조만 계속합니다
```

---

# 10. 분류 상태 — 지금 무엇이 잘못되어 있나

## 「공통」 103건 중 21건이 치료사 자리가 아닙니다

「공통」은 작업치료사와 물리치료사를 둘 다 받는 공고라는 뜻입니다.
103건 중 **82건은 제대로** 담겼습니다 — 요양원·주간보호센터의
「물리치료사 또는 작업치료사」 공고입니다.

**21건은 아닙니다.** 국민건강보험공단 신규직원 채용, 국민연금공단 공무직,
자동차손해배상진흥원 전문직, 광주보훈병원 언어재활사 같은 것들입니다.

**그중 11건을 2026-09-20 에 내렸습니다.** 나머지 10건은 이미 내려져 있었습니다.

## 원인

직군을 판정하는 낱말 목록이 이렇습니다.

```js
const JOB_WORDS = ['작업치료', '물리치료', '재활치료'];
```

그리고 판정할 때 **제목·직종명·자격증·우대조건·직무내용을 다 이어붙여** 한 번에 훑습니다.

```js
job = matchJob_([jobsNm, certificate, etcPfCond, pfCond, jobCont, title].join(' ')) || '공통';
```

그래서 병원 이름이나 부서 이름에 **「재활치료실」 한 마디만 있어도 공통**이 됩니다.

## 「재활치료」를 목록에서 빼지 않습니다

**왜** — 빼면 진짜 재활 공고를 놓친다는 판단입니다 (2026-09-20 결정).

다만 **제가 확인하지는 못했습니다.** 지금 자료에서 제목에 「재활치료」만 있고
작업치료·물리치료가 없는 공고는 **3건뿐이고 셋 다 우리 자리가 아니었습니다**
(안성병원 간호사 · 청주의료원 재활치료업무보조 2건).

제목만 본 것이라 적게 나온 것일 수 있습니다 — 판정은 제목이 아니라
직종명·자격증·우대조건·직무내용까지 이어붙인 글에서 하기 때문입니다.
**「재활치료」로만 걸려 들어온 진짜 공고가 몇 건인지는 아직 세어보지 않았습니다.**
규칙을 고치기 전에 그걸 먼저 세면 판단이 단단해집니다.

**대신 글을 나눠서 봅니다.**

| 어디서 걸렸나 | 얼마나 믿나 |
|---|---|
| 제목 · 직종명 · 자격증 | **강하게** — 그대로 분류 |
| 직무내용 · 우대조건 | **약하게** — 이것만으로는 분류하지 않음 |

이건 **아직 안 고쳤습니다.** 수집기를 옮길 때 같이 합니다.

## `job_hides` — 손으로 내린 목록 21줄

코드 안(`hideCommonJunk`)과 DB 에 흩어져 있던 것을 한 표로 모았습니다.
줄마다 **왜 내렸는지**가 적혀 있습니다 (「요양보호사 자리」 「언어재활사 자리」 …).

**왜 목록을 남기나** — 나중에 규칙을 다시 짤 때 **시험 자료**가 됩니다.
「이 21건이 안 걸리는가」로 새 규칙을 검사할 수 있습니다.

이 표는 아무것도 막지 않습니다. 실제로 가리는 건 `job_posts.hidden` 입니다.

## 정규식으로 거르지 마십시오

「제목에 작업치료·물리치료가 없는 것」을 정규식으로 골랐더니 **26건**이 걸렸는데,
그중 **15건이 멀쩡한 요양원 치료사 공고**였습니다.

```
「물리(작업)치료사 채용공고」
「물리 및 작업 치료사를 모집합니다」
「물리치료사선생님or 작업치료사」
```

낱말이 쪼개져 있어 기계가 못 알아봅니다. **제목을 사람이 읽고 골라야 합니다.**

## 직군이 비었는데 화면에 뜨는 21건

요양보호사·사회복지사·임상병리사·행정직 공고이거나 합격자 발표 글입니다.

**왜 보류함에 안 갔나** — 보류함 규칙이 「직군이 비면 보류」가 아니라
**수집처마다 다른 특수 조건**이라 그물에 구멍이 났습니다.
보류를 켜는 자리가 일곱 군데인데 그중에 「직군이 비었으니 보류」가 없습니다.

나라일터 쪽은 더 이상합니다 — 거르는 조건에 「장애」「재활」이 들어 있어서
**그 말이 제목에 있으면 오히려 통과시킵니다.**

**「직군 비면 무조건 보류」로 바꾸면** 보류함이 52건 → 73건이 됩니다.
보통 날은 하루 15~17건이 들어오고 그중 직군 빈 것이 2~7건이라
관리자가 볼 것이 그만큼 늘어납니다.
다만 새 수집처를 붙이는 날에는 몰립니다 — 2026-09-16 에 병원 게시판을 받으면서
109건이 들어왔고 그중 직군 빈 것이 72건이었습니다.

---

# 11. 한 번 당한 함정 모음

**같은 곳에 두 번 빠지지 않게 모았습니다.**

## 데이터베이스

- **GRANT 를 빠뜨리면 RLS 까지 안 먹습니다.** 세 번 걸렸습니다.
  표를 새로 만들 때마다 GRANT 를 같이 주십시오
- **표 단위 GRANT 가 있으면 칸 단위 REVOKE 가 무시됩니다.**
  표 단위를 회수하고 칸 단위로 다시 줘야 막힙니다
- **RLS 규칙 안의 함수는 anon 에게도 EXECUTE 가 있어야 합니다.**
  뺏으면 규칙이 통째로 막힙니다
- **`select *` 는 칸 권한이 없는 표에서 통째로 거절당합니다.** 칸을 적으십시오

## 로그인

- **카카오 동의항목에서 프로필 사진을 끄면 `KOE205` 로 로그인이 막힙니다.**
  Supabase 에 Scopes 칸이 없어 코드로 못 뺍니다. **켜둔 채 값을 안 씁니다**
- **네이버는 `provider: 'naver'` 가 아니라 `'custom:naver'`** 입니다.
  `'naver'` 로 부르면 400 "Unsupported provider"
- **네이버 식별자는 pairwise** 라, 앱 등록을 새로 만들면
  **기존 회원이 전부 남남이 됩니다**

## 화면

- **4px 배수 자동 생성을 껐습니다** (`--spacing: initial`).
  눈금 밖 여백은 **CSS 가 아예 안 나옵니다** — 화면에서 바로 들통납니다.
  왜 껐나 — `teamsparta.md` 가 15px 와 32px 사이에 20·24px 를 만들지 말라고 못박았는데,
  자동 생성이 켜져 있으면 `p-5` 가 20px 로 살아납니다
- **같은 속성의 Tailwind 클래스 둘을 같이 쓰면** 누가 이기는지가
  클래스 순서가 아니라 **스타일시트 순서**로 정해집니다 (`w-full` + `w-[88px]`).
  공용 상수에 너비를 넣지 말고 쓰는 자리에서 주십시오
- **한글은 `word-break: keep-all` 이 없으면 낱말 중간에서 끊깁니다.**
  「적어 주세 / 요」처럼 됩니다. `overflow-wrap: break-word` 를 같이 둡니다
  (띄어쓰기 없는 긴 주소 때문)
- **조사가 틀리면 대충 만든 티가 납니다.** 「댓글를」 「첫 입사연도은」.
  받침을 보고 고르는 `eun()` · `eul()` 을 씁니다

## 미리보기 그림 (OG)

- **Pretendard woff2 는 그림 생성기(satori)가 못 읽습니다.** woff 를 씁니다
- **서버 런타임(nodejs)에서는 글꼴 파일을 `fetch` 로 못 엽니다** — `file://` 을 거부합니다.
  edge 런타임으로 바꿔야 `new URL(…, import.meta.url)` 이 열립니다
- **edge 는 ICU 가 잘려 있을 수 있어 `Intl` 의 timeZone 을 못 믿습니다.**
  한국 시간은 +9시간 오프셋으로 직접 계산합니다 (서머타임이 없어 언제나 정확)

## Apps Script

- **밑줄(`_`)로 끝나는 함수는 실행 목록에 안 뜹니다.**
  손으로 실행할 함수는 밑줄 없이 지으십시오
- **한 번 실행은 6분까지.** 오래 걸리는 일은 어디까지 했는지 기억했다가 이어가야 합니다
- **캐시가 옛 자료를 물고 있습니다.** 목록 21,600초 · 급여 6시간.
  자료를 바꾼 뒤에는 `refreshAll` 을 돌리십시오
- **주소 길이 한도** — 한글은 주소에서 9배로 부풉니다.
  긴 요청은 600자씩 쪼개 JSONP 로 보냅니다. form+iframe 은 응답을 못 읽어 안 됩니다

## 자료

- **XML 응답은 `&amp;` 를 풀지 않으면 HTML 이 옵니다.** `&#38;` 도 같이 푸십시오
- **PDF 는 Drive `convert: true` 만.** `mimeType` 을 같이 주면 충돌합니다
- **받은 파일이 진짜 그 형식인지 확인하십시오.** PDF 는 앞 5글자가 `%PDF-` 입니다
- **`.hwp` 본문은 못 읽습니다.** OLE 복합문서라 본문이 압축돼 있고
  Apps Script 로는 못 풉니다. Drive 도 못 엽니다.
  다만 **`PrvText`(미리보기 글)는 압축 없이 UTF-16LE 로** 들어 있어,
  파일을 바이트로 훑어 되살릴 수 있습니다. 문서 앞 1,000자쯤이라
  제목·모집분야가 대개 여기 있습니다.
  **못 찾았다고 「없다」고 단정하면 안 됩니다** — 압축된 본문은 안 걸리니까요.
  찾으면 담고, 못 찾으면 보류함으로
- **첨부가 `href` 가 아니라 `onclick` 안에 있는 곳이 있습니다** (작은따옴표)
- **부동소수점.** `3,000,000 × 0.009 = 26999.999` 가 됩니다.
  4대보험은 10원 단위로 버리는 게 실제 관행이라 `floor(round(x)/10)*10` 을 씁니다

---

# 12. 남은 일

## 회원을 받기 전에 (반드시)

- [ ] **목록·검색을 로그인 뒤로 옮기기.**
      지금은 **로그인 없이 요청 한 번에 공고 443건이 통째로 나옵니다.**
      2026-09-20 에 실제로 쏴서 확인했습니다 — `Content-Range: 0-442/443` · 121KB.
      우리 화면이 100건씩 받는 건 **우리 화면의 사정일 뿐**입니다.
      직접 쏘는 쪽은 `limit` 을 자기가 정합니다.
      **막으려면 로그인 상태를 서버가 알아야 하는데**, 지금은 브라우저
      안(localStorage)에만 있어서 서버가 못 봅니다
      (`lib/supabase-browser.ts` 주석에도 그렇게 적혀 있습니다).
      **쿠키로 옮겨야 합니다** — `@supabase/ssr` 을 넣고 `middleware.ts` 를 만들고
      서버 화면 16개를 손봐야 합니다. **반나절.**
- [ ] **속도 제한**은 위 항목이 있어야 뜻이 생기는 **보조 장치**입니다.
      목록이 열려 있는 한, 상대는 한 번만 부르면 되니 「많이 부르면 막기」가
      걸리지 않습니다. 만들 때 **IP 는 되돌릴 수 없게 섞어 10분만 보관**하는
      방식으로 — 방문자를 추적하지 않는다는 선을 지키기 위해서입니다.
      제안 숫자: 공고 상세 10분에 30건 · 목록 10분에 60번 (회원 자료가 쌓이면 다시 잡기)
- [ ] **`service_role` 키 교체** (위 ⚠①)
- [ ] `ADMIN_KEY` 변경
- [ ] 인증키 16개를 코드 밖으로
- [ ] 애플 로그인 열쇠 받기 — 지금 단추는 있고 「준비 중」이 뜹니다
- [ ] 사업자·신고번호를 이 문서에 채우기 (위 2번)

## 공고 횟수는 2026년 11월 말 전에는 쓰지 않습니다

「이 병원은 최근 1년에 N번 공고를 냈어요」 같은 줄을 **어떤 형태로도 화면에 쓰지
않습니다.** 지금 쓰면 거짓말이 됩니다.

### 왜

공고일(posted_at)은 2026-01-13 까지 있어서 **8개월치가 있는 것처럼 보입니다.**
그런데 출처마다 사정이 다릅니다.

| 출처 | 공고 | 공고일이 있는 것 | 수집 시작(2026-08-26) 이전 |
|---|---:|---:|---:|
| 워크넷 | 377 | 377 (100%) | 140건 |
| 병원 게시판 | 133 | 104 (78%) | 21건 |
| **알리오** | 58 | **4 (7%)** | **0건** |
| **나라일터** | 47 | **1 (2%)** | **0건** |

**알리오·나라일터는 접수 중인 공고만 줍니다.** 지난 공고를 아예 안 줍니다.

그래서 기관당 「최근 1년 공고 수」를 세면 이렇게 나옵니다.

```
병원 게시판으로 들어오는 기관   평균 1.49
워크넷으로 들어오는 기관        평균 1.08
알리오로 들어오는 기관          평균 0.20
나라일터로 들어오는 기관        평균 0.03
```

**근로복지공단은 우리 자료에 8건이나 들어 있는데 「1년에 0번」으로 나옵니다.**
기관의 성격이 아니라 **우리 수집 방식이 만든 차이**입니다.

### 언제부터 쓸 수 있나

공고에 적힌 날짜가 아니라 **우리가 본 날(collected_at)** 로 세야 출처별 편향이
없어집니다. 어느 출처든 「우리가 본 것」은 똑같으니까요.

```
수집 시작            2026-08-26
「최근 3개월에 N번」   2026-11-26 부터   ← 여기서 시작
「최근 6개월에 N번」   2027-02-26 부터
「최근 1년에 N번」     2027-08-26 부터
```

**2026-09-20 에 해둔 것 (다시 안 해도 됩니다)**

- Supabase **Max rows 를 100 으로** 낮췄습니다. 한 번에 443건 대신 100건입니다.
  자물쇠가 아니라 과속방지턱입니다 — 나눠 받으면 그만입니다.
  **이것 때문에 도구가 조용히 잘릴 수 있습니다.** 실제로 `sync_jobs.js` 가
  630줄을 받아야 하는데 100줄만 받아 관리자가 고친 값 하나를 날렸습니다.
  **오류가 안 납니다.** 받는 줄 수는 늘 서버 쪽에서 좁히십시오.
- 옛 쪽 `exportRows` 에 **열쇠를 붙였습니다** (스크립트 속성 `EXPORT_KEY`).
  그 전에는 배포 주소만 알면 누구나 채용공고 시트를 통째로 받아갔습니다.

## 화면

- [ ] 어학 통계를 화면에 붙이기 — `lang_stats()` 함수는 있는데 자료가 0줄이라 빈 화면
- [ ] 급여 결과 카드 — 「상위 몇 %」를 카드로 저장하고 링크로 보내는 기능.
      **화면이 아직 없습니다.** 무엇을 보여줄지 정해야 열쇠 구조도 정할 수 있습니다
- [ ] 병원정보 공유 버튼 — 자리만 잡아 뒀습니다
- [ ] 관리자 커뮤니티·신고·관리자 관리 화면
- [ ] 홈 랜딩의 그래프와 병원 카드가 **아직 예시 숫자**입니다.
      카드마다 「화면 예시입니다」라고 적어 뒀습니다
- [ ] 주인 없는 사진 파일 치우기 — 글을 지워도 저장소 파일이 남습니다

## 수집기

- [x] **옛 쪽 → 새 쪽 다리** (2026-09-20). `tools/sync_jobs.js` ·
      `.github/workflows/sync.yml` 이 30분마다 돕니다. 새벽 5시에 전체를 한 번.
      원본은 `job_posts`, 관리자가 고친 값은 `job_post_edits`,
      사람이 내린 것은 `job_hides` — 화면은 `job_posts_pub` 뷰만 봅니다
- [ ] 수집기를 Supabase 로 옮기기 (위 9번 5단계)
- [ ] 분류 규칙 고치기 (`수집기_옮길때_고칠것.md` 네 가지)
- [ ] `버림분석` 시트에 대응하는 새 표 만들기
- [ ] 판정 근거를 DB 에 남기기 — 지금은 안 남아서 병원 게시판 5건과
      워크넷 3건이 왜 그렇게 됐는지 못 밝혔습니다

## 확인이 필요한 것

- [ ] **카톡 미리보기가 실제로 뜨는지** — 태그가 공고마다 다르게 들어간 것까지
      확인했지만, 카톡에 보내보지는 못했습니다 (localhost 를 카톡이 못 읽습니다)
- [ ] **알림이 실제로 가는지** — 옛 쪽에 `pushPending` 을 부르는 곳이
      우리 파일에 없습니다. 별도 서버가 있는 구조로 보이는데 확인 안 됐습니다
- [ ] **관리자 화면을 관리자 계정으로** — 시험용 세션으로는 관리자 판정을
      통과하지 못해 직접 열어보지 못했습니다

## 옛 문서(2026-09-08)에서 넘어온 것 — 아직 확인 못 했습니다

제가 이번에 확인하지 못했습니다. **해결됐는지 모릅니다.**

- [ ] **날짜 오류** — 어느 화면인지 확인 필요. 제보만 있고 재현 못 했습니다
- [ ] **알리오 첨부 내려받기가 HTML 을 돌려줍니다** (`testAtch` 로 확인 가능).
      `&amp;` 를 안 풀어 생긴 문제였다는 기록이 있는데, 지금도 그런지는 모릅니다
- [ ] **청년정책 시트를 다시 받아야** 링크·마감일이 채워집니다
- [ ] **운영계정 활용사례 등록** — `POTJOB_활용사례_신청서.md` 참고
      (저장소에 그 파일이 지금 없습니다)

---

# 13. 자주 쓰는 명령

## 새 쪽 (`web/` 폴더에서)

```bash
npm run dev            내 컴퓨터에서 띄우기 (localhost:3000)
npm run build          배포 전에 깨지는 데 없는지
npx tsc --noEmit       타입 확인
node lib/pay.test.mjs  시험 하나 돌리기
for f in lib/*.test.mjs; do node "$f"; done    시험 전부 (13벌)
node tools/check.js    API 표와 코드 대조 · 이름 중복
```

배포는 `git push` 하면 Vercel 이 자동으로 합니다.

## 옛 쪽 (Apps Script 편집기에서 실행)

```
collectAllJobs   네 수집기를 한 번에 (6분 제한으로 끊길 수 있음)
collectJobs      알리오
collectJobs2     잡플러스
collectJobs3     나라일터 (이어서 돌기)
collectWorknet   워크넷
collectNid       치매센터
showTriggers     트리거와 이어가기 표시 보기
setupJobTrigger  트리거 만들기 (손으로 한 번)

reviewHold       보류함을 다시 자동 판별
refreshAll       캐시 비우기          ← 자료를 바꾼 뒤 꼭
rotateAdminKey   관리자 열쇠 바꾸기
cleanNidRows     치매센터 공고 지우고 다시 받기
resetAlio        알리오 처음부터 다시
resetJob3        나라일터 처음부터 다시
hideCommonJunk   「공통」으로 잘못 담긴 공고 숨기기 (손으로 적은 목록대로)

testHwp          한글 읽기가 되는지
testCommon       「공통」으로 잘못 담긴 공고
testNoti         알림이 갈 상태인지
testNidRows      치매센터 공고 상태
testAtch         알리오 첨부가 진짜 PDF 인지
testMail         메일 상태
whyMissed('…')   놓친 공고의 원인
sendHoldMailAgain  보류함 메일 다시 보내기
```

## 자료 옮기기 (`potlab/` 에서)

```bash
node tools/load_orgs.js   기관 자료를 Supabase 로 (표를 비우고 다시 붓고 대조)
node tools/copy_jobs.js   공고를 시트에서 Supabase 로
```

둘 다 `service_role` 키를 `.env.local` 에서 읽습니다.

---

# 14. 일하는 방식

`POTJOB_작업지침.md` 에 자세히 있습니다. 여기는 핵심만.

## 고치기 전에

**「이게 원인이라는 증거가 무엇인가」를 먼저 답하십시오.**
한 문제를 다섯 번 헛짚은 일이 있습니다. 같은 증상을 두 번 고쳐서 안 되면
세 번째는 코드를 만지지 말고 진단부터 하십시오.

**진단 함수는 응답 원문을 찍어야 합니다.** 500~1,200자와 항목 이름 목록(`Object.keys`)을
같이 찍으십시오. 「몇 건 왔다」만 찍는 진단은 쓸모없습니다.

실제로 미리보기 그림이 500 을 낼 때 본문이 비어 있어 원인을 알 수 없었습니다.
오류를 글자로 돌려주는 임시 진단을 넣고서야 `fetch failed` 를 봤습니다.

## 짐작하지 않기

**주소·명세를 지어내지 마십시오.** 네 번 지어냈습니다.
주소 하나를 확인했다고 나머지를 규칙으로 만들지 마십시오.
후보를 두 번 두드려 실패하면 멈추고 명세를 요청하십시오.

`[Unverified]` 는 면죄부가 아닙니다.

## 이미 있는 것 먼저 찾기

새 상수·함수 이름은 반드시 `grep` 으로 먼저 검색하십시오.
중복 선언 하나로 스크립트 전체가 안 돈 일이 있습니다.

기존 함수를 재활용할 때는 **그 함수가 무엇을 하는지 먼저 읽으십시오.**
`rgPlain_` 을 쓰려다 `<2026년 한가위 정담장터>` 가 통째로 사라질 뻔했습니다 —
꺾쇠를 태그로 오해하는 함수였습니다.

## 블록을 통째로 갈아끼우지 않기

큰 블록 교체 대신 작은 문자열 치환을 쓰십시오.
`collectTick` 주석부터 `setupJobTrigger` 까지를 잘라냈다가 그 사이의
함수 14개가 통째로 날아간 일이 있습니다.

## 시험은 실제와 같은 조건에서

`jsdom` 으로 통과했는데 실제 브라우저에서 안 된 일이 있습니다.
**「통과했다」가 아니라 「이 조건에서는 통과했다」**고 말하십시오.

## 막힌 것은 막혔다고

**안 된 것은 안 됐다고, 모르는 것은 모른다고 적으십시오.**
「완료」 「성공적으로」 대신 무엇이 어떻게 됐는지 적으십시오.

관리자 화면을 직접 열어보지 못했으면 「관리자에게 보입니다」라고 쓰지 말고
「DB 규칙을 안 건드렸고 공고 수가 그대로인 것만 확인했습니다」라고 쓰십시오.
그 차이가 다음 사람의 시간을 아낍니다.
