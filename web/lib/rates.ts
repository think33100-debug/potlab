/* 해마다 확인해야 하는 값. **여기 한 곳에만 둡니다.**

   계산기 셋(연봉 · 퇴직금 · 연차)과 가입 설문이 전부 이 파일을 봅니다.
   해가 바뀌면 여기만 고치면 화면이 전부 따라옵니다.

   짐작한 숫자는 하나도 없습니다. 아래에 어디서 확인했는지 적었습니다.
   출처를 못 찾은 값은 **아예 안 넣습니다** — 틀린 숫자를 보여주면
   그걸 믿고 계산한 사람이 손해를 봅니다 (실업급여를 안 만든 이유도 그것입니다).

   ── 확인한 곳 (2026-09-20 · 2026-09-23) ────────────────────────────
   국민연금 9.5%, 노사 절반씩 → 근로자 4.75%
     보건복지부 연금개혁 — 2026년부터 매년 0.5%p, 2033년 13%
   건강보험 7.19%, 노사 절반씩 → 근로자 3.595%
     보건복지부 보도자료 「2026년 건강보험료율 7.19%로 결정」
   장기요양 = 건강보험료의 13.14%
     보건복지부 보도자료 「2026년도 장기요양보험료율 0.9448%」
   고용보험(실업급여) 1.8%, 노사 절반씩 → 근로자 0.9%
     고용노동부 고용보험기금
   근로소득공제 · 소득세율 · 근로소득세액공제 — 국세청 세율표
     소득세율표는 국세청이 「2023~2025년 귀속」까지만 올려두었습니다.
     2023년부터 구간이 안 바뀌어 그대로 씁니다
   연차 — 근로기준법 제60조 (숫자가 법에 박혀 있어 해마다 안 바뀝니다)
   퇴직금 — 근로자퇴직급여보장법 제8조 (요율이 없는 계산식입니다)
   ──────────────────────────────────────────────────────────────── */

/** 화면에 「N년 요율 기준」으로 찍는 해 */
export const RATE_YEAR = 2026;

/* ── 4대보험 · 근로자 부담분 ───────────────────────── */
export const 국민연금 = 0.0475;
export const 건강보험 = 0.03595;
export const 장기요양_대_건강보험 = 0.1314;
export const 고용보험 = 0.009;

/* 국민연금은 소득에 상·하한이 있습니다. 2026년 값을 못 찾아 안 넣었습니다 —
   치료사 급여대에서는 상한(월 600만원대)에 거의 안 닿습니다.
   ponytail: 닿는 사람이 생기면 그때 넣습니다. 지금 넣으면 틀린 숫자만 늡니다 */

/* ── 연차 (근로기준법 제60조) ───────────────────────
   숫자가 법 조문에 박혀 있어 해마다 바뀌지 않습니다.
   법이 바뀌면 여기만 고칩니다 */
export const 연차 = {
  /** 1년 미만 — 한 달 개근마다 하루. 이만큼까지 */
  미만_한도: 11,
  /** 1년 이상 — 기본 일수 */
  기본: 15,
  /** 3년째부터 2년마다 하루씩 늘어납니다 */
  가산_주기_년: 2,
  /** 아무리 오래 다녀도 이 이상은 안 늡니다 */
  한도: 25,
} as const;

/* ── 퇴직금 (근로자퇴직급여보장법 제8조) ─────────────
   퇴직금 = 1일 평균임금 × 30 × 재직일수 ÷ 365 */
export const 퇴직금 = {
  /** 이 일수를 못 채우면 법정 퇴직금이 없습니다 */
  최소_재직일: 365,
  /** 평균임금은 퇴직 전 이 기간의 임금으로 셉니다 */
  평균임금_개월: 3,
  /** 1년치로 환산할 때 곱하는 일수 */
  일수: 30,
} as const;

/* ── 안 만든 것 ────────────────────────────────────
   **실업급여 계산기는 일부러 안 만들었습니다** (2026-09-23).

   1일 상한액·하한액과 기초 임금일액 상한이 **해마다 바뀝니다.**
   옛 앱 코드에 박혀 있던 2026년 값도 출처를 확인하지 못했습니다.
   틀린 금액을 보여주면 그걸 믿고 계산한 사람이 손해를 봅니다.

   **매년 확인할 사람이 정해지기 전에는 만들지 않습니다.**
   만들 때는 이 파일에 값과 출처를 함께 넣고, 해가 바뀌면 화면에
   「작년 기준입니다」가 뜨게 해야 합니다. */
