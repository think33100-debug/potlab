# 수집기 지도

2026-09-20. `gas/wage.js` 를 읽고 적은 것입니다. **코드는 건드리지 않았습니다.**
계획은 여기 없습니다 — 지도만 그립니다.

파일 전체는 **19,879줄 · 함수 656개**입니다.
그중 **수집에 이어진 것이 391개 · 12,791줄** (밑줄로 끝나는 것 228 · 아닌 것 163).
나머지 265개는 급여·커뮤니티·관리자 화면·카카오 로그인 쪽입니다.

---

## 1. 수집 함수 목록

### 1-1. 밑줄 없는 것 — 손으로 실행하거나 트리거·화면이 부르는 것

| 함수 | 줄 | 줄수 | 무엇을 | 누가 부르나 | 무엇을 부르나 | 시트 |
|---|---:|---:|---|---|---|---|
| `collectTick` | 14888 | 18 | **수집 전체의 입구.** 30분 트리거가 여기로 들어옴. 오류를 `tick_err` 에 남김 | 시간 트리거 | `collectTick_` | — |
| `collectTick_` | 14906 | 107 | 시간을 나눠 수집기들을 순서대로 부름 (아래 6번) | `collectTick` | `schedCfg_` · `collectJobs` · `collectJobs2` · `collectNid` · `collectWorknet` · `collectJobs3` · `stampJobClass` · `applyDropRules` · `flushViews` | 채용공고 |
| `collectJobs` | 9302 | 198 | **알리오** — 공공기관 경영정보 API | `collectTick_` · `collectAllJobs` · `showTriggers` | `alioDetail_` · `alioJob_` · `matchJob_` · `holdEvents_` · `mailNewJobs` | 채용공고 |
| `collectJobs2` | 1608 | 114 | **잡플러스** — 지자체 채용 | 〃 | `matchJob_` · `notOurs_` · `holdEvents_` | 채용공고 |
| `collectJobs3` | 8109 | **286** | **나라일터** — 상세·첨부(PDF·HWP)까지 열어 봄 | 〃 | `job3Url_` · `job3Detail_` · `job3File_` · `job3Type_` · `job3Worth_` · `job3SkipInst_` · `holdEvents_` | 채용공고 |
| `collectWorknet` | 2620 | 153 | **워크넷** — 목록 받고 애매한 것만 상세를 엶 | 〃 | `wnMatcher_` · `wnJudge_` · `matchJob_` · `notOurs_` · `rejudgeWorknet` · `holdEvents_` | 채용공고 · WN판정 |
| `collectNid` | 12968 | 153 | **중앙치매센터** — 게시판 긁기 | `collectTick_` · `collectAllJobs` | `nidParse_` | 채용공고 |
| `collectHosp` | 6835 | 106 | **병원 홈페이지 게시판** 약 200곳 긁기 | `hospTick` (별도 트리거) | `hospDetailJob_` · `stampJobClass` · `applyDropRules` | 채용공고 |
| `hospTick` | 7266 | 8 | 병원 게시판 트리거 입구 | 시간 트리거 | `schedCfg_` · `collectHosp` | — |
| `collectAllJobs` | 1876 | 123 | 네 수집기를 한 번에 — **손으로 실행하는 용도** | `showTriggers` | `collectJobs`·`2`·`3` · `collectWorknet` · `collectNid` | 채용공고 |
| `rejudgeWorknet` | 2773 | 102 | 워크넷 줄을 다시 판정 (상세를 열어) | `collectWorknet` | `wnJudge_` · `matchJob_` · `notOurs_` | 채용공고 · WN판정 |
| `stampJobClass` | 3051 | 34 | 네 탭 중 어디인지 시트에 적음 | `collectTick_` · `collectHosp` | — | 채용공고 |
| `applyDropRules` | 7623 | 18 | 버림 규칙에 걸리는 것을 숨김 | `collectTick_` · `collectHosp` | `dropRules_` · `dropRuleHit_` | 채용공고 · 버림규칙 |
| `dropAnalyze` | 7475 | 79 | 버린 공고를 모아 규칙 후보를 뽑고 메일 | 시간 트리거(9시·18시) | `dropRulesText_` | 버림분석 · 버림규칙 |
| `cleanJobs` | 9569 | 20 | 오래된 공고 치우기 | 시간 트리거(월 4시) | — | 채용공고 |
| `mailNewJobs` | 9608 | 109 | 새 공고를 회원에게 메일 | `collectJobs` · `watchRegionJobs` | — | 채용공고 · 알림설정 |
| `flushViews` | 18011 | 30 | 쌓아둔 조회수를 시트에 적음 | `collectTick_` | — | 공고반응 |
| `setupJobTrigger` | 15054 | 17 | 트리거를 만듦 — **손으로 한 번** | 손 | `ScriptApp.newTrigger` | — |
| `showTriggers` | 3610 | 47 | 트리거·표시 상태를 봄 | 손 | `schedCfg_` · 수집기들 | — |
| `collectEdu` | 19236 | 27 | 교육소식 수집 | 시간 트리거 | — | 교육소식 |

**진단·조사용 함수가 77개 더 있습니다** (`test*` · `diag*` · `survey*`).
전부 손으로 실행하는 것이고 수집 흐름에는 안 끼어 있습니다.
`testJob3File`(115줄) · `testHwp`(80줄) · `diagNoDeadline`(178줄) 처럼 큽니다.

### 1-2. 밑줄로 끝나는 것 — 안에서만 쓰는 것

| 함수 | 줄 | 줄수 | 무엇을 | 누가 부르나 |
|---|---:|---:|---|---|
| `matchJob_` | 9522 | 27 | **직군 판정** (작업/물리/공통) | 수집기 전부 |
| `notOurs_` | 8554 | 16 | 제목이 남의 직군 자리인지 | `collectJobs2` · `collectWorknet` · `rejudgeWorknet` |
| `jobsIn_` | 9549 | 13 | 글에 어떤 직종들이 있는지 | `jobPosts_` |
| `wnJudge_` | 4413 | 33 | 워크넷 상세를 보고 담기/보류/버림 | `collectWorknet` · `rejudgeWorknet` |
| `wnMatcher_` | 2314 | 150 | 워크넷 기관을 심평원 병원과 맞춤 | `collectWorknet` |
| `holdEvents_` | 3136 | 13 | 행사·박람회를 보류함으로 | 수집기 전부 |
| `initJobSheet` | 9084 | 22 | 채용공고 시트를 열고 머리글을 맞춤 | 거의 모든 수집·진단 함수 |
| `job3Url_` | 8100 | 9 | 나라일터 주소 조립 (인증키 붙임) | `collectJobs3` 외 |
| `job3Detail_` | 8465 | 14 | 나라일터 상세 | `collectJobs3` |
| `job3File_` | 8395 | 70 | **나라일터 첨부** — PDF 고르고 HWP 는 따로 | `collectJobs3` |
| `job3Type_` | 8497 | 28 | 첨부 유형(A공고문/B지원서/C직무기술서) | `collectJobs3` |
| `job3Worth_` | 8491 | 6 | 상세를 열 값어치가 있나 | `collectJobs3` 외 |
| `job3SkipInst_` | 5397 | 10 | 나라일터 기관 거르기 | `collectJobs3` |
| `alioDetail_` | 13668 | 21 | 알리오 상세 (`/detail` · `steps`) | `collectJobs` 외 |
| `alioJob_` | 13689 | 66 | 알리오 `steps` 에서 직군 뽑기 | `collectJobs` |
| `alioCookie_` / `alioFile_` | 5335 / 5358 | 22 / 9 | 알리오 첨부를 쿠키 경로로 받기 | `nidPdfText_` 외 |
| `nidParse_` | 12798 | **170** | 치매센터 글 파싱 (HWP 포함) | `collectNid` |
| `hospDetailJob_` | 7686 | 27 | 병원 게시판 상세로 담기/보류/버림 | `collectHosp` |
| `dropRules_` | 7576 | 9 | 버림 규칙 읽기 (캐시 10분) | `dropRuleHit_` · `applyDropRules` |
| `dropRuleHit_` | 7585 | 13 | 규칙에 걸리나 | `applyDropRules` · `dropRulesFilter_` |
| `schedCfg_` | 14870 | 18 | 수집 시각 설정 읽기 | `collectTick_` · `hospTick` |

---

## 2. 6분 제한 때문에 쪼개둔 곳

**한 번 실행 상한은 300초**로 잡혀 있습니다 (`collectTick_` 의 `LIMIT`).
6분(360초)까지 60초를 남겨 둔 것입니다 — 예산이 끝나도 열던 상세는 끝까지 가기 때문입니다.

| 어디 | 무엇을 · 어디에 기억 | 한 번에 | 깨지면 |
|---|---|---|---|
| **`collectTick_`** | 시간 예산을 나눠 줌. `heavy_done`(속성) 에 `yyyyMMdd_시` 를 적어 같은 시각 두 번 방지 | 알리오 100초 · 잡플러스 50초 · 치매센터 40초 · 워크넷 60~120초 · 나라일터 남은 것 전부 | 그 시각 무거운 것을 건너뜀. 30분 뒤 다시 |
| **워크넷** `collectWorknet` | `wn_done`(속성) = 그날 다 했는지 · `wn_more` = 상세를 다 못 열었는지 | 목록은 예산 절반까지, 상세는 예산 15초 남을 때까지 | `wn_more` 가 남아 30분 뒤 이어감. 못 붙으면 `null` 을 돌려 그날 계속 재시도 |
| **나라일터** `collectJobs3` | `job3_done`(속성, JSON) = 열어본 공고 번호 · `job3_at` = 어디까지 | 한 쪽 100건 · 최대 30쪽(3,000건) | 열어본 기록이 있어 다음 실행에 새 것부터. **기록이 날아가면 처음부터 다시 엶** |
| **알리오** `collectJobs` | `alio_done`(속성) | 한 쪽 API `ROWS` 씩 · 최대 120쪽 | 처음부터 다시 |
| **병원 게시판** `collectHosp` | `hs_next`(속성) = 다음에 볼 사이트 번호 · `hs_cnt` = 사이트별 건수 · `hs_dt_tried` = 상세 시도 기록 | 예산 8초 남을 때까지 사이트를 돎. 상세는 15초 남을 때까지 | `hs_next` 가 날아가면 1번 사이트부터. **약 200곳이라 한 바퀴가 여러 번에 나뉨** |
| **치매센터** `collectNid` | 전역 변수 `NID_DEADLINE` — **속성이 아님** | 예산(40초) 안에서 쪽 단위 | 실행이 끝나면 사라짐. 다음 실행은 1쪽부터 |
| **청년정책** `pullYouth` | `ypage`(속성) = 다음 쪽 | 쪽 단위 | `ypage` 가 날아가면 1쪽부터 |
| **바우처기관** | `vpage_8000` · `vpage_4000`(속성) | 쪽 단위 | 〃 |

### 풀 수 있는 것 / 원래 쪼개야 하는 것

**시간 제한 때문에 쪼갠 것 — Supabase 로 가면 없어도 되는 것**

- `collectTick_` 의 300초 예산 나누기 전체
- 워크넷 `wn_more` (상세를 다 못 연 것)
- 병원 게시판 `hs_next` (200곳을 나눠 도는 것)
- 나라일터의 예산 중단 (`job3_at`)
- 치매센터 `NID_DEADLINE`
- 무거운 것을 하루 3번으로 미룬 `heavyHours`

**상대 쪽 사정이라 그대로 쪼개야 하는 것**

- **API 쪽 나누기** — 알리오 `MAX_PAGE: 120` · 나라일터 `numOfRows: 100` · 워크넷 `display=100`.
  상대가 한 번에 주는 양이 정해져 있어 쪽 단위 요청은 그대로 남습니다
- **나라일터 `job3_done`** — 한 번 연 공고를 또 열지 않기 위한 기록입니다.
  시간이 아니라 **상대 서버에 대한 예의**라 남겨야 합니다
- **병원 게시판 `hs_dt_tried`** — 같은 글의 상세를 반복해서 두드리지 않기 위한 것
- **알리오 쿠키** (`alioCookie_`) — 첨부를 받으려면 세션을 먼저 받아야 합니다

---

## 3. Apps Script 에만 있는 것

파일 전체 등장 횟수입니다 (수집 밖 코드 포함).

| 무엇 | 횟수 | 쓰는 자리 |
|---|---:|---|
| `Logger` | **461** | 거의 모든 함수의 기록 |
| `Utilities` | **223** | 날짜 형식(`formatDate`) · HMAC · base64 · `sleep` |
| `SpreadsheetApp` | **107** | 시트 33개를 읽고 씀 |
| `CacheService` | **94** | 목록 캐시 21,600초 · 급여 6시간 · 버림규칙 600초 |
| `UrlFetchApp` | **74** | 모든 바깥 요청 (API·게시판 긁기·첨부 받기) |
| `PropertiesService` | **35** | 이어가기 표시 · 인증키 · 설정 |
| `ScriptApp` | **29** | 시간 트리거 만들기·지우기 · 배포 주소 |
| `MailApp` | **15** | 보류 메일 · 새 공고 알림 |
| `ContentService` | **11** | 웹앱 응답(JSONP) |
| `DriveApp` | **6** | **PDF 읽기** (`convert: true`) · 재직증명서 폴더 |
| `GmailApp` | **5** | 받은 메일 읽기(`mailInboxTick`) |
| `LockService` | **5** | 동시 실행 막기 |
| `HtmlService` | **4** | 관리자 화면 |

**그 밖에 Apps Script 고유**

- **Drive `convert: true` 로 PDF 읽기** — `alioFile_` · `job3File_` · `nidPdfText_` 에서.
  `mimeType` 을 같이 주면 충돌한다는 주석이 있습니다
- **HWP 바이트 훑기** — `hwpJob_` · `nidParse_` 에서 `getBlob().getBytes()` 로
  앞 5바이트를 봐 `%PDF-` 인지 확인하고, `.hwp` 는 못 읽어 보류함으로 보냅니다
  (첨부 관련 코드는 `job3File_` 70줄 · `nidParse_` 170줄에 흩어져 있습니다)
- **웹앱 `doGet`** — 화면과 관리자 API 가 전부 이 하나를 거칩니다
- **주소 길이 한도** — 한글이 주소에서 9배로 부푸는 문제. 600자씩 쪼개 JSONP 로 보냄
- **밑줄로 끝나는 함수는 실행 목록에 안 뜸** — 손으로 실행할 것은 밑줄 없이 지음

---

## 4. 인증키

**코드 안에 값이 박혀 있는 것 — 13곳**
(이름만 적습니다. 값은 여기 안 옮깁니다)

| 상수 | 줄 | 무엇 |
|---|---:|---|
| `JOB_API.KEY` | 1462 | 알리오(공공기관 경영정보) |
| `JOB2_API.KEY` | 1492 | 잡플러스(지자체 채용) |
| `JOB3_API.KEY` | 1910 | 나라일터 |
| `WORK_API.KEY` | 1989 | 워크넷(고용24) |
| `PUSH.KEY` | 8885 | 웹푸시 |
| `VOL_API.KEY` | 10899 | 자원봉사 |
| `DRS_API.KEY` | 11329 | 발달재활 |
| `SSV_API.KEY` | 11405 | 사회서비스 |
| `YTH_API.KEY` | 11940 | 온통청년 |
| `EXAM_API.KEY` | 12497 | 국가고시 |
| `ALIO_D.KEY` | 13662 | 알리오 상세 |
| `KAKAO.REST_KEY` · `KAKAO.CLIENT_SECRET` | 16262 | 카카오 로그인 (**2개**) |
| `NAVER.CLIENT_ID` · `NAVER.CLIENT_SECRET` | 16280 | 네이버 로그인 (**2개**) |

**스크립트 속성에 든 것 — 3곳**

| 이름 | 줄 | 무엇 |
|---|---:|---|
| `ADMIN_KEY` | 215 · 244 | 관리자 화면 열쇠. 보류 메일 단추 서명(HMAC)에도 씀 |
| `GH_TOKEN` | 18576 | GitHub — SEO 페이지 올리기 |
| `KAKAO_SECRET` | 16335 · 16338 | 카카오 쪽. 속성에 없으면 코드의 `CLIENT_SECRET` 을 넣어 둠 |

**합 16개**입니다. 인수인계의 「21곳」과 다릅니다 —
같은 키를 여러 자리에서 쓰는 것을 따로 센 것으로 보이나, **확인하지 못했습니다.**

키를 **가리는 코드**가 따로 있습니다 — 오류 메시지에서
`String(e.message).split(WORK_API.KEY).join('(키)')` 처럼 값을 지웁니다 (8곳).

---

## 5. 시트 목록

`SpreadsheetApp` 으로 여는 시트 **33개**입니다.
새 쪽 표는 Supabase 의 실제 줄 수입니다.

### 수집이 직접 읽고 쓰는 것

| 시트 | 상수 | 줄 | 새 쪽 표 | 새 쪽 줄수 |
|---|---|---:|---|---:|
| 채용공고 | `JSHEET` | 8503 | `job_posts` | 628 |
| 공고수정 | `XSHEET` | 15617 | `job_post_edits` | 0 |
| 버림규칙 | `DR_SHEET` | 7410 | `drop_rules` | 108 |
| 버림분석 | `DA_SHEET` | 7408 | **없음** | — |
| WN판정 | `WN_VERDICT_SHEET` | 2576 | `wn_verdicts` | 416 |
| 수집 가능 기관 | `OSHEET` | 15779 | `collectable_orgs` | 671 |
| 공고반응 | `TKSHEET` | 5721 | `job_events` | 0 |
| 알림설정 | `NSHEET` | 8778 | `notification_settings` | 3 |

### 기관 자료 — 이미 옮겨진 것

| 시트 | 상수 | 새 쪽 표 | 새 쪽 줄수 |
|---|---|---|---:|
| 병원목록 | `HSHEET` | `hospitals` | 15,332 |
| 요양기관 | `CARE_SHEET` | `ltc_facilities` | 30,595 |
| 복지시설 | `WELF_SHEET` | `welfare_facilities` | 14,710 |
| 복지관 | `WSHEET_WF` | `welfare_centers` | 266 |
| 공공기관 | `PUB_SHEET` | `public_hospitals` | 231 |
| 바우처기관 | `VSHEET` | **없음**. 발달·재활·인지·감각 사업유형만 남긴 것 (`dev_rehab_orgs` 305 와 다름) | — |
| 지역기관수 | `RSHEET` | **없음** (`org_facet_mv` 가 대신) | 133 |

### 수집 밖 — 급여·커뮤니티·화면

| 시트 | 상수 | 새 쪽 표 |
|---|---|---|
| 급여데이터 | `WSHEET` | `salary_records` |
| 기관초임 | `PSHEET` | **없음** |
| 커뮤니티 · 댓글 · 좋아요 · 신고 | `CSHEET` 외 3 | `posts` · `comments` · `post_likes` · `reports` |
| 배너 | `BSHEET` | `home_blocks` |
| 메뉴설정 · 문구 | `MSHEET` · `TSHEET` | **없음** |
| 관심공고 | `SSHEET` | `job_stars` |
| 건의사항 | `FSHEET` | **없음** |
| 접근기록 | `LOGSHEET` | `page_hits` |
| 기업 | `GSHEET` | **없음** |
| 청년정책 | `YSHEET` | **없음** |
| 교육소식 | `ESHEET` | **없음** |
| 조사_워크넷265 · 조사_워크넷상세 | `WN_FULL_SHEET` · `WN_DTL_SHEET` | **없음** (조사용) |

**새 쪽에 대응이 없는 것 13개**입니다.
그중 `버림분석` 은 수집이 매번 쓰는 시트라 옮길 때 자리가 필요합니다.

---

## 6. 도는 순서

### 트리거 — 6개

| 함수 | 주기 | 만드는 함수 |
|---|---|---|
| `collectTick` | **30분마다** | `setupJobTrigger` (15064) |
| `hospTick` | **1시간마다** (또는 2시간) | 7297 · 7304 |
| `dropAnalyze` | **하루 2번 — 9시 · 18시** | 7676 · 7677 |
| `cleanJobs` | **매주 월요일 4시** | 15065 |
| `mailInboxTick` | **5분마다** | 7880 |
| `watchRegionJobs` | **매일 5시** | 17452 |
| `buildSeoPages` | **매일 4시** | 18768 |
| `collectEdu` | 정해진 시각 | 19269 |

### `collectTick` 한 번에 도는 순서

먼저 건너뛸지 봅니다 — **평일만 · 7시~19시 · 수집이 켜져 있을 때**
(`collect_cfg` 속성에서 읽고, 관리자 화면에서 바꿉니다)

```
① 알리오          100초
② 조회수 반영      가벼움
③ (9·14·18시에만)
     잡플러스      50초
     치매센터      40초
④ 워크넷          60~120초   (그날 한 번 다 끝낼 때까지 30분마다 재시도)
⑤ 나라일터        남은 시간 전부 (최소 60초)
⑥ 탭 분류 기록
⑦ 버림 규칙 적용 (최근 2일)
                  ─────────
                  상한 300초
```

병원 게시판(`collectHosp`)은 **이 흐름 밖**입니다 — `hospTick` 이 1시간마다 따로 돕니다.

---

## 7. 읽으면서 까다롭겠다 싶었던 것 셋

### ① 첨부를 읽는 길이 Apps Script 에 묶여 있습니다

PDF 를 읽는 유일한 방법이 **Drive 에 올려 `convert: true` 로 변환**하는 것입니다
(`alioFile_` · `job3File_` · `nidPdfText_`). HWP 는 바이트를 직접 훑습니다.

나라일터 한 건에 **첨부를 읽느라 수 초씩** 듭니다 (4711행 주석).
그 위에 **알리오는 쿠키를 먼저 받아야** 첨부가 옵니다.

옮기면 PDF·HWP 읽기를 처음부터 다시 만들어야 하고,
**지금 잘 읽히는지 틀리게 읽히는지 견줄 기준이 없습니다** — 원문이 시트에 안 남습니다.

### ② 이어가기 표시가 속성 8개에 흩어져 있고, 서로 규칙이 다릅니다

`job3_done` 은 JSON 뭉치, `hs_next` 는 숫자 하나, `wn_more` 는 `'1'` 문자열,
`NID_DEADLINE` 은 아예 **전역 변수**라 실행이 끝나면 사라집니다.

시간 제한이 없어지면 대부분 풀 수 있지만,
**「상대 서버를 다시 안 두드리기」 위한 기록(`job3_done` · `hs_dt_tried`)은 남겨야 하고**,
그 둘이 같은 속성 안에 시간 제한용 표시와 섞여 있습니다. 갈라내야 합니다.

`collector_state` 표가 새 쪽에 이미 있는데 **0줄**입니다.

### ③ 수집과 화면과 관리자가 한 파일에 섞여 있습니다

`collectJobs` 가 `mailNewJobs` 를 부르고, `mailNewJobs` 가 `알림설정` 시트를 읽고,
관리자 화면 함수들이 `ADMIN_KEY` 로 같은 시트를 만집니다.
`initJobSheet` 는 **수집·진단·관리자 함수 수십 개가 같이** 부릅니다.

수집만 떼어내려 해도 **391개 함수 12,791줄이 서로 물려 있어**
어디서 자를지가 분명하지 않습니다. 진단 함수 77개도 같은 시트를 봅니다.
