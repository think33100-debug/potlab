# 클린아이 이전 지도 (2026-09-26)

두 번째 경로입니다. **지도만 만들었습니다. 코드는 한 줄도 안 썼습니다.**

> **먼저 읽으실 것 — 이 경로는 알리오와 성격이 아주 다릅니다.**
> 알리오는 「상세를 열어야 알 수 있다」가 문제였는데,
> 클린아이는 **「어디를 봐도 직군이 안 적혀 있다」** 가 문제입니다.
> 아래 4번을 먼저 보시고 옮길지 정하시는 게 낫습니다.

---

## 1. 클린아이 경로가 쓰는 함수

알리오보다 훨씬 단순합니다. **상세 API 도 첨부 읽기도 없습니다.**

| 함수 | 줄 | 하는 일 | node 로 옮길 때 |
|---|---|---|---|
| `collectJobs2` | 1609 | **본체.** 시도 17곳을 돌며 목록 → 판정 → 시트 | 새로 씀 |
| `job2Fetch_` | 1524 | 한 시도. 3번까지 다시 시도 | 그대로 |
| `job2Url_` | 1541 | 인증키를 붙인 주소. **키는 다시 인코딩 안 함** | 그대로 |
| `xmlItems_` | 1550 | XML → 객체. **엔티티를 변화 없을 때까지 반복해 풂** | 그대로 (아래 ③) |
| `matchJob_` · `notOurs_` · `sameJob_` · `fmtDate_` · `cut_` | — | 알리오와 **같은 것** | `tools/gas-rules.mjs` 그대로 |
| `sortRows_` | 3389 | 네 갈래 문지기 | `tools/sort-rule.mjs` 그대로 |

**안 옮기는 것** — 6분 예산 나누기. 17개 시도를 한 번에 돕니다(아래 ②에서 21초).

---

## 2. 시트 칸 ↔ DB 칸

| # | 시트 칸 | 클린아이 항목 | DB 칸 |
|---|---|---|---|
| 0 | 공고ID | `'CE' + NO` | `id` |
| 2 | 기관명 | `ENT_NAME` | `org_name` |
| 3 | 공고명 | `ENT_TITLE` | `title` |
| 4 | 채용구분 | `EMPLOY_GB` | `hire_type` |
| 5 | 고용형태 | `JOB_TYPE` | `employ_type` |
| 6 | 근무지 | `WORK_PLACE` 또는 `SIDO_CD` | `work_place` → `sido`·`sgg` |
| 8 | 인원 | `EMPLOY_NUM` | `headcount` |
| 9 | 접수시작 | `PUB_DATE` | `apply_from` |
| 10 | 접수마감 | `PUB_END_DATE` | `apply_to` |
| 11 | 링크 | `URL` | `url` |
| 12 | 직군 | 판정 결과 | `job_group` |
| 18 | 지원자격 | `ENT_LICENSE1~4` 를 이어 붙임 | `detail.지원자격` |
| 25 | 연봉 | `YEARINCOME` | `detail.연봉` |

**안 쓰고 있는 항목** — `OFFICE_HOURS` · `RETIREMENT` · `JUDGE_METHOD` ·
`LOCAL_YN`/`LOCAL_NAME`(지역인재) · `SPECIAL_ITEM` · `ACCUSATION` · `REFERENCE` ·
`EXHIBIT` · `FILE_NAME1~3` · `ADDRESS` · `UPD_DATE`.
옮길 때 `detail` 에 담아 두면 화면에서 쓸 수 있습니다.

---

## 3. 직접 두드려 확인한 것

### ① 목록은 GET · XML · **node 에서 됩니다**

```
GET https://apis.data.go.kr/B551982/openApiEmployInfo/openXmlEmployInfo
      ?serviceKey=…&type=xml&sidoCd=007001
  → HTTP 200 · text/xml · 80,523자 · 1.1초
```

인증키는 **알리오 상세와 같은 값**입니다 (공공데이터포털 계정 열쇠 하나).
**이 열쇠를 갈면 두 경로가 같이 멈춥니다.**

### ② 17개 시도 전부 — **360건 · 21초**

```
전체            360건
그중 ENT_KIND 가 「의료」  95건
상태            모집중 93 · 모집예정 2
100건이 꽉 찬 시도  없음
```

> ⚠ **gas 주석이 지금은 안 맞습니다.** 「경기도가 100건이라 일부 누락될 수
> 있습니다」 라고 적혀 있는데, 오늘 재보니 **100건에 닿은 시도가 하나도
> 없습니다.** 서울이 57건으로 제일 많았고 `totalCount` 도 57 로 맞았습니다.
> (gas 는 동결이라 그 주석을 안 고쳤습니다)

### ③ 엔티티가 두 번 감싸여 옵니다

`xmlItems_` 가 「변화가 없을 때까지」 반복해서 푸는 까닭입니다 —
`&amp;amp;` 같은 값이 옵니다. **한 번만 풀면 안 됩니다.**

### ④ 항목 40개 (한 줄에 다 옵니다)

```
ENT_NAME · ENT_TITLE · ENT_GB · ENT_KIND · SIDO_CD · STATUS · EMPYEAR ·
ENT_RECRUIT · EMPLOY_NUM · EMPLOY_GB · JOB_TYPE · RE_MAN_YN · POSITION ·
YEARINCOME · OFFICE_HOURS · WORK_PLACE · DUTY_DETAIL · RETIREMENT ·
JUDGE_METHOD · LOCAL_YN · LOCAL_NAME · LOCAL_REASON ·
ENT_LICENSE1~4 · SPECIAL_ITEM · PUB_DATE · PUB_END_DATE · ACCUSATION ·
REFERENCE · EXHIBIT · JOB_SEEK_ETC · FILE_NAME1~3 · URL · ADDRESS · NO · UPD_DATE
```

---

## 4. 이 경로의 진짜 문제 — 직군이 **목록 어디에도** 안 적혀 있습니다

의료 공고 **95건을 전부** 훑어 「어느 칸에서 직군이 갈리나」 를 셌습니다.

| 어디서 갈리나 | 건수 |
|---|---|
| 제목 | **0건** |
| 자격증(`ENT_LICENSE1~4`) | **0건** |
| 직무(`POSITION`·`DUTY_DETAIL`·`ENT_RECRUIT`) | **0건** |
| 남의 자리라 버림 | 25건 |
| **못 가림** | **70건** |

```
서울의료원    | 기간제근로자 전문의(중환자실 당직 전문의) 상시모집     [첨부 없음]
부산의료원    | 직원 모집 공고(기록물관리 전문요원 - 행정직)        [첨부 …행정직) 공고.hwp]
대구의료원    | 감사실장(별정직) 모집 공고                      [첨부 …모집 공고.hwp]
경기도의료원  | 수원병원 의사직(안과 전문의) 초빙 공고            [첨부 …안과 전문의).hwp]
```

**답은 전부 첨부에 있습니다.** 그리고 **의료 95건이 전부 첨부를 갖고 있습니다.**

```
.hwp   48건
.hwpx   1건
.pdf   44건
없음    2건
```

### ✅ 2026-09-26 — `.hwp` 가 읽힙니다 (이 지도의 결론이 바뀌었습니다)

처음 쓸 때는 「`.hwp` 는 못 읽으니 뚫을 구멍이 없다」 며 **미루기를 권했습니다.**
그날 오후에 실제 파일로 확인해 보니 **읽힙니다.** 자세한 것은 `hwp_읽기_조사.md`.

라이브러리 셋(`hwp.js`·`@ohah/hwpjs`·`node-hwp`)은 다 실패했고,
**파일을 통째로 UTF-16LE 로 훑는 맨손 방법**이 됐습니다. 부품은 `tools/hwp/`.

`.pdf` 44건은 이미 읽습니다 (알리오에 붙인 `tools/ocr/`).
→ 첨부를 읽으면 **95건 중 93건**의 본문이 손에 들어옵니다.

그리고 `.hwp` 를 쓰는 곳이 어디인지 보면 —

```
부산 · 대구 · 경기도 · 영월 · 홍성 · 공주 · 군산 · 진안군 · 제주 · 서귀포 의료원
```

**전부 지방의료원 — 1층입니다.**

### ⚠ 그 대신 새 걸림돌 — 첨부 다운로드를 **건수로** 막습니다

`tools/hwp/bucket.mjs` 로 **재봤습니다** —

```
0.25초 간격 → 15건에서 HTTP 429
1.2초  간격 → 15건에서 HTTP 429      ← 같은 자리. 간격 문제가 아닙니다
2분 쉰 뒤   → 다시 15건
5분 쉰 뒤   → 다시 15건              ← 2분이면 이미 다 찹니다
  본문 「다운로드 요청이 많습니다. 잠시 후 다시 시도해 주세요.」 (text/plain · 75바이트)
```

**「하루 N건」도 「시간당 N건」도 아닙니다. 통 15건 · 2분이면 가득입니다.**
쉬어 가며 받으면 시간당 450건쯤 되니 의료 95건은 넉넉합니다.

**그리고 429 로 튕긴 요청도 통을 먹습니다.** 곧바로 다시 두드리면 통이 영영
안 찹니다. 그래서 `첨부받기` 의 기본 재시도를 **껐습니다**(다시: 0).
수집기는 통이 차면 2분 반 쉬고 이어가다가, 이번 실행 몫(18분)을 넘기면
나머지를 **「다음에」** 로 넘깁니다 — 다음 실행이 이어서 읽습니다.

> ⚠ 이걸 모르면 **읽기 실패로 잘못 셉니다.** 429 는 파일이 아니라 짧은 글로 와서,
> 그냥 넘기면 「한글 파일이 아닙니다」 가 34건 나옵니다. 처음에 그렇게 셌습니다.

### 표에 쌓인 것 — 옛 수집기는 **못 가린 것을 그냥 버립니다**

```
클린아이로 들어온 공고  7건 (2026-08-29 ~ 09-11) · 회원 목록 6건
```

한 달에 7건인 까닭이 이것입니다. `collectJobs2` 에 `if (!job) return;` 한 줄이 있습니다.
**첨부를 안 읽으니 70건이 그냥 사라집니다.**

---

## 5. 판단이 바뀌었습니다 — ㉰ 미루기 → ㉯ 첨부 읽기

| 길 | 처음(오전) | 지금 |
|---|---|---|
| ㉮ 그냥 옮긴다 | 쉽지만 얻는 게 없음 | 같음 |
| ㉯ **첨부를 읽는다** | 「뚫을 구멍이 없다」 | **뚫렸습니다.** 95건 중 93건이 열립니다 |
| ㉰ 뒤로 미룬다 | **권했음** | 미룰 이유가 없어졌습니다 |

「한 달에 7건」 은 클린아이가 공고를 안 낸다는 뜻이 아니라
**우리가 70건을 버리고 있었다**는 뜻이었습니다.

---

## 6. 새 수집기 (CE2) — 만들었습니다

`tools/collect-cleaneye.mjs`

```
목록 17시도 → 의료만 → notOurs
  → 제목 → 자격증 → 직무
  → **첨부 열기** (hwp: tools/hwp · pdf: tools/ocr)
  → 네 갈래 (tools/sort-rule.mjs)
  → collect_put(source='CE2')
```

**보류함이냐 쓰레기통이냐를 「읽었나」 로 가릅니다** (2026-09-26 에 정한 것)

```
보류함   = 「모르겠다, 사람이 봐 달라」   ← 첨부가 없거나 못 읽었을 때
쓰레기통 = 「우리 것이 아닌 걸 안다」     ← 첨부를 읽었는데 우리 직군이 없을 때
```

첨부를 읽으면 「모른다」 가 「안다」 로 바뀝니다. 이 구분이 없으면
하루 70건이 보류함에 쌓이고, 그중 23건이 의사직·병원보조입니다 —
관리자가 그걸 다 넘겨야 진짜 봐야 할 것에 닿습니다.

| 갈래 | 첨부 안 읽을 때 (옛 조건) |
|---|---:|
| 회원 목록 | 0 |
| 보류함 | 70 |
| 쓰레기통 | 0 |

붙인 것 — `collect_source` 에 `CE2` · `collect_secret` 열쇠 ·
`.github/workflows/collect-cleaneye.yml` (**2시간마다.** 30분이면 통이
비어 있는 채로 계속 두드립니다). `sync.yml` 은 안 건드렸습니다.

**세중님이 GitHub Secrets 에 넣으실 것**

```
CLEANEYE_KEY      알리오 상세(ALIO_DETAIL_KEY)와 같은 값입니다.
                  이름만 따로 둡니다 — 나중에 갈 때 헷갈리지 않게
COLLECT_KEY_CE2   -clFK_rowqf2id10dbJ9bi8gjSN07jZ2
```

나머지(`SUPABASE_URL`·`SUPABASE_ANON_KEY`·`OCR_GAS_URL`·`OCR_KEY`)는
알리오 때 넣으신 것을 그대로 씁니다.
