# Supabase 단계 기록 · 되돌리는 법

프로젝트 `qbnxycavokzcdonlfdat` · 2026-09-18

모든 변경은 **마이그레이션으로** 넣었습니다. 대시보드에서 손으로 만든 표는 없습니다.
되돌릴 때는 **번호가 큰 것부터 거꾸로** 하십시오.

| 번호 | 이름 | 만든 것 |
|---|---|---|
| 20260918002557 | `step1_extensions_and_profiles` | `pg_trgm` · `profiles` · `admins` · `is_admin()` |
| 20260918002612 | `step2_job_posts` | `job_posts` · `job_attachments` · `job_post_edits` · 색인 6 |
| 20260918002625 | `step3_migrated_rest` | `drop_rules` · `collectable_orgs` · `wn_verdicts` |
| 20260918002633 | `step4_member_data` | `salary_records` · `student_specs` · `salary_stats()` |
| 20260918002649 | `step5_community` | `posts` · `comments` · `post_images` · `post_likes` · `reports` · 트리거 3 |
| 20260918002712 | `step6_chat` | `chat_rooms` · `chat_members` · `chat_messages` · `in_room()` · `unread_count()` |
| 20260918002720 | `step7_reactions_and_stars` | `job_events` · `job_stars` |
| 20260918002729 | `step8_collector_state_and_notify` | `collector_state` · `seen_postings` · `notification_settings` · `push_devices` · 버킷 2 |
| 20260918002804 | `step9_harden` | `pg_trgm` 을 `extensions` 로 · 트리거 함수 EXECUTE 회수 |
| 20260918002825 | `step10_grants` | 표 권한(GRANT) — **이게 없으면 RLS 를 잘 짜도 안 열립니다** |
| 20260918002857 | `step11_score_column` | `job_scores` 뷰 → `job_posts.score` 칸 · `refresh_job_scores()` |
| 20260918041338 | `step12_org_reference_tables` | 기관 자료 표 9개 + 색인 + RLS |
| 20260918041821 | `step13_service_role_grants` | **service_role 권한** — 없으면 수집기·적재기가 막힙니다 |
| 20260918041852 | `step14_org_directory` | `org_directory` 대조용 한 장 (trgm) |

## 지금 상태

```
표 33개 · 전부 RLS 켜짐 · 대조용 뷰 org_directory 1개
기관 자료 62,749줄 (1단계 완료)
공고 0 · 회원 1(세중 · 관리자) · auth 계정 1(kakao)
버킷 2개 (post-images 공개 · chat-images 비공개, 둘 다 2MB 제한 · webp/jpeg/png)
```

## 되돌리기

### 통째로 (0단계 이전으로)

```sql
drop schema public cascade;
create schema public;
grant usage on schema public to anon, authenticated, service_role;
delete from storage.buckets where id in ('post-images','chat-images');
```

자료가 0줄이라 **지금은 이게 가장 깔끔합니다.** 자료가 들어간 뒤에는 쓰지 마십시오.

### 한 단계씩

```sql
-- 14
drop materialized view org_directory;
-- 13
revoke all on all tables in schema public from service_role;
-- 12
drop table hospitals, public_hospitals, dementia_safe_centers, dementia_centers,
           dev_rehab_orgs, ltc_facilities, mental_centers,
           welfare_centers, welfare_facilities cascade;
-- 11
alter table job_posts drop column score, drop column score_at;
drop function refresh_job_scores();
-- 10
revoke all on all tables in schema public from anon, authenticated;
revoke all on all sequences in schema public from anon, authenticated;
-- 9
alter extension pg_trgm set schema public;
-- 8
delete from storage.buckets where id in ('post-images','chat-images');
drop table push_devices, notification_settings, seen_postings, collector_state cascade;
-- 7
drop table job_stars, job_events cascade;
-- 6
drop function unread_count(bigint); drop function in_room(bigint);
drop table chat_messages, chat_members, chat_rooms cascade;
-- 5
drop table reports, post_images, post_likes, comments, posts cascade;
drop function bump_like_count(); drop function bump_comment_count(); drop function comment_depth_guard();
-- 4
drop function salary_stats(text,text,int);
drop table student_specs, salary_records cascade;
-- 3
drop table wn_verdicts, collectable_orgs, drop_rules cascade;
-- 2
drop table job_post_edits, job_attachments, job_posts cascade;
-- 1
drop function is_admin(); drop table admins, profiles cascade;
```

## 만들면서 확인한 것

| 확인 | 결과 |
|---|---|
| 부분 색인이 정의대로 들어갔나 | `WHERE NOT hidden AND NOT hold` 3개 · trgm 1개 확인 |
| 댓글수·좋아요수 트리거 | 댓글 2개 → `comment_count`=2, 좋아요 1 → `like_count`=1 |
| 3단 댓글 막기 | 「댓글은 2단까지만 달 수 있습니다」 로 거부 |
| 급여 3명 미만 규칙 | `salary_stats()` 가 줄 자체를 안 돌려줌 |
| `pg_trgm` 을 옮긴 뒤 색인 | `Bitmap Index Scan on job_title_trgm` — 그대로 씀 |
| anon 으로 공고 읽기 | 2건 중 **숨기지 않은 1건만** 보임 |
| anon 으로 못 읽어야 할 것 | `collector_state` · `seen_postings` · `salary_records` · `admins` · `drop_rules` **다 막힘** |
| 시험 자료 | 전부 지웠습니다 (공고 0 · 회원 0 · auth 계정 0) |

## 짚어둘 것

**① 표 권한(GRANT)이 따로 필요합니다.**
RLS 는 「이미 닿을 수 있는 것」 을 좁히는 장치입니다. 규칙만 만들고 권한을 안 주면
`permission denied for table job_posts` 가 납니다. 실제로 그렇게 막혔고 10단계에서 고쳤습니다.
**표를 새로 만들 때마다 GRANT 를 같이 주십시오.**

**①-2 `service_role` 도 GRANT 가 필요합니다.**
RLS 는 통과하지만 표 권한까지 통과하지는 않습니다. 10단계에서 `anon`·`authenticated` 만 주고
빠뜨려서 기관 자료 적재가 `403 permission denied` 로 막혔습니다 (2026-09-18).
13단계에서 `alter default privileges` 까지 걸어 **앞으로 만드는 표에는 자동으로 붙습니다.**

**② 규칙이 하나도 없는 표 둘은 의도한 것입니다.**
`collector_state` · `seen_postings` 는 RLS 를 켜고 규칙을 안 만들어 `service_role` 만 닿습니다.
점검에 INFO 로 계속 뜨는데 고칠 것이 아닙니다.

**③ `is_admin()` · `in_room()` 은 anon 에게도 열려 있어야 합니다.**
RLS 규칙 안의 함수는 「질의를 던진 사람」 권한으로 불립니다. EXECUTE 를 뺏으면 규칙이 통째로 막힙니다.
새는 것은 「내가 관리자인가」 「내가 그 방에 있나」 뿐이고 둘 다 본인이 이미 아는 사실입니다.
점검에 WARN 으로 뜨지만 고치면 안 됩니다.

**④ `rls_auto_enable()` 은 제가 만든 게 아닙니다.**
Supabase 가 프로젝트에 넣어둔 이벤트 트리거로, `public` 에 표를 만들면 RLS 를 자동으로 켜줍니다.
주인이 `postgres` 라 건드리지 않았습니다.

**⑤ 사진은 버킷에만 만들어 뒀습니다.**
글을 지울 때 저장소 파일을 같이 지우는 일은 **아직 없습니다.** `on delete cascade` 는 DB 줄만 지웁니다.
화면을 만들 때 지우는 함수에 같이 넣거나, 주인 없는 파일을 치우는 일을 하나 두십시오.

**⑥ 안 만든 것** — `drop_analysis`(버림분석 · 수집기 이관 때), `hosp_sites`(병원 게시판 설정 288줄 · 수집기 이관 때).

## 2단계 — 공고 5종 (2026-09-18 완료)

```
채용공고 → job_posts            662 → 628   (공고ID 중복 34건 제외 · 전부 HS)
버림규칙 → drop_rules           108 → 108
수집 가능 기관 → collectable_orgs 671 → 671
WN판정 → wn_verdicts            416 → 416
공고수정 → job_post_edits          0 → 0
```

도구는 `tools/copy_jobs.js` 입니다. **시트는 읽기만 합니다** — `exportRows`(서버 v303)로 300줄씩 받아옵니다.
`공고수정` 이 0줄이라 `edited_fields` 는 `tools/copy_jobs_test.js` 로만 확인했습니다.

출처별 — WN 377 · HS 131 · AL 58 · GJ 47 · ND 8 · CE 7

**`source` 는 문서와 실제가 달랐습니다.** `현황.md` 는 「앞 두 글자 AL·JP·GJ…」 라고 하는데,
실제 공고ID 는 `WNK…`/`WNKF…`(워크넷) · `HS…` · `GJ…` · `ND…` · `CE…`(클린아이) 이거나 숫자(알리오)입니다.
662건을 세어 `sourceOf()` 를 그 모양에 맞췄습니다. `AL`·`JP` 접두사는 안 쓰입니다.

### 안 옮긴 것

| 무엇 | 왜 |
|---|---|
| 공고ID 중복 34건 | DB 는 기본열쇠라 하나만. 뒤엣것(나중 수집)을 남겼습니다 |
| 버림분석(`DA_SHEET`) | 옮기는 5종에 없습니다. 규칙만 갔고 근거 기록은 안 갔습니다 |
| 첨부(`job_attachments`) | 시트에 첨부 표가 없습니다. **수집기 이관 때** 채워집니다 |
| 인기 점수 | `job_events` 가 0줄이라 전부 0 |

---

## 수집기 옮길 때(5단계) 같이 볼 것

2026-09-18 공고를 옮기면서 자료에서 눈에 띈 것입니다. **고치지 않고 그대로 옮겼습니다.**

1. **`분류근거` 자리에 시각이 들어 있습니다** — 예: `{"분류근거": "2026-09-09 17:58:00"}`
   근거가 들어가야 할 칸에 판정한 시각이 들어갑니다. 수집기 어딘가에서 값을 잘못 넣는 것으로 보입니다.

2. **`연봉` 자리에 주소가 들어 있습니다** — 예: `{"연봉": "https://www.bohun.or.kr/…"}`
   연봉 칸에 공고 링크가 들어갑니다.

둘 다 `job_posts.detail` · `evidence` 에 그대로 남아 있으니, 수집기를 고친 뒤
다시 부으면 정리됩니다 (`exportRows` 는 그때까지 남겨둡니다).

`exportRows` · `sheetCounts` 와 API 표의 한 줄은 **5단계에서 지웁니다.**

---

## 1단계 — 기관 자료 (2026-09-18 완료)

```
표                      원본        DB        결과
hospitals              15332     15332     맞음
public_hospitals         231       231     맞음
dementia_safe_centers    256       256     맞음
dementia_centers         317       317     맞음
dev_rehab_orgs           305       305     맞음
ltc_facilities         30595     30595     맞음
mental_centers           737       737     맞음
welfare_centers          266       266     맞음
welfare_facilities     14710     14710     맞음
합계 62,749줄
```

붓는 도구는 `tools/load_orgs.js` 입니다. 표를 비우고 다시 붓고 건수를 대조합니다.
열쇠는 `.env.local`(저장소에 안 올라감)에서 읽습니다. 원본이 갱신되면 그냥 다시 돌리면 됩니다.

확인한 것 — 상급종합 47곳(실제와 일치) · 작업치료사 합계 11,071 · 물리치료사 54,880 ·
치매센터 남는 칸 6개가 `extra` jsonb 에 317줄 다 들어감.

`org_directory` 는 원본을 다시 부은 뒤 `refresh materialized view org_directory;` 를 돌려야 합니다.

---

## 로그인해야 보이게 (2026-09-25 완료)

오픈 전 필수 셋 중 첫째입니다. **막는 자리를 화면에서 DB 로 옮겼습니다.**
화면에서만 숨기면 요청을 직접 쏘는 쪽은 못 막습니다.

### 고치기 전에 실제로 쏴본 것 (로그인 없이, anon 열쇠만)

```
공고 목록 job_posts_pub?limit=1000     200   Content-Range 0-99/497
공고 검색 title=ilike.*치료*            200   100줄
병원정보 검색 org_search                200   20줄
기관 한 곳 org_public                   200   1줄
그 기관 공고 org_jobs                   200   8줄
근처 기관 org_nearby                    200   3줄
거르기 목록 org_facets                  200   전부
```

기관 표는 더 심했습니다 — 로그인한 회원 한 명이
`ltc_facilities` 30,595 · `hospitals` 15,332 · `welfare_facilities` 14,710 줄을
그대로 받아갈 수 있었습니다. anon 도 넷은 읽혔습니다.
「누구나 · true」라는 RLS 규칙이 붙어 있어서 사실상 아무것도 안 막고 있었습니다.

### 한 일

**공고** — 뷰를 통째로 읽는 길(`job_posts_pub`)을 닫고 함수 넷으로만 엽니다.

```
job_one(id)      한 건. 공유 링크가 살아야 해서 누구나. 본문(detail)은 회원만
job_list(...)    목록·검색. 회원만 · 한 번에 20건에서 끊음 (함수 안에 박아둠)
job_counts(...)  탭별 건수. 회원만
job_totals()     홈에 쓰는 개수. 줄이 안 나가므로 누구나
```

**기관** — 함수 일곱의 실행 권한을 걷었습니다.
`anon` 에서만 걷으면 안 됩니다 — 권한이 `PUBLIC` 에 붙어 있어서 anon 이 그걸
물려받습니다. 처음에 이걸 몰라 한 번 헛돌았습니다. `revoke ... from public` 이 답입니다.
`org_total` 만 남겨뒀습니다(홈의 「55,338곳」).

**기관 원자료 표·뷰 열** — 읽기 권한을 걷고 「누구나」 규칙을 지웠습니다.
RLS 는 켜둔 채 규칙이 없으면 전부 막힙니다.

```
hospitals · ltc_facilities · welfare_facilities · mental_centers
dementia_centers · public_hospitals · collectable_orgs
org_hospital_stat · org_coverage · org_targets
```

화면은 안 깨집니다 — `org_*` · `job_*` 함수 열둘이 모두 주인(postgres) 권한으로
돌아서 RLS 와 표 권한을 그냥 지나갑니다. 화면 코드가 이 표들을 직접 읽는 곳은
없습니다(확인함 · 0곳). 수집기는 `service_role` 이라 그대로 돕니다.

### 고친 뒤 다시 쏴본 것

로그인 없이 — 위의 일곱과 표·뷰 열, **전부 401 `42501`**.
`job_one` 만 200 이고 `detail` 은 `{}` 입니다(공유 링크는 살아 있어야 하므로).
홈은 그대로 돕니다 — `org_total` 55,338 · `job_totals` 497 · 커뮤니티 · 홈 문구.

회원으로 흉내 내어(`set local role authenticated`) 확인한 것 —

```
job_list()              20줄      job_list(p_page=>1)   20줄
org_search()            20줄      org_search(300 달라고) 20줄  ← least(…,20)
org_public 1 · org_hospital_by_name 1 · org_nearby 3 · org_facets 1
위의 표·뷰 열                     전부 막힘
```

### 확인 못 한 것

카카오·네이버 로그인은 **시작**만 확인했습니다(각자의 로그인 화면까지 도달,
PKCE 검증값이 쿠키에 들어간 것까지). 계정이 없어 로그인을 끝내지는 못했고,
그래서 **로그인한 상태의 화면은 브라우저로 확인하지 못했습니다.**
DB 쪽은 위처럼 흉내 내어 확인했습니다.

### 덧 — 마지막 쪽이 빈 쪽으로 넘어가던 것 (같은 날 고침)

쪽 넘기기를 확인하다 찾았습니다. 화면이 「받은 줄이 20이면 다음 쪽이 있다」로
판단하고 있었는데, 오늘 조건에 맞는 공고가 **정확히 320건(20 × 16)** 이라
마지막 쪽이 꽉 찼고, 「다음쪽」을 누르면 빈 쪽이 나왔습니다.

세어서 확인한 것 — 0~15쪽 각 20줄 · 16쪽 0줄 · 겹치는 줄 0 ·
원본(`job_posts_pub`)에서 같은 조건으로 직접 센 것도 320.

`job_list` 가 전체 건수(`total`)를 줄마다 같이 내주게 했습니다.
`org_search` 가 이미 그렇게 하고 있어서 같은 방식으로 맞췄습니다.
한 번에 20건 제한은 그대로입니다 — 숫자 한 칸이 늘어날 뿐입니다.

---

## 심평원 기관 자료를 API 로 (2026-09-25)

기관별 파일데이터가 공개돼 있지 않아 API 로 갑니다.
**`hospital_data.json` 은 지우지 않았습니다.** 저장소에 그대로 있고,
`hospitals` 표도 안 건드렸습니다. API 값은 옆에 따로 쌓고 화면이 골라 씁니다.

### 쓰는 API 둘

```
병원정보서비스        https://apis.data.go.kr/B551182/hospInfoServicev2/getHospBasisList
                     목록으로 옵니다 — 한 번에 10,000줄 · 전국 79,874곳이면 8번
의료기관별상세정보     https://apis.data.go.kr/B551182/MadmDtlInfoService2.8/<기능>
                     ykiho 하나씩만 됩니다. 목록 조회가 없습니다
```

주소 끝의 `2.8` 은 그대로 씁니다(`2_8`·`28` 아님). 점은 인코딩하지 않습니다.
키는 이미 URL 인코딩된 값이라 **다시 인코딩하면 안 됩니다** — 문자열로 이어 붙입니다.

**`_type=json` 은 무시되지 않습니다.** 붙이면 `application/json`, 안 붙이면
`application/xml` 로 옵니다. 실제로 둘 다 쏴서 Content-Type 을 확인했습니다.

### 새로 만든 표

```
hira_org      요양기호 79,874줄 · 이름 · 종별 · 지역 · 주소 · 전화 ·
              홈페이지(8,824곳) · 개설일자 · 좌표
hira_detail   허가병상 · 일반병상 · 물리치료실 · 설립구분 ·
              물리치료사 · 작업치료사 · 약사 · 사회복지사 · 재활의학과 전문의
              받은 시각을 셋으로 나눠 적습니다 (하나만 실패할 수 있어서)
hira_quota    하루 요청 횟수. 한도 10,000번
hira_todo     아직 안 받은 곳 (뷰). 차례가 박혀 있습니다 —
              목표 기관 → 종합·상급 → 병원·요양·정신 → 보건기관 → 의원 → 나머지
```

받는 도구는 `tools/hira.js` 입니다. **끊겨도 이어받습니다** —
`hira_detail` 에 줄이 있으면 받은 것으로 보고 건너뜁니다.

```
node tools/hira.js orgs              전국 목록 (8번)
node tools/hira.js detail targets     목표 기관
node tools/hira.js detail rest        나머지 (한도까지 받고 멈춤)
node tools/hira.js quota              오늘 몇 번 썼는지
```

### 꼭 알아둘 것

**기관별 파일데이터는 공개돼 있지 않습니다.** 공개된 파일은 시도별 합계뿐이라
(의료인력통계·의료시설및장비통계) 기관 단위 자료는 API 말고 길이 없습니다.

**기타인력수 직종은 다섯뿐입니다** — 약사 · 한약사 · 물리치료사 · 작업치료사 ·
사회복지사. **언어재활사 · 임상병리사 · 방사선사는 없습니다.**
보건직을 넓혀도 그 직군의 **기관 인원은 못 보여줍니다. 공고만 가능합니다.**

**병상은 허가병상(`permSbdCnt`)을 씁니다.** 자료판과 다른 것은 시점 차이입니다 —
2,836곳을 견줘 병상은 71% 일치(평균 차이 6.9), 작업치료사는 88% 일치
(평균 차이 0.30명)였고, 우리 쪽이 높은 곳도 낮은 곳도 있었습니다.

**기타인력수가 비어 있는 것은 오류가 아닙니다.** 의원급은 원래 없습니다.
`eqp_at`·`hst_at`·`sdr_at` 이 「받았다」는 표시이고, 값이 `null` 인 것과
아직 안 받은 것은 다릅니다.

### 설립구분 — 이름 어림을 버리면 안 됩니다

설립구분 9가지가 옵니다. 공립 · 의료법인 · 개인 · 학교법인 · 재단법인 ·
군병원 · 국립 · 특수법인 · 사회복지법인.

1,131곳을 견주니 1,049곳(93%)이 같았습니다. 그런데 **엇갈린 6곳이 중요합니다.**

```
경기도립노인전문동두천병원   설립구분 「의료법인」
광주시립정신병원            설립구분 「의료법인」
광주시립요양병원            설립구분 「의료법인」
정선군립병원                설립구분 「재단법인」
대구정신병원                설립구분 「재단법인」
대구광역시시지노인전문병원    설립구분 「재단법인」
```

**지자체가 세우고 법인에 맡긴 곳입니다.** 심평원 설립구분은 *운영* 주체를 적고,
이름은 *설립* 주체를 적습니다. 우리 목표는 「나라·지자체가 운영하는 기관」이라
이 여섯 곳은 대상이 맞습니다. **설립구분만 믿으면 빠집니다.**

→ 설립구분은 이름 어림의 **대체가 아니라 보탬**으로 씁니다.
  반대로 심평원만 공공이라 한 76곳(공립 종합병원 등)은 이미 종별로 대상이라
  들어와 있습니다.

### 이어받기 — 며칠 걸립니다 (2026-09-25 기준)

```
node tools/hira.js detail rest     하루 한 번. 한도까지 받고 멈춥니다
node tools/hira.js quota           오늘 몇 번 썼는지
```

`hira_detail` 에 줄이 있으면 받은 것으로 보고 건너뜁니다. 처음부터 다시 안 받습니다.

```
차례  무엇                              남은곳    요청수   며칠째에 끝남
 3   병원·요양병원·정신병원                 536    1,608        1
 4   한방병원                            612    1,836        1
 5   보건소·보건지소·보건의료원            1,062    3,186        1
 6   보건진료소                         1,899    5,697        2
 7   의원                              37,861  113,583       13
 9   치과병원·치과의원·한의원·조산원        34,574  103,722       23
```

**하루만 더 돌리면 병원급이 전부 채워집니다.** 의원까지 하려면 13일,
치과·한의원까지 하면 23일입니다. 치과·한의원은 기타인력수에 물리치료사·
작업치료사가 잡힐 일이 사실상 없어 맨 뒤로 뒀습니다.

### 종별코드 — 짐작하지 말 것

차례를 짤 때 `51` 을 한방병원으로 짐작했다가 틀렸습니다. **치과의원 19,426곳**입니다.
그대로 뒀으면 치과의원부터 받아 한도가 이틀 날아갔습니다.
`hira_org` 에서 실제로 세어 확인한 값은 이렇습니다.

```
01 상급종합 47       11 종합병원 336      21 병원 1,431
28 요양병원 1,276    29 정신병원 261      31 의원 37,866
41 치과병원 248      51 치과의원 19,426   61 조산원 16
71 보건소 249        72 보건지소 1,286    73 보건진료소 1,899
75 보건의료원 16     92 한방병원 624      93 한의원 14,893
```

### 목표 기관 1,677곳 중 1,053곳만 받은 이유

**이름 맞추기 문제가 아닙니다.**

```
갈래            목표줄   요양기호 찾음   못 찾음
보건기관          475       475          0
치매센터          317         2        315   ← 요양기관이 아님
종합병원          294       293          1
치매안심센터       256         0        256   ← 요양기관이 아님
공공보건의료       231       225          6
대학병원           57        57          0
상급종합           47        47          0
```

못 찾은 578곳 중 **571곳이 치매센터·치매안심센터**입니다. 요양기관이 아니라
심평원 목록에 애초에 없습니다. **치매 둘을 빼면 1,106곳 중 1,099곳(99.4%)이 맞습니다.**

나머지 7곳은 이렇습니다.

```
이름이 달라서 못 찾음
  양산부산대학교치과병원  → 심평원에는 「부산대학교치과병원」
  학교법인 춘해병원       → 심평원에는 「춘해보건대학교병원」
심평원 목록에 아예 없음
  강릉원주대학교치과병원 · 경상북도포항노인전문요양병원 ·
  성남시노인보건센터의원 · 완주군아름다운노인전문병원 · 서울지구병원
```

찾은 1,099줄이 요양기호로는 1,053곳입니다 — 목표 목록 안에 같은 기관이
두 줄로 들어 있는 곳이 있습니다.

### 지금 받은 2,277곳은 큰 병원에 치우쳐 있습니다

「몇 %에 치료사가 있다」를 이 표본으로 내면 안 됩니다.

```
종별          전국     받음    받은 비율
상급종합        47      47      100%
종합병원       336     336      100%
보건의료원      16      16      100%
보건소         249     228       92%
병원         1,431   1,237       86%
요양병원      1,276   1,011       79%
정신병원        261     184       71%
보건지소      1,286     245       19%
치과병원        248       7        3%
한방병원        624      12        2%
의원        37,866       5        0%
한의원      14,893       2        0%
```

비율을 내는 집계는 **차례 7(의원)까지 받은 뒤에** 해야 합니다.

### 종별코드표 — 한 곳에 모았습니다

표는 **`hira_cl_cd`** 하나입니다. 쓰는 자리도 **`hira_todo` 뷰 하나뿐**입니다
(DB 전체를 훑어 확인 · `tools/hira.js` 는 코드를 해석하지 않고 그대로 옮기기만 합니다).
받을 차례를 바꾸려면 그 표의 「차례」 칸만 고치면 됩니다.

값은 **활용가이드가 아니라 실제 응답의 `clCdNm` 으로 확인한 것**입니다
(`hira_org` 79,874줄에서 셈).

```
차례 2   01 상급종합 47 · 11 종합병원 336
차례 3   21 병원 1,431 · 28 요양병원 1,276 · 29 정신병원 261
차례 4   92 한방병원 624
차례 5   71 보건소 249 · 72 보건지소 1,286 · 75 보건의료원 16
차례 6   73 보건진료소 1,899
차례 7   31 의원 37,866
차례 9   41 치과병원 248 · 51 치과의원 19,426 · 61 조산원 16 · 93 한의원 14,893
```

**문서에서 잘려 있던 73·74 는 순서로 짐작하면 틀립니다.**
73 은 보건진료소가 맞지만 보건의료원은 **74 가 아니라 75** 이고,
**74 는 전국 79,874곳 어디에도 안 나옵니다.**

### 별칭 · 손 표시

이름이 다른 곳은 `org_alias` 에 손으로 넣습니다. 두 곳뿐이라 규칙을
만들지 않았습니다 — 규칙을 만들면 엉뚱한 곳이 붙습니다.

```
양산부산대학교치과병원  →  부산대학교치과병원   (치과병원 · 아직 상세 안 받음)
학교법인 춘해병원       →  춘해보건대학교병원   (종합병원 · 허가병상 201 · 물리치료사 5)
```

심평원 목록에 없는 5곳은 `org_note` 에 「확인 필요」로 적어 뒀습니다.
`org_targets` 의 맨 뒤 `note` 칸으로 보입니다. **문 닫았는지는 확인 안 했습니다.**

```
강릉원주대학교치과병원      치과병원이라 대상이 아닐 수 있음
경상북도포항노인전문요양병원
성남시노인보건센터의원
완주군아름다운노인전문병원
서울지구병원                군 병원이라 대상이 아닐 수 있음
```

### 병원급이 다 채워지면 셀 것 (그대로 붙여 쓰면 됩니다)

```sql
-- 종별로 치료사가 있는 기관 수 · 목표 안팎 · 인원
with 목표열쇠 as (
  select public.hira_name_key(name) as k from public.org_targets
  union select public.hira_name_key(hira_name) from public.org_alias
)
select o.cl_cd_nm as 종별,
       count(*) filter (where d.ykiho is not null)            as 받은곳,
       count(*) filter (where d.ot > 0)                       as 작업치료사있음,
       count(*) filter (where d.pt > 0)                       as 물리치료사있음,
       count(*) filter (where d.ot > 0 and o.name_key not in (select k from 목표열쇠)) as 그중_목표밖,
       sum(d.ot) filter (where o.name_key not in (select k from 목표열쇠)) as 목표밖_작업치료사,
       sum(d.pt) filter (where o.name_key not in (select k from 목표열쇠)) as 목표밖_물리치료사
from hira_org o left join hira_detail d on d.ykiho = o.ykiho
group by 1 order by 3 desc nulls last;
```

**주의** — 의원(37,866곳)을 받기 전에는 비율을 내면 안 됩니다.
정형외과 의원에 물리치료사가 많아서 병원급만으로는 그림이 안 나옵니다.
작업치료사는 병원급이 채워지면 대충 그림이 나옵니다.

---

## 이름 다듬기를 하나로 (2026-09-25)

넷이던 규칙을 **한 벌**로 모았습니다. 코드는 세 벌이지만 규칙은 한 벌입니다.

```
DB      public.org_name_key(s, p_drop_sido)
tools   tools/org-name-key.js
gas     gas/wage.js 의 orgNameKey_   (hospLoose_ 는 이걸 부르는 껍데기로 남김 · 부르는 곳 21군데)
```

**세 벌이 같은 값을 내는지 심평원 이름 3,000개로 견줬습니다 — 하나도 안 다릅니다.**
견주는 도구는 DB 의 `exec_keys(text[])` 입니다 (service_role 만 실행 가능).
규칙을 고칠 때마다 이걸로 세 벌을 다시 견주세요.

### 규칙

```
① 전각을 반각으로. 여는 괄호 앞에 빈칸 하나 (③이 걸리게)
② 법인 표기를 뗀다
③ 법인명을 뗀다 — 뒤에 병원·의원·요양원·의료원·센터·클리닉이 올 때만
④ 괄호 안은 남기고 기호만 뗀다
⑤ 공백·가운뎃점·붙임표·쉼표를 뗀다
⑥ 소문자로
※ 시도 접두어는 기본으로 안 뗀다. p_drop_sido 로 뗀 열쇠는 따로 뽑아
  「한 곳에만 붙을 때만」 쓴다
```

법인 표기 목록은 **짐작이 아니라 심평원 79,874곳의 yadmNm 에서 세어 뽑았습니다** —
의료법인 799 · (의) 59 · 재단법인 45 · 사회복지법인 29 · (사) 28 · (재) 23 ·
학교법인 22 · 사단법인 22 · (주) 8 · ㈜ 2 · 주식회사 1.
법인 표기가 든 곳 1,073곳 중 1,003곳이 「법인명 + 병원이름」 꼴입니다.

### 고친 결함 셋

```
넣은 값                            옛 org_key         새 org_name_key
의료법인 평화의료재단 군산성신병원    평화군산성신병원      군산성신병원
（의）백상의료재단 가족사랑요양병원    의백상가족사랑요양병원  가족사랑요양병원
(의)백상의료재단 가족사랑요양병원     백상가족사랑요양병원   가족사랑요양병원
의료법인자인의료재단(더자인병원)       자인               더자인병원
다함께돌봄(봉담)센터                다함께돌봄센터       다함께돌봄봉담센터
다함께돌봄(비봉)센터                다함께돌봄센터       다함께돌봄비봉센터
대구광역시 중구보건소               중구보건소          대구광역시중구보건소
```

### 앞뒤 비교

```
                          고치기 전   고친 뒤
공고 A 지금 붙음              475       475
공고 B 다듬으면 붙음            46        52   (B1 36 + B2 10 + B3 6 중 붙일 수 있는 것)
공고 C 못 붙음                197       189
기관 열쇠가 남과 겹치는 줄   13,606    12,729
기관 열쇠 최악 겹침             49곳      18곳
심평원 목표 매칭             99.37%   99.37%   ← 안 떨어졌습니다 (같은 7곳만 못 찾음)
```

바로 붙일 수 있는 것(한 곳에만 붙음)이 **34건 → 42건**으로 늘었습니다.

### 아직 안 한 것

**`org_key` 는 안 바꿨습니다.** 뷰 5개(`org_coverage` · `org_group_mv` ·
`org_hospital_stat` · `org_targets`)와 함수 3개(`org_hospital_by_name` ·
`org_jobs` · `org_search`)가 씁니다. 바꾸면 `org_group_mv` 의 **뭉치기가 달라져**
기관 화면이 통째로 흔들립니다. 따로 옮겨야 합니다.

화면이 공고↔기관을 붙이는 길(`org_public`)은 아직 **글자 그대로 같은 이름**만
봅니다 (`where g.name = p_name`). 새 규칙은 옆에 두고 재기만 했습니다.

---

## 화면을 새 열쇠로 (2026-09-25)

`org_public` 이 「글자 그대로 같은 이름」만 봐서, 규칙을 고쳐도 회원이 보는 것은
그대로였습니다. 새 열쇠(`org_name_key`)가 화면에 닿게 했습니다.

### 붙어 있던 것이 딴 데로 갈 수 없는 구조

**글자 그대로 같은 이름을 먼저 찾습니다.** 그게 있으면 그걸 줍니다.
없을 때만 열쇠로 찾고, 그때도 **한 곳에만 붙을 때만** 줍니다.
그래서 지금 붙어 있던 것은 구조상 옮겨갈 수 없습니다. 늘어나기만 합니다.

바꾸기 전 475건을 `org_match_before` 에 통째로 찍어 두고 앞뒤를 견줬습니다.

```
가  그대로 같은 기관        475건
나  안 붙던 게 새로 붙음      36건
다  딴 기관으로 옮겨감         0건   ← 없습니다
라  붙던 게 떨어짐            0건   ← 없습니다
둘 다 안 붙음              207건

붙는 공고 475 → 511건 (66.2% → 71.2%)
```

`org_key` 와 `org_group_mv` 는 안 건드렸습니다. 인덱스 하나만 더했는데
내용이 아니라 찾는 속도만 바꿉니다(없으면 55,338줄에 정규식을 그때그때 돌립니다).

### 나라일터 — 결과발표 3건 감춤

```
GJ303825  홍성군 보건소     서류전형 합격자 발표
GJ303897  의정부교도소      서류전형 합격자 발표   ← 교도소 건
GJ304170  홍성군 보건소     최종 합격자 공고
```

지우지 않고 `job_hides` 에 넣었습니다. 감춘 뒤 공개 뷰의 GJ 는 7건이고
결과발표 0 · 교도소 0 입니다.

**나머지 7건은 안 건드렸습니다.** 원문을 열어보니 둘은 진짜 자리였습니다.

```
경상남도 임기제공무원   「모집분야 : … 임상병리사, 물리치료사, 비서 등 6개 분야」
                      지방의료기술서기 2명이 그 자리로 보입니다
통영시 임기제공무원     「임용분야: 물리치료사」 · 시간선택제임기제마급 1명
```

**알리오의 「의료기술직」 과 같은 꼴입니다** — 공무원 직렬 이름 뒤에 치료사 자리가
숨어 있습니다. 제목만 보고 버리면 놓칩니다.

### 「장애」 — 지우지 말고 갈라냅니다

담긴 공고 전체에서 제목에 「장애」가 든 것은 **51건**이고, 그중 회원에게 보이는
것은 20건입니다. 대부분은 **진짜 치료사 공고**입니다 —
장애인종합복지관 · 지역장애인보건의료센터 · 발달장애인 주간활동센터 ·
장애전담 어린이집 · 장애인자립생활센터.

전형 이름으로 쓰인 것은 제목에서 이렇게 나왔습니다(짐작 아님).

```
장애인 제한경쟁 · 장애인제한경쟁 · 장애인 제한채용 · 장애인우선채용 ·
장애인 구분모집 · 장애인 전형 · 장애인 특별우대 · 장애인우대 · 사회형평적(장애인)
```

이미 담긴 것을 다시 거르는 함수를 만들었습니다 — `gojobs_recheck(낱말목록, 보기만)`.

```
select * from gojobs_recheck(array['장애인 제한경쟁','장애인우선채용'], true);   보기만
select * from gojobs_recheck(array['장애인 제한경쟁','장애인우선채용'], false);  실제로 감춤
```

안전장치 셋 — **직군이 붙은 공고는 안 건드립니다**(사람이 확인한 것입니다) ·
기관 이름에 장애·복지관·재활·발달·자립생활·특수교육이 있으면 안 건드립니다 ·
지우지 않고 감춥니다.

위 아홉 낱말로 보기만 돌리면 10건이 잡히고 진짜 치료사 공고는 한 건도 안 잡힙니다.
**아직 적용하지 않았습니다.**

---

## 오후 (2026-09-25)

### 근거 칸의 시각 — 수집기 잘못이 아니었습니다

인수인계에 적힌 「근거 칸에 판정한 시각이 들어간다」를 세어봤습니다.

```
분류근거 605건 중 15건이 시각 — 그 15건이 **전부 같은 값** 2026-09-09 17:58:00
보류사유 223건 중  7건이 시각 — 값은 두 개뿐 (09-08 00:53 · 09-08 10:13)
직군근거 590건 · 탭근거 718건 — 시각 0건
```

여러 줄이 한 시각을 공유하면 **한 번 돌린 일괄 작업**입니다. 지금 코드가 그 칸에
쓰는 곳(`gas/wage.js` 의 `r[28]`·`r[31]`)을 전부 봤는데 모두 사유 문장을 씁니다.
2026-09-08~09 에 남은 찌꺼기였습니다.

**보류함은 멀쩡했습니다** — 69건 전부 사유가 제대로 들어 있고, 시각이 남은 15건은
전부 `hold=false` 라 보류함에 안 나옵니다. 그래도 쓰레기통이 같은 자리를 쓸
예정이라 치웠습니다. 지우면서 `분류근거_치움`·`보류사유_치움` 에 원래 값을 남겼습니다.

### 기관째로 막힌 것 — 있습니다

`job_hides` 는 열쇠가 `job_id` 라 **한 건씩만** 막습니다. 기관 단위가 아닙니다.

그런데 **다른 자리에 기관 단위 막기가 있습니다** — `hosp_sites.off_why`.

```
켜짐                236곳
「구글 서버에서 못 엶」  49곳   ← 전부 목표 기관입니다
```

그 49곳에서 들어온 공고는 지금 **3건뿐**입니다.

```
꺼진 49곳    공고 3건     기관당 0.06건
켜진 236곳   공고 185건   기관당 0.78건     13배 차이
```

지방의료원(삼척·마산·목포·순천·서귀포·천안·홍성) · 근로복지공단 병원 7곳 ·
국립중앙의료원 · 고신대복음병원 · 원광대산본병원 등입니다.
**푸는 것은 아직 안 했습니다.**

### 기관 찾기를 네 단계로

앞 단계가 찾으면 거기서 끝나고, 뒤 단계는 모두 「한 곳에만 붙을 때만」 입니다.
그래서 **이미 붙어 있던 것은 옮겨갈 수 없습니다.**

```
1  글자 그대로 같은 이름            475건 (지금까지 붙던 것)
2  다듬은 열쇠                     36건
3  시도 접두어를 뗀 열쇠              6건
4  기관명 + 제목의 대괄호 안           9건
                                  ─────
                                  526건
```

**4단계를 「대괄호만」으로 하면 사고가 납니다.** 「[대구병원]」 을 그대로 찾으면
대구 북구 칠곡중앙대로의 **다른 사립 병원**에 붙습니다. 공고는 근로복지공단
대구병원(학정로)입니다. 9건 중 3건이 그렇게 엉뚱한 곳에 붙었습니다.
기관명을 앞에 붙여야 맞습니다.

```
가  그대로 475건 · 나 새로 붙음 51건 · 다 옮겨감 0건 · 라 떨어짐 0건

매칭률   전체 718건          526건  73.3%
        나라일터 뺀 666건     519건  77.9%   ← 진짜 매칭률
```

### 본문 {} — 깨진 게 아닙니다

```
출처  공고   detail 있음
WN   426    426 (100%)   ← 연봉·기관홈·접수방법뿐. 본문 아님
HS   164    164 (100%)   ← 기관홈 하나뿐
AL    61     14 ( 23%)   ← 지원자격·전형방법이 여기 있습니다
ND     8      6 ( 75%)
CE     7      5 ( 71%)
GJ    52      0 (  0%)
```

**진짜 본문(「지원자격」)이 담긴 공고는 718건 중 24건(3.3%)** 입니다.

나라일터가 0인 이유는 코드에 적혀 있습니다 — `gas/wage.js:8410`

```js
/* 나라일터는 상세 항목을 안 줍니다. 칸만 맞춰둡니다. [16]~[26] */
'', '', '', '', '', '', '', '', '', '', ''
```

**애초에 안 담게 되어 있습니다.** 다만 그 주석의 「상세 항목을 안 줍니다」는
다시 볼 값어치가 있습니다 — 나라일터 API 에 `getItem`·`getItemPosition`·
`getItemFile` 이 있는데 안 쓰고 있습니다 (인수인계 4번의 그 실수).

### 할 일 — 알리오 「의료기술직」

경남·통영처럼 제목에 직군이 없고 본문에만 있는 공고가 더 있습니다.
**본문을 담기 전에는 제대로 셀 수 없습니다.** 위 [본문] 이 끝나야 합니다.

---

## 오후 2부 (2026-09-25)

### 나라일터 상세 — **주석이 틀렸습니다. 본문을 줍니다**

`gas/wage.js:8410` 에 「나라일터는 상세 항목을 안 줍니다」 라고 적혀 있었는데
직접 두드려보니 **틀렸습니다.**

```
getItem?idx=<공고번호>      ← 이름표가 idx 입니다 (Empmn_sn 은 <body/> 빈 응답)
   contents 에 본문이 통째로 옵니다
   areacode · areaname · begindate · contents · enddate · idx ·
   insttcode · insttname · link01~03 · moddate · readnum · regdate ·
   title · type01 · type02

getItemPosition?idx=       채용직급과 인원
getItemFile?idx=           첨부 목록 (filename · filepath · filesize)
getClsfList                직급 코드 662개
getInsttList               기관 코드 453,568개
```

받아본 원문 (공고 303799 · `&#xD;` 를 푼 뒤):

```
[ 인천특수교육지원센터 기간제 교육공무직(치료사) 채용]
○ 모집분야 : 기간제 교육공무직(치료사)-작업치료사
○ 선발직급 : 기간제 교육공무직
○ 자격요건 : 작업치료사 면허증
```

공고 302625 — 「○ 모집분야 : … 임상병리사, 물리치료사, 비서 등 6개 분야」
공고 303244 — 「○ 임용분야: 물리치료사 / ○ 직무내용: 도서면 건강증진형 보건지소 물리치료실 운영」

**`&#xD;` 가 줄바꿈이고 `&amp;` 도 함께 풀어야 합니다** (`getItemFile` 의 filepath 에 들어 있습니다).

주석은 고쳤습니다. **담는 코드는 아직 안 만들었습니다** — 먼저 보고하기로 했습니다.

### 꺼진 병원 홈페이지 — 목록이 두 벌이고 서로 다릅니다

```
gas/wage.js 의 HOSP_SITES 에서 off 가 붙은 곳   49곳   ← 수집기가 실제로 쓰는 것
DB 의 hosp_sites.off_why 가 붙은 곳             49곳   ← 화면·관리자가 보는 사본
둘 다 있는 곳                                  14곳
```

**35곳씩 어긋나 있습니다.** DB 사본이 오래된 것입니다.
관리자 화면은 지금 틀린 목록을 보여주고 있습니다.

`HOSP_SITES` 에는 **꺼진 이유가 이미 적혀 있었습니다.**

```
HTTP 403                    19곳   ← 구글 IP·User-Agent 를 막았을 가능성
HTTP 0 Address unavailable  10곳
HTTP 0 Timeout               7곳
HTTP 400                     7곳   ← 전부 근로복지공단 (comwel.or.kr 공통)
HTTP 200                     3곳
HTTP 406                     2곳
HTTP 302                     1곳
```

제 컴퓨터에서 49곳을 두드리니 **45곳이 채용 게시판까지 멀쩡히 열립니다.**
그런데 이미 `off` 주석에 「컴퓨터에서는 열림」 이라고 적혀 있었습니다 —
**제가 아는 것을 되풀이한 셈입니다. 답이 아닙니다.**

꺼진 이유가 「구글 서버에서 못 엶」 이니 **구글에서 재야** 답이 나옵니다.
그 함수를 넣었습니다 — **`checkOffSites()`**. 시트는 안 바꾸고 재기만 합니다.
User-Agent 를 넣어 부르므로, 403 19곳이 그걸로 풀리는지 바로 보입니다.

### 워크넷 — 할 일 목록에만

워크넷이 주는 연봉·기관홈·접수방법은 본문이 아닙니다.
워크넷에도 상세 본문을 주는 기능이 있는지 나라일터와 같은 방식으로 두드려볼 것.
**오늘은 안 했습니다.**

### 받기 단어의 띄어쓰기 구멍

받기 단어가 하나도 없는 292건 중 **18건이 제목에 「치료사」를 갖고 있고,
그중 6건은 공백만 빼면 「물리치료사」·「작업치료사」에 걸립니다.**
「물리 치료사」 「작업 치료사」 처럼 띄어 쓴 것입니다.
→ 새 규칙에서 **비교 전에 공백을 빼야** 합니다.

---

## 꺼진 홈페이지를 경로로 가르기 (2026-09-25)

### 먼저 — 제가 센 49곳 중 2곳이 틀렸습니다

`HOSP_SITES` 를 정규식으로 훑으면서 **다음 줄의 `off` 를 끌어와** 제주대학교병원과
경기도의료원 이천병원을 「꺼짐」으로 잘못 넣었습니다. 둘은 처음부터 켜져 있었습니다.
한 줄씩 읽어 다시 세니 이렇습니다.

```
HOSP_SITES 항목    288곳
  꺼진 곳           47곳
  켜진 곳          241곳
```

※ 2026-09-25 밤 정정 — 위에 한때 **317곳**이라고 적었습니다. **틀렸습니다.**
`gas/wage.js` 6669~6958줄을 한 줄씩 세면 **288곳**입니다 (`name:` 이 288번,
한 줄에 두 번 나오는 줄 0개). 꺼진 곳 47은 맞습니다.
저장소 다른 곳의 317 은 **치매센터 표준데이터 317곳**이라 그대로 둡니다 —
같은 숫자라고 같은 것이 아닙니다.

### 「덮는다」를 목록으로 확인했습니다

이름만 보고 「공공이니 덮겠지」 하지 않고 두 API 의 **기관 목록을 통째로 받아** 대조했습니다.

```
알리오    recruitment/list 30,000건을 받아 기관 353곳   (공공기관 지정이 350곳 안팎이라 사실상 전부)
클린아이  openXmlEmployInfo 17개 시도 363건 → 기관 176곳
         ※ type=xml 이름표가 필요합니다. 빠뜨리면 NOTEXISTPARAMETER_ERROR(type)
         ※ 기관 이름 칸은 instNm 이 아니라 ENT_NAME 입니다
```

꺼진 47곳을 가르면 —

```
가 알리오가 덮음      8곳
나 클린아이가 덮음     1곳
다 API 없음 (사립)   38곳
```

**「가」 「나」 9곳은 이유를 사실대로 바꿔 적었습니다.** 「구글 서버에서 못 엶」이 아니라
「알리오로 들어옴 · 홈페이지 불필요」로, 근거(목록의 이름 · API 공고수 · 우리에게 들어온 건수 ·
마지막 날짜)를 함께 적었습니다. 다음 사람이 또 뚫으려 들면 안 되니까요.

```
근로복지공단 7곳   알리오 「근로복지공단」 2,009건 · 우리에게 8건 (마지막 2026-09-15)
                  [태백병원] 공무직(작업치료사) · [안산병원] 공무직(물리치료사) 가 그렇게 들어왔습니다
국립중앙의료원      알리오 1,071건 · 우리에게 1건 (2026-08-26)
삼척의료원         클린아이 10건 · 나라일터로도 1건 (2026-09-07)
```

### [4] 켜진 곳 중 API 가 덮는 29곳 — **끄면 안 됩니다**

중복이 생기는지 봤더니 **제목이 똑같은 중복은 0건**입니다. 같은 기관에서 두 경로가
**서로 다른 공고**를 가져옵니다.

```
경북대학교병원   알리오 7건 · 홈페이지 2건
  알리오는 칠곡경북대병원 임시직원 공고 위주
  홈페이지는 「2026년 8월 제4차 임시직원 모집(임상병리사, 주차관리)」 — 알리오에 없습니다
```

그래서 켜진 29곳은 그대로 둡니다. **「API 가 덮으니 끈다」는 꺼진 곳에만 맞는 말입니다.**

### [5] 경로 지도 — 「없음」이 1,191곳

`수집기_경로지도.txt` 에 목표 기관 1,535곳마다 경로를 적었습니다.

```
없음        1,191곳   ★ 우리가 놓치는 곳
홈페이지      223곳
나라일터       52곳
알리오        38곳
클린아이       19곳
워크넷         7곳
치매센터        5곳
```

「없음」을 갈래로 나누면 —

```
보건기관      472곳
치매센터      256곳
치매안심센터    206곳
공공보건의료    134곳
종합병원       94곳
대학병원       28곳
상급종합        1곳
```

**보건기관 472곳과 치매센터 462곳이 가장 큰 구멍입니다.**
종합병원 94곳·대학병원 28곳은 홈페이지를 붙이면 되는 곳입니다.

---

## 경로 표 [0][1] (2026-09-25)

### [0] 29일 비율 — 경로 유무로 갈라서

앞서 드린 비율은 **채용 빈도와 경로 유무가 섞여** 있어 못 쓰는 숫자였습니다.
가르고 나니 이렇습니다.

```
「지정 경로」 = 알리오·클린아이 기관 목록에 있거나 홈페이지가 켜져 있음
「훑기」     = 워크넷·나라일터. 기관 지정 없이 전국을 훑습니다

갈래          지정 경로 있는 곳         지정 경로 없는 곳
보건기관      0곳                       473곳 · 그중 1곳은 훑기로
종합병원      166곳 중 50곳 (30.1%)     98곳 · 그중 4곳은 훑기로
치매센터      0곳                       262곳 · 그중 6곳은 훑기로
치매안심센터  0곳                       250곳 · 그중 44곳은 훑기로
공공보건의료  44곳 중 36곳 (81.8%)      144곳 · 그중 10곳은 훑기로
대학병원      27곳 중 9곳 (33.3%)       28곳 (전부 0)
상급종합      42곳 중 19곳 (45.2%)      1곳 (전부 0)
──────────────────────────────────────────────────────────
합계          279곳 중 114곳 (40.9%)    1,256곳 · 그중 65곳(5.2%)만 훑기로
```

**지정 경로가 있으면 40.9%, 없으면 5.2% — 8배 차이입니다.**
이 한 줄이 경로를 붙여야 하는 이유입니다.

보건기관 473곳 · 치매안심센터 250곳 · 치매센터 262곳은 **지정 경로가 0곳**입니다.
치매안심센터 44곳은 나라일터가 잡아주고 있습니다.

### [1] org_coverage — 만든뷰로 · org_name_key 로

**바꾸기 전에 세어본 것**

```
옛 org_key       기관 1,315곳
새 org_name_key  기관 1,535곳      ← 220곳이 남과 뭉쳐 있었습니다
```

무엇이 뭉쳐 있었나 —

```
광역치매센터        7곳  경상북도·광주·부산·세종·전남·전북·충북
동구치매안심센터     7곳  광주·대구·대전·부산·울산·인천 …
중구치매안심센터     7곳
서구치매안심센터     6곳
중구보건소          5곳  대구·부산·서울·울산 …
서구보건소          4곳
사하구치매안심센터   4곳  본소·다대분소·신평분소  ← 괄호 안이 유일한 구분자였습니다
```

**쓰는 곳 grep** — 저장소 전체에서 코드가 부르는 곳은 **없습니다**(문서에만 나옵니다).
그래서 뷰를 만든뷰로 바꿔도 화면이 안 깨집니다.

**만든 것**

```
org_coverage (만든뷰)   기관 1,535곳
   key · 기관 · 갈래 · 종별 · 시도 · 시군구 · 손표시
   공고수 · 알리오 · 클린아이 · 워크넷 · 나라일터 · 홈페이지 · 치매센터경로
   마지막 · 수집처 · 며칠전 · 상태 · 갱신시각
refresh_org_coverage()  손으로 돌리는 갱신. 수집기도 이걸 부르면 됩니다
```

「조용함」 기준은 **비워뒀습니다**. 29일치로는 못 정합니다.
**2026-12-24 쯤(90일치) 다시 봅니다.**

### 아직 안 맞는 것 — 알리오가 본사 이름으로 옵니다

표에서 알리오 공고가 22건으로만 잡힙니다. 근로복지공단 8건이 「근로복지공단」
열쇠로 들어가서 목표 기관 「근로복지공단 태백병원」과 안 맞기 때문입니다.
화면(org_public)에서는 「기관명 + 제목 대괄호」로 풀었는데, 이 표에는 아직
그 규칙이 안 들어갔습니다. [4] 에서 API 기관 목록을 담을 때 같이 고칩니다.

---

## [A][C][D] (2026-09-25)

### [A] checkOffSites 가 안 죽게

```
checkOffSites()     죽은 6곳은 빼고 돕니다
checkDeadSites()    그 6곳만 따로. 한 달에 한 번쯤
clearOffSiteLog()   기록을 비우고 처음부터
```

**한 묶음(8곳) 끝날 때마다 「사이트점검」 시트에 적습니다.** 죽어도 남습니다.
다시 돌리면 오늘 이미 적힌 곳은 건너뜁니다.

```
기관 · 두드린날 · 차수(1·2) · 응답코드 · 걸린시간ms · 채용글수 ·
쪽제목 · 본문앞500자 · 전에꺼둔이유
```

뺀 6곳 — 2026-09-17 과 09-25 두 번 다 못 열었습니다.

```
충청남도 천안의료원 · 대진의료재단 분당제생병원 · 의료법인 오성의료재단 동군산병원
한마음병원(제주) · 제주특별자치도 서귀포의료원 · 전주고려병원
```

### [C] 규칙을 두 벌로 만들지 않았습니다

표가 **화면이 쓰는 `org_public` 을 그대로 부릅니다.**

```
job_org (만든뷰)   공고 719건 → 기관. org_public(기관명, null, 제목) 을 그대로 부릅니다
org_coverage       그 위에 세웁니다
```

공고 719건 중 **527건이 기관에 붙습니다** (못 붙음 192건).
알리오가 **22 → 31건**으로 늘었습니다. 근로복지공단 공고가 「[태백병원]」 처럼
제목 대괄호로 풀려 제 기관에 붙은 덕입니다.

### 고친 것 둘 — 둘 다 제가 만든 문제입니다

**1. `org_public` 이 두 벌이었습니다.** 3칸짜리(p_title)를 만들 때 2칸짜리를
안 지웠습니다. 둘 다 기본값이 있어 `org_public('이름')` 이 **「is not unique」**
오류를 냅니다. 화면은 세 칸을 다 넘겨서 살아 있었을 뿐입니다.
받는 값이 다르면 다른 함수입니다 — 덮어쓴 게 아니었습니다.

**2. 안 붙은 공고의 열쇠가 빈 문자열이었습니다.** `org_name_key(NULL)` 이
coalesce 때문에 `''` 을 돌려줘서, 기관을 못 찾은 192건이 「붙었다」로 세어졌습니다
(719건 전부 붙은 것처럼 보였습니다).

### [D] 1,535곳 기준 숫자 — 안 바뀝니다

[0] 에서 **이미 새 열쇠로** 냈습니다. 바뀐 것은 열쇠가 아니라 「경로」의 정의였고
(처음엔 「공고가 들어왔으니 경로가 있다」는 동어반복이었습니다), 그때
1,191곳 → 1,256곳으로 늘어난 것입니다.

갈라진 220곳이 어느 갈래였는지 확인했습니다.

```
치매센터      177덩어리 → 200곳
치매안심센터   174덩어리 → 196곳
보건기관        7덩어리 →  22곳
종합병원        1덩어리 →   2곳
```

**갈라진 곳이 거의 전부 치매·보건입니다.** 그 갈래는 지정 경로가 0곳이라,
경로 없는 곳이 늘어난 것이 맞습니다.

---

## [4] 조용한 실패 잡기 (2026-09-25)

오늘 하루에 네 가지를 **우연히** 발견했습니다 — clasp 로그인 만료(9/20부터
올리기가 조용히 실패) · 병원 홈페이지 47곳이 9/17에 통째로 꺼짐 · 나라일터가
본문을 주는데 「안 준다」고 적힌 주석 · 결과발표 공고가 회원 화면에 노출.
전부 누가 볼 때까지 몰랐습니다. 매일 스스로 재게 했습니다.

### 만든 것 — 새 표는 안 만들었습니다

```
route_health()              DB 함수. job_posts.source + collected_at 으로 셉니다
components/route-health.tsx  관리자 맨 위 빨간 줄
app/admin/layout.tsx         <header> 위에 한 줄 끼움
```

경로마다 「어제 들어온 수」와 「지난 14일 하루 평균」을 나란히 놓고,
**14일 평균이 0보다 큰 경로가 사흘 연속 0건이면** 빨간 줄입니다.
원래 조용한 경로(평균 0)는 안 울립니다 — 시끄러우면 아무도 안 봅니다.

다리(시트→DB)는 `source` 가 없어 `job_posts.updated_at` 의 가장 큰 값으로
갈음합니다. **한계** — `updated_at` 은 줄이 실제로 바뀔 때만 움직입니다.
더 정확히 하려면 다리가 `site_settings` 에 심장박동을 찍게 해야 합니다
(열쇠 `sync_heartbeat`). 새 표는 필요 없습니다.

### 돌려보니 — **빨간 줄 둘, 그리고 더 큰 것**

```
경로            어제  14일평균  연속0일  마지막       빨간줄
나라일터           0     0.71      4   2026-09-21   ★
알리오            0     0.50      3   2026-09-22   ★
홈페이지(구글)     0    10.14      2   2026-09-23
워크넷            0     7.64      2   2026-09-23
클린아이          0     0.00     15   2026-09-10    (원래 조용)
치매센터          0     0.00     18   2026-09-07    (원래 조용)
다리(시트→DB)     0        —      0   2026-09-25    돌고 있음
```

**더 큰 것 — 09-24·09-25 이틀 동안 어느 경로에서도 한 건도 안 들어왔습니다.**

```
수집일        공고   경로
2026-09-23     14   HS,WN
2026-09-22     30   AL,HS,WN
2026-09-21     24   GJ,HS,WN
2026-09-20     21   GJ,HS,WN
2026-09-18      2   HS
2026-09-17     16   GJ,HS,WN
2026-09-16    109   GJ,HS,WN
```

다리는 09-25 15:20 에 돌았으니 **수집기 쪽**입니다. 빨간 줄이 내일이면 넷이 됩니다.
**이게 이 기능을 만든 이유 그대로입니다** — 이틀 전부터 멈췄는데 오늘에야 봤습니다.

### [4] 고침 — 날짜를 한국 시각으로 (같은 날)

`route_health()` 가 `collected_at::date`(UTC 날짜)를 한국 날짜와 견주고 있었습니다.
다리가 시트의 「수집일」을 `날짜 + 'T00:00:00+09:00'` 으로 넣어서, 한국 09-24 자정이
UTC 로는 09-23 15:00 입니다. **하루씩 밀려서 「이틀 동안 0건」 이라고 잘못 말씀드렸습니다.**

```
             고치기 전 (UTC)        고친 뒤 (한국)
마지막 수집일   2026-09-23           2026-09-24
빨간 줄        나라일터 · 알리오      나라일터 · 클린아이
워크넷 어제     0건                  12건
홈페이지 어제    0건                   2건
```

**시각을 날짜로 바꿀 때는 어느 시간대인지 반드시 적습니다.**

### 시트 753줄 ↔ DB 719건 — 빠진 줄은 0입니다

```
시트 753줄 · 서로 다른 공고ID 719개 · 같은 ID 가 두 번 있는 것 34개
DB  719건
시트에 있는데 DB 에 없는 줄   0줄
```

34줄은 신규가 아니라 **2026-09-17 에 한 번 겹쳐 담긴 HS(병원 홈페이지) 줄**입니다
(68줄 = 34개 × 2). 오늘 일과 무관합니다.

### 다리 실패의 원인 — **아직 모릅니다**

```
09-20  10번 중 1번 실패
09-21   6번 중 1번 실패 (구글 화면)
09-22   7번 중 1번 실패 (구글 화면)
09-23   7번 전부 성공
09-24   7번 전부 실패 ★
09-25   2번 중 1번 실패 · 06:20 UTC 에 753줄을 담고 성공
```

실패할 때 JSONP 대신 이것이 왔습니다.

```
<!DOCTYPE html><html lang="en"><head><script nonce="...">
window['ppConfig'] = { productName: '26981ed…', deleteIsEnforced: false,
sealIsEnforced: false, heartbeatRate: 0.5, … }
```

**「clasp 만료가 원인」이라는 제 짐작은 틀렸습니다.** 다리는 `/exec`(배포판)을
부르는데 저는 `-deploy` 없이 올렸습니다. **배포판은 안 바뀌었습니다.**
시간이 맞아떨어진 것은 우연이었을 수 있습니다.

지금 배포판을 여덟 번 불러보니 **여덟 번 다 성공**했습니다(2.9~4.6초).
가끔 실패하는 것이라 지금은 재현이 안 됩니다.
로그의 `note` 가 300자에서 잘려 더 볼 수가 없습니다 — 그래서 심장박동에
응답 앞 300자를 같이 남기기로 했습니다.

---

## 회원인데 「가입하고 전부 보기」 가 뜨던 것 (2026-09-25)

### 원인 — 오류를 「비회원」으로 그렸습니다

서버에서 그리는 관문 넷 중 **셋이 `getUser()` 의 오류를 버리고** 있었습니다.

```js
const { data: who } = await sb.auth.getUser();   // ← error 를 안 받습니다
if (!who.user) → 가입 권유 카드
```

`getUser()` 는 **실패해도 `user` 가 `null`** 입니다. 잠깐 연결이 끊기거나 토큰
갱신이 실패하면 로그인한 회원에게 「가입하고 전부 보기」 가 떴습니다.
눌러 보면 「이미 로그인되어 있어요」 — 회원인데 비회원 화면을 본 것입니다.

**목록 화면(`app/jobs/page.tsx`)은 이 함정을 안 밟았습니다.**
거기서는 **DB 가 보낸 `42501`** 로 가립니다. 「권한 없음」은 물어봐서 받은
답이지, 못 물어본 것이 아닙니다.

### 고친 것 — `serverWho()` 하나로

`lib/supabase-server.ts` 에 넣었습니다 (`serverUserId` 옆). 이름은 grep 으로
먼저 확인했습니다 — `serverWho`·`whoServer`·`memberState` 전부 0이었습니다.

```
회원    로그인한 사람
비회원  물어봤고 아니라는 답을 받음 (AuthSessionMissingError)
모름    물어봤는데 답을 못 받음 → **가입 권유도 회원 자료도 안 그립니다**
```

화면 쪽 규칙과 같습니다 (`org-stat.tsx` · `job-veil.tsx` 가 `loading` 을 그렇게 씁니다).

### 아직 두 값인 곳 — 목록만 (안 고쳤습니다)

`useAuth()` 를 쓰면서 `loading` 을 안 받는 곳이 9개입니다. 그중 **가입 권유를
그리는 곳은 하나**입니다.

```
★ app/pay/page.tsx      「가입하고 급여를 등록하면」 · 단추가 /login 으로
                        확인 중에는 회원에게도 그렇게 보입니다

나머지 8곳은 단추를 막거나 표시만 바꿉니다 (가입 권유를 안 그립니다)
  app/admin/reset · app/login · components/admin-job-tools ·
  job-hospital · job-open-link · job-save · org-save · post-actions
```

---

## 카카오톡 인앱에서 회원이 비회원으로 보이는 것 (2026-09-25)

**서버 판정 코드는 맞았습니다.** 크롬에서는 정상이고 카톡 인앱에서만 납니다.

### 쿠키 설정 — 짐작이 아니라 코드에서 읽은 값

`@supabase/ssr` 0.12.7 의 기본값입니다
(`node_modules/@supabase/ssr/dist/main/utils/constants.js`).

```
path      /
sameSite  lax
secure    지정 없음          ← 붙지 않습니다
domain    지정 없음          ← 호스트 전용 쿠키가 됩니다
httpOnly  false              ← 브라우저 JS 가 읽고 씁니다
maxAge    400일
조각      3180바이트마다 .0 .1 로 쪼갬 (utils/chunker.js)
```

브라우저 쪽은 `document.cookie = serialize(...)` 한 줄로 심습니다
(`dist/main/cookies.js:94`). 서버 쪽은 미들웨어가 같은 값으로 다시 씁니다.

### 재는 자리를 세 군데로 나눴습니다

같은 「쿠키」라도 어디서 보느냐가 다릅니다. 셋을 갈라야 원인이 갈립니다.

```
브라우저가 가진 것   document.cookie          ← 심기긴 했나
문서 요청이 들고 온 것 화면을 그린 그 요청       ← 화면에 실려 갔나
서버가 따로 받은 것   /api/diag-who            ← 다른 요청에는 실리나
```

`/api/diag-who` 는 브라우저가 **따로** 쏘는 요청이라 화면을 그린 문서 요청과
다릅니다. 인앱이 문서 요청에만 쿠키를 안 붙이는 경우를 보려면 갈라야 합니다.
그래서 가입 권유 카드 자체가 `lib/diag-server.ts` 로 제 요청을 잽니다.

### 브라우저 판정 함수에 있던 같은 함정 (고쳤습니다)

`app/auth.tsx` 의 `verify()` 가 **오류가 나기만 하면 로그아웃**시켰습니다.

```js
if (error || !data.user) { await sb.auth.signOut({ scope: 'local' }); }
```

오류에는 두 가지가 섞여 있습니다 —

```
답을 받았다   서버가 「그런 사람 없다」고 답함     → 내보내는 게 맞습니다
답을 못 받았다 연결이 끊김                        → 모름입니다. 내보내면 안 됩니다
```

안 갈랐기 때문에 잠깐 끊긴 것만으로 로그인이 풀렸고, 쿠키가 지워져
**서버는 다음 새로고침에서 정직하게 「비회원」이라고 답하게** 됩니다.
서버 쪽을 아무리 고쳐도 소용이 없는 상태입니다.

지금은 `isAuthApiError(error)` 일 때만 — 서버가 실제로 답했을 때만 —
내보냅니다. 못 받았으면 가진 세션을 그대로 씁니다.

---

## 회원 여부 판정을 세 값으로 통일 (2026-09-25 저녁)

하루에 같은 버그를 네 번 고쳤습니다. 네 번 다 **두 값으로 판정한 자리**였습니다.
자세한 규칙은 `web/회원여부_읽는_법.md` 에 따로 적었습니다.

### [1] 뿌리 — `app/auth.tsx` 의 `loadMe`

```js
setMe((prof.data as Me) ?? null);      // ← 오류를 버립니다
setIsAdmin(admin.data === true);
```

`prof.error` 가 있는 것과 `maybeSingle()` 이 「그런 줄 없다」고 답한 것이
똑같이 `me = null` 이 됐습니다. profiles 를 한 번 못 읽으면 회원이
「가입 안 한 사람」이 되고, 흐림·댓글·상단바가 전부 그 말을 따라갔습니다.

이제 `meOk` 를 따로 두고, `useAuth()` 가 두 값을 더 내놓습니다.

```
who   '회원' | '비회원' | '모름'      serverWho 와 같은 이름·같은 뜻
설문  '마침' | '안마침' | '모름'      profiles 를 못 읽으면 모름
```

`Who` 타입은 `lib/supabase-server.ts` 한 곳에만 있고, 화면은 `import type` 으로
모양만 가져옵니다. 이름표를 두 벌 만들지 않습니다.

### [2] 「모름」은 빈 화면으로 통일

```
app/jobs/[id]/page.tsx     이미 null 이었습니다
app/orgs/page.tsx          회원 쪽으로 넘어가던 것 → null
components/org-detail.tsx  회원 쪽으로 넘어가던 것 → null
```

빈 자료를 채운 회원 화면은 「찾는 결과가 없습니다」처럼 보입니다.
없는 사실을 지어내는 꼴이라 빈 자리가 낫습니다.

### [3] 설문 벽과 세션 벽을 말로 구분

```
회원인데 설문 전  「3분 설문 마치고 전부 보기」 → /welcome
비회원            「가입하고 전부 보기」        → /login
모름              아무것도 안 그림
```

`app/gate.tsx` 에 `gateOf(who, 설문)` 순수 함수를 두고 `job-veil.tsx` 가
그것을 불러 씁니다. 전에는 `job-veil.tsx` 가 따로 판정해서, 문구를 고치면
한쪽만 고치게 돼 있었습니다.

### [4] 회원에게 틀리게 보이던 세 곳 + 튕기던 네 곳

```
app/login/page.tsx   loading 을 아예 안 받아, 로그인한 분이 열면 첫 그림에
                     카카오·네이버 단추가 통째로 떴습니다 → 「잠시만요…」
app/pay/page.tsx     「가입하고 급여를 등록하면」 + /login → loading 이면 단추도 안 그림
app/spec/page.tsx    글귀는 loading 을 보는데 바로 아래 단추는 안 봤습니다 → 같은 규칙으로

post-actions · job-closed · job-save · org-save
   확인 중에 누르면 /login 으로 튕겼습니다 → 잠깐 기다립니다
```

`job-save` · `org-save` 는 토스트를 안 쓰던 파일이라 새로 끌어오지 않고
누른 것을 삼키게 했습니다. 잠시 뒤 다시 누르면 됩니다.

### [5] `serverUserId` 삭제

부르는 곳이 한 곳도 없는데 `error` 를 버리는 옛 방식이었습니다.
같은 파일 주석이 그러지 말라고 적고 있는데 정작 자기가 그랬습니다.

### 검사

`web/tools/check-gate.mjs` — 여섯 칸을 한 번에 봅니다.

```
회원 · 마침    → 전부 보임
회원 · 안마침  → 설문 권유 → /welcome
회원 · 모름    → 자리 비움      ← 오늘의 그 버그
비회원 · 안마침 → 가입 권유 → /login
비회원 · 모름   → 자리 비움
모름 · 모름    → 자리 비움
```

`app/gate.tsx` 의 `gateOf` 원문도 같이 대조합니다 — 규칙이 두 벌이 되면
검사가 거짓말을 하기 때문입니다. 일부러 규칙을 망가뜨려 끝값 1 로
떨어지는 것까지 확인했습니다.

---

## 첫 가입 설문 손보기 (2026-09-25 밤)

### 고치기 전에 — 칸이 어디에 있었나

현직 9장 중 경력에 관한 것이 **두 화면에 흩어져** 있었습니다.

```
1장  약관·개인정보
2장  언제부터 일하셨나요   hired_year · current_hired_year · region   ← 경력
…
8장  경력                 이전 근무지 최대 5개 (career)              ← 경력
```

저장되는 곳은 이렇습니다.

| 화면 칸 | 표 · 열 | 빈 값 |
|---|---|---|
| 첫 입사연도 | `salary_records.hired_year` | **NOT NULL** |
| 지금 병원 입사연도 | `salary_records.current_hired_year` | 비어도 됨 (비면 첫 입사연도로 채움) |
| 지역 | `salary_records.region` | NOT NULL |
| 이전 근무지 | `student_specs.career` (jsonb) · `career_months` | 비어도 됨 |
| 연간 상여금 | `salary_records.bonus_yearly` | 비어도 됨 |

### [1] 경력을 한 화면으로

2장을 없애고 —

- 첫 입사연도 · 지금 병원 입사연도 → **경력 화면 맨 위로**
- 지역 → 「어떤 곳에서 일하세요」 (근무지 기준이라 거기가 맞습니다)

**저장되는 열은 하나도 안 바뀝니다.** 묻는 순서만 바뀝니다.
이미 등록한 회원 자료도 그대로입니다 — `/me` 에서 고칠 때 같은 값이
새 자리에 뜹니다. 현직은 9장 → 8장이 됐습니다.

### [2] 경력은 전부 쓰거나, 전부 안 쓰거나

**화면과 저장이 다른 규칙을 쓰고 있었습니다.**

```
화면   rows.some(완성)              한 줄만 다 채우면 통과
저장   filter(hospital && region)   개월수는 안 봄
```

반쯤 쓴 줄이 화면은 통과하고 **저장에서 조용히 버려졌습니다.**
적어놓은 것이 없어지는 것이라, 넘어가기 전에 막는 쪽으로 바꿨습니다.

규칙을 `lib/signup-fields.ts` 한 곳에 두고 화면과 저장이 같이 씁니다.

```
rowStarted   한 칸이라도 채웠나
rowFull      세 칸 다 채웠나
rowsReady    다음으로 넘어갈 수 있나   ← 화면
rowsToSave   저장할 줄                ← 저장
```

아무것도 안 쓰면 그냥 넘어갑니다. 막 졸업한 분에게 「경력 없음」을 누르게
하던 것은 걸림돌이었습니다. 반쯤 쓴 줄은 **그 칸 옆에** 빨간 테두리와
「지역을 골라 주세요」 같은 한 줄이 붙습니다.

### [3] 상여는 안 써도 넘어갑니다 — 다만 통계에 함정이 있습니다

칸은 0 과 빈 값을 **구분합니다.**

```
bonus_yearly integer NULL
CHECK (bonus_yearly IS NULL OR (bonus_yearly >= 0 AND bonus_yearly <= 9999))
```

그런데 **연 총소득은 구분하지 않습니다.**

```sql
annual_total  GENERATED ALWAYS AS (
  base_monthly*12 + COALESCE(extra_pay_monthly,0)*12
  + COALESCE(bonus_yearly, 0)            -- ← 「안 적음」을 0원으로 셉니다
  + ... )
```

`pay_stats` · `home_stats` · `rookie_salary` 가 전부 `annual_total` 로 중앙값을
냅니다. 그래서 상여를 안 적은 분은 **연봉이 상여만큼 낮게** 집계됩니다.
열을 나눌 필요는 없습니다 — 구분은 이미 됩니다. 고칠 자리는 `annual_total`
쪽이고, 어떻게 할지는 정해야 합니다.

지금 자료는 급여 3줄 · 상여 0원 2줄 · 상여 안 적음 0줄입니다.

#### 정해졌습니다 — 지금대로 둡니다 (2026-09-25 · 세중님)

상여를 안 적은 분도 **연봉 통계에 들어갑니다.** 0 으로 셉니다.

```
저장  비우면 NULL — 「0원」과 「안 적음」은 칸에서 그대로 갈라 남습니다
셀   annual_total 은 COALESCE(bonus_yearly, 0) — 0 으로 셉니다
화면  「제외」 같은 말을 안 적습니다
```

자료가 적을 때는 한 명이라도 더 세는 쪽이 낫다는 판단입니다.
**`annual_total` 은 고치지 않습니다.** DB 도 `lib/pay.ts` 의 `annualTotal` 도
이미 같은 규칙이라 손댑 것이 없었고, 둘이 어긋나지 않도록 양쪽에 주석을
남겼습니다. 설문 상여 칸은 「없으면 비워두세요」 로 바꿨습니다.

### 검사

`web/tools/check-career.mjs` — 아홉 칸. 제일 중요한 줄은 이것입니다.

```
화면이 통과시킨 줄은 저장에서 하나도 안 없어져야 합니다
```

`lib/signup-fields.ts` 원문과 `components/signup-survey.tsx` 가 그 규칙을
쓰는지도 같이 봅니다. 옛 규칙을 일부러 되살려 끝값 1 로 떨어지는 것까지
확인했습니다.

---

## 알리오·클린아이 기관 명부를 표로 (2026-09-25 밤)

### 왜 — 「길이 있나」와 「공고가 왔나」는 다릅니다

`org_coverage` 의 「수집처 없음」은 **치료사 공고가 한 번도 안 들어옴**입니다.
그 기관이 그 API 에 아예 안 올라온다는 뜻이 아닙니다.

실제로 **충남대학교병원은 알리오에 접수중 공고가 153건** 있습니다.
치료사 공고가 없었을 뿐입니다. 경로지도에서 「길 없음」으로 세면 틀립니다.

### 새 표 — `public.api_roster`

이름은 grep 으로 먼저 봤습니다 (`api_roster` · `roster` · `명부` 전부 0건).

```
source      AL 알리오 · CE 클린아이
org_name    명부에 적힌 이름 그대로
name_key    org_name_key() 로 다듬은 이름  ← 대조는 이걸로
posts       그 명부에서 본 공고 수 (직군 안 가림)
last_posted 본 것 중 가장 새 공고의 시작일
fetched_on  받은 날
```

RLS 켜고 anon·authenticated 권한은 걷었습니다.

### 받은 방법 — 짐작한 것 없음

**알리오는 POST 입니다.** GET 으로 두드렸더니 **HTTP 405** 가 왔습니다.
`gas/wage.js` 의 `callJobApi_` 를 읽고 맞췄습니다.

```
알리오    opendata.alio.go.kr/new/v1/recruit/list.do
          ongoingYn=Y 만 보면 507건 · 141곳
          거르기를 빼면 전체 114,174건
          새 공고부터 9,500건(95쪽)을 훑어 348곳
          15쪽 연속 새 기관이 없어서 멈췄습니다
클린아이  시도 17개 · 시도당 100건 한도 · 176곳
```

세중님이 오후에 받으신 수는 **353 · 176** 이었습니다.
**클린아이 176은 정확히 맞습니다.** 알리오는 348로 5곳 모자랍니다 —
전부 안 훑고 9,500건에서 멈춰서입니다. 더 훑으면 채워집니다.

**이 명부는 「적어도 이만큼」입니다.** 없다고 해서 그 API 에 안 올라오는
곳이라고 단정하면 안 됩니다.

### 국립대 3곳 — 답

```
충남대학교병원      알리오에 있음 (153건, 최근 2026-09-17)   → 길 있음
양산부산대학교병원  알리오에 그 이름으로 없음                → 부산대학교병원(73건)에 섞임
세종충남대학교병원  알리오에 없음                            → 진짜 구멍
```

이름 대조 실패가 아니었습니다. 별칭을 넣어도 양산·세종충남은 붙을 것이 없습니다.
양산부산대는 부산대학교병원 법인이라 알리오에 한 덩어리로 올라옵니다 —
모기관을 타고 들어온 공고에서 근무지로 갈라야 합니다.

### 명부와 대상 기관을 붙이면

대학병원·상급종합 104곳 중 **11곳이 알리오 명부에 있습니다.** 전부 국립대입니다.

```
강원대 · 제주대 · 분당서울대 · 경상국립대 · 경북대 · 충남대 ·
부산대 · 서울대 · 전남대 · 전북대 · 충북대
```

그중 **둘은 명부에 있는데 공고가 0건**입니다 — 충남대학교병원 · 전북대학교병원.
길은 있는데 안 들어오는 것이라, 주소를 새로 찾을 곳이 아닙니다.

**그래서 지정 경로 없는 상급종합은 14곳 → 13곳입니다** (충남대학교병원 빠짐).

---

## 알리오 명부 353 완성 · 경로지도에 「길」 칸 · 분원 갈라내기 (2026-09-25 밤)

### [1] 348 → 353. 세중님 수와 같아졌습니다

이어받기로 받았습니다 (`alio-state.json` 에 어디까지 봤는지 남김).

```
 1~250쪽 (25,000건)  → 353곳
251~500쪽 (50,000건) → 353곳  (한 곳도 안 늘었습니다)
```

25,000건에서 353곳에 닿고 그 뒤 25,000건 동안 그대로였습니다. 확정입니다.
늘어난 5곳은 **전부 병원이 아닙니다** — 한국공공조직은행 · 차세대수치예보
모델개발사업단 · 국립항공박물관 · 한국잡월드 · 한국방송광고진흥공사.
목표 기관이 아니고 최근 공고도 2024~2025년으로 오래됐습니다.

`api_roster` 에 `scanned` 칸을 더했습니다. `posts` 가 **9,500건을 훑었을 때의
수**인지 **50,000건일 때인지** 섞이면 안 되기 때문입니다.

### [2] 경로지도에 「길」 칸

`org_coverage` 에 `길` · `명부이름` 을 넣고 `상태` 를 세 값으로 바꿨습니다.

```
들어온 적 있음      108곳   공고가 실제로 왔습니다
길은 있는데 안 옴     9곳   명부에는 있는데 치료사 공고가 안 왔습니다
길도 모름         1,418곳
```

「길은 있는데 안 옴」 9곳 —

```
상급종합  충남대학교병원 · 전북대학교병원                 (알리오)
종합병원  삼척의료원 · 대구의료원 · 군산의료원 …          (클린아이)
```

**충남대학교병원이 「길 AL」 로 바뀌었습니다.** 확인했습니다.

※ 홈페이지 긁기(HOSP_SITES 288곳)는 아직 `gas/wage.js` 안이라 여기서 못 봅니다.
「길」이 비어 있어도 홈페이지 경로는 있을 수 있습니다.

### [3] 양산부산대 — 원문을 보고 칸을 골랐습니다

알리오 원문 21건을 받아 봤습니다.

```
srcUrl        pnuh.recruiter.co.kr  → 부산 (11건)
              pnuyh.recruiter.co.kr → 경남 (9건)
workRgnNmLst  호스트와 100% 일치
제목의 (양산)  9건 중 2건뿐          ← 못 믿습니다
```

대괄호(4번째 칸) 자리가 아니라 **근무지역(시도)** 이 답이었습니다.
그래서 `org_public` 에 **5번째 칸**을 더했습니다 — 분원 이름은 모기관 이름으로
끝나고, 시도가 맞고, 단일 일치일 때만.

**넣기 전에 쟀습니다.** 규칙만 떼어놓고 재면 경북대 9건·경상국립대 1건이
딴 데로 갑니다. 하지만 그 둘은 1번 칸에서 이미 붙어 5번째 칸까지 안 옵니다.
실제로 5번째 칸에 닿는 것은 부산대+경남 2건뿐이었습니다.

#### 그런데 5번째 칸이 한 번도 안 켜졌습니다

`job_org` 이 `org_public(이름, **NULL**, 제목)` 로 **시도를 안 넘기고** 있었습니다.
시도를 그냥 넘겨봤더니 200건 중 2건이 떨어졌습니다. 원인 둘 —

```
① 시도 표기가 두 벌
     공고    '충청북도' · '경기도' · '서울,' · '서울,부산,대구,…'
     기관표  '충북' · '경기' · '서울'
② 동명 기관이 여러 시도
     '서울요양원' 이 기관표에 7곳 (울산 · 인천 · 경기 · 서울 …)
```

다듬어도 ②는 안 풀려서, **두 번 봅니다** — 시도까지 주고 먼저, 안 붙으면
시도 없이. `coalesce` 로 앞이 붙으면 뒤를 안 부릅니다
(`union all + limit 1` 은 두 번 다 계산해서 새로고침이 시간 초과했습니다).

결과 — **떨어짐 0건.** 지금 안 붙은 185건을 옛 방식으로 다시 불러 확인했습니다.

```
527 → 534건 (+7)
  양산부산대학교병원 2건   ← 분원이 제자리로
  요양·재활 7건            ← 이름이 여러 곳에 겹쳐 시도 없이는 못 붙던 것
  같은 곳의 다른 표기 2건  ← key 는 같아 실질 변화 없음
기관 107 → 108곳
```

#### 이 칸으로 못 가리는 곳

분원이 모기관과 **같은 시도**면 못 가릅니다. 1번 칸이 먼저 이깁니다.

```
칠곡경북대학교병원     대구 (경북대학교병원도 대구)
창원경상국립대학교병원 경남 (경상국립대학교병원도 경남)
빛고을·화순전남대학교병원 전남 (둘이라 단일 일치도 아님)
```

이 셋은 `srcUrl` 호스트로 갈라야 합니다. 다른 칸이라 이번엔 안 했습니다.

#### 모기관 하나에 분원 여럿 — 전체 목록

```
경북대학교병원      ← 칠곡경북대학교병원          같은 시도
경상국립대학교병원  ← 창원경상국립대학교병원      같은 시도
부산대학교병원      ← 양산부산대학교병원          ✅ 갈라냄
서울대학교병원      ← 분당서울대학교병원          ✅ 갈릴 수 있음
서울대학교치과병원  ← 관악서울대학교치과병원
전남대학교병원      ← 빛고을 · 화순전남대학교병원  둘이라 못 가림
충남대학교병원      ← 세종충남대학교병원          ✅ 갈릴 수 있음
```

### [4] HOSP_SITES 288 정정 — 위쪽 878줄에 적었습니다

---

## HOSP_SITES 288곳을 경로지도에 (2026-09-25 밤)

### 먼저 — 이미 있던 표가 낡아 있었습니다

`public.hosp_sites` 가 **2026-09-20 판**으로 이미 있었습니다 (285줄).
gas 와 대조하니 —

```
이름이 다른 곳     0곳          ← 285개 이름이 전부 같습니다
gas 288 vs DB 285  겹침이 아니라 **한 병원에 게시판이 둘**
    창원경상국립대학교병원  일반직 · 전문직
    경상국립대학교병원      일반직 · 의사직
    영남대학교병원          정규직(agp=20) · 계약직
꺼짐 gas 47 vs DB 49
    의료법인안동병원 · 충청남도 홍성의료원 — gas 에서 다시 켠 것
```

표의 열쇠가 **이름 하나**여서 둘째 게시판이 통째로 빠져 있었습니다.
열쇠를 `(이름 + 주소)` 로 바꿨습니다. 주소가 없는 곳 17개(appsite·cmc
처럼 host+번호로 부르는 것)가 있어 빈 값으로 맞춥니다.

밀어넣은 뒤 — **288줄 · 다른 이름 285 · 꺼짐 47.** gas 와 같습니다.

※ `note`(사람이 읽는 긴 메모)는 안 담았습니다. 지도에 안 쓰고 gas 에
그대로 있습니다. **gas 가 주인이고 DB 는 사본입니다.**

### 「길」에 홈페이지를 더하니 지도가 달라졌습니다

```
             전        후
들어온 적 있음   108      108
길은 있는데 안 옴   9      158
길을 꺼 둠         –       41   ← 새 칸
길도 모름      1,418    1,228
```

**「길도 모름」이 190곳 줄었습니다.** 그동안 홈페이지 경로가 멀쩡히 있는
곳을 「길 없음」으로 세고 있었습니다 — 지도가 거짓말을 하고 있었습니다.

「꺼 둠」을 따로 뒀습니다. 주소는 알고 있고 지금 안 될 뿐이라, 없는 것과
한 칸에 섞으면 또 거짓말이 됩니다.

### 갈래별

```
갈래          전체   들어옴  길있음  꺼둠  길없음
상급종합        47     24     10     1     12
대학병원        57     11     18     1     27
종합병원       290     59    126    39     66
공공보건의료   156      5      4     0    147
보건기관       473      3      0     0    470
치매안심센터   228      0      0     0    228
치매센터       284      6      0     0    278
```

상급종합의 「길 없음」이 13 → **12곳**입니다 (홈페이지 경로가 있던 한 곳이
빠졌습니다). 대학병원 27곳은 그대로 — 27곳 중 명부에도 홈페이지에도
없습니다.

---

## 빨간 줄 기준을 경로마다 · 갈래 경로 붙이기 (2026-09-25 밤)

### [1] 「사흘 연속 0건」 하나로 모든 경로를 보고 있었습니다

나라일터·클린아이에 빨간 줄이 떴는데 현직자 확인 결과 **고장이 아니라
원래 공고가 드문 것**이었습니다. 시끄러우면 아무도 안 봅니다.

실제로 세어봤습니다 (틈 = 공고가 온 날 사이의 날수).

```
경로            틈 갯수  중앙값  가장 긴   틈들
홈페이지(HS)      12      1일     2일     1,1,2,1,1,1,1,1,2,1,1,1
워크넷(WN)        10      1일     3일     2,3,1,1,1,1,3,1,1,1
나라일터(GJ)       9      1일     5일     1,1,1,1,5,1,1,3,1
알리오(AL)        11      2일     7일     2,2,1,2,1,3,1,3,4,1,7
클린아이(CE)       4      3일     6일     2,4,6,1
치매센터(ND)       0       –       –      (9/8 딱 한 번)
```

**나라일터는 드문 경로가 아닙니다.** 중앙값 1일로 워크넷과 비슷합니다.
3일째인데 과거에 5일 틈이 있었으니 사흘 기준이 너무 예민했습니다.

**자료가 아직 30일치뿐입니다** — 90일 값이 30일 값과 똑같이 나옵니다.

새 기준 — 셋 다 맞아야 빨간 줄.

```
① 틈을 5개 이상 봤을 것            그 경로를 판단할 만큼 봤나
② 「지금까지 가장 긴 틈 × 2」 넘김
③ 그 값이 최소 7일
```

①이 중요합니다. 클린아이(틈 4개)·치매센터(0개)는 **판단을 미룹니다.**
모르는 것을 고장이라고 하면 안 됩니다.

지금 경로별 기준 — HS 7 · WN 7 · GJ 10 · AL 14 · CE 12(보류) · ND 보류.
**빨간 줄이 전부 꺼졌습니다.**

화면에 근거를 같이 적습니다 —
「평소 ○일에 한 번 · 가장 오래 쉰 것 ○일 · 기준 ○일」.

### 중앙치매센터를 긁고 있나 — **긁고 있습니다**

`gas/wage.js` 15336줄, `collectTick` 안에서 돕니다.

```
if (c.heavyHours.indexOf(hour) >= 0) {   // 9시 · 14시 · 18시, 평일
   … collectJobs2(잡플러스) …
   … collectNid(치매센터, 40초 예산) …
}
```

**3928줄의 주석이 낡았습니다** — 「collectNid 는 collectTick 에 없고
collectAllJobs 에만 있습니다」라고 적혀 있는데, 2026-09-10 에 들어갔습니다.

클린아이와 **같은 묶음**에서 돕니다. 둘 다 조용한 것이 그래서일 수도
있지만, 하루 3번 도는 설정이라 안 돈 것 같지는 않습니다.
확실히 하려면 Apps Script 실행 기록을 봐야 합니다 — 그건 세중님 쪽입니다.

### [2] 갈래 통째로 덮는 경로

보건기관·치매안심센터·치매센터는 기관마다 홈페이지를 긁는 곳이 아니라
**갈래 통째로 한 경로가 덮는** 곳입니다.

**「덮는다」를 확인했습니다** — 실제로 붙은 공고를 갈래별로 세었습니다.

```
GJ 나라일터     → 보건기관  공고 7건 · 3곳   ← 확인
ND 중앙치매센터 → 치매센터  공고 5건 · 5곳   ← 확인
WN 워크넷       → 치매센터  공고 2건 · 2곳
치매안심센터    → **어느 경로에서도 한 건도 안 옴** (228곳 전부 0)
```

치매안심센터는 보건소 부설이라 나라일터가 덮을 것으로 보이지만
**증거가 없습니다.** 그래서 `경로근거` 칸에 「확인」과 「아직」을 갈라
적습니다. 짐작을 확인처럼 적으면 다음 사람이 그걸 사실로 씁니다.

```
             전        후
들어온 적 있음    108      108
길은 있는데 안 옴  158      158
길을 꺼 둠         41       41
갈래 경로로 덮음    –      976   ← 새 칸
길도 모름       1,228      252
```

갈래별 「길도 모름」 — 상급종합 12 · 대학병원 27 · 종합병원 66 ·
공공보건의료 147. 보건기관·치매센터·치매안심센터는 0이 됐습니다.

**공공보건의료 147곳이 이제 가장 큰 구멍입니다.**

---

## 나라일터 확인 · 기준을 중앙값으로 · 공공보건의료 147곳의 정체 (2026-09-25 밤)

### [1] 나라일터는 「받았는데 걸러진」 것입니다

API 를 지금 직접 두드렸습니다.

```
HTTP 200 · 총건수 2,335건 (최근 30일) · 전부 받음
날짜별 전체 공고 — 9/23 130건 · 9/22 126건 · 9/21 113건 · 9/18 131건 …
제목에 치료사 말이 든 공고 — **30일에 1건**
   20260917 | 인천광역시교육청 | 인천특수교육지원센터 기간제 교육공무직(치료사)-작업치료사
```

**받은 게 0이 아닙니다.** 매일 100건 넘게 오는데 우리 직군이 드뭅니다.

항목 이름은 전부 소문자였습니다 — `title` · `insttname` · `regdate` · `idx`.
제가 처음에 `Empmn_nm` · `Instt_nm` 로 찾아 빈 값이 나왔습니다.
**원문을 찍어놓고 이름을 안 보고 썼습니다.** 찍은 다음에 봐야 합니다.

### 기준을 「중앙값 × 5, 최소 3일」 로

세중님이 짚으신 대로 **「가장 긴 틈 × 2」는 한 번 튄 값이 영영 기준**이
됩니다. 알리오가 한 번 7일 쉬면 그 뒤로 14일까지 아무 말도 안 합니다.

```
경로            중앙값  기준     지금      판정
홈페이지(구글)    1일    5일    1일째     들어오고 있음
워크넷            1일    5일    1일째     들어오고 있음
나라일터          1일    5일    3일째     들어오고 있음
알리오            2일   10일    2일째     들어오고 있음
클린아이          3일   15일   14일째     판단 미룸 (틈 4개)
치매센터           –      –    17일째     판단 미룸 (틈 0개)
```

### [2] 공공보건의료 147곳 — **147이 실은 40쯤입니다**

```
147곳
├ 모기관이 명부에 있음                10곳
│    근로복지공단 6 · 한국보훈복지의료공단 3 · 울진군의료원 1
│    → 대조를 **기관 이름으로만** 해서 빠졌습니다. 모기관 이름으로 봐야 합니다
└ 명부에 없음                        137곳
   ├ 노인·요양 (시립·도립·군립 노인요양병원)   75곳  ← 재활·요양, 중요도 낮음
   ├ 군 병원 (국군 9 · 해군 2 · 공군 1 · 서울지구)  14곳
   ├ 정신병원                                11곳
   ├ 그 밖 (서울시립 4 · 지방의료원 분원 등)   10곳
   ├ 치과·한방                                9곳
   ├ 재활병원 (권역재활 4 · 국립재활원 · 어린이재활 2 …)  8곳
   ├ 국립병원 (결핵·정신·소록도·법무 등)       8곳
   └ 적십자 (거창 · 통영)                      2곳
```

※ 제 분류에서 **「남해군립노인전문병원」이 군 병원으로 잘못 갔습니다** —
「군립」의 「군」이 걸렸습니다. 실제로는 군 병원 14곳 · 노인요양 75곳입니다.

**노인·요양 75곳과 치과·한방 9곳을 뒤로 미루면 63곳**,
군 병원 14곳을 따로 두면 **49곳**입니다.

치료사가 가장 많은 곳부터 보면 —

```
재활병원      8곳   권역재활병원 · 국립재활원 · 어린이재활병원  ← 가장 급합니다
국립병원      8곳
정신병원     11곳
서울시립      4곳   북부 · 서북 · 어린이 · 은평
적십자        2곳
= 33곳
```

### [3] 치매안심센터 228곳 — 그대로 둡니다

`경로근거` 를 「아직」으로 둔 채 다음 주 나라일터 공고에서
「치매안심센터」가 기관명으로 오는지 지켜봅니다.

---

## 모기관 별칭 · 갈래 밖의 자리 (2026-09-25 밤)

### [5] 모기관 별칭 — `org_alias` 에 용도 칸

지금까지 `org_alias` 는 **심평원 이름 대조** 하나에만 썼습니다
(`our_name` → `hira_name`). 쓸 곳이 하나 더 생겼습니다 — **분원 → 모기관**.

근로복지공단 태백병원의 공고는 알리오에 **「근로복지공단」 이름으로**
올라옵니다. 기관 이름으로만 명부를 대조해서 빠져 있었습니다.

뜻이 둘인데 한 칸에 섞으면 다음 사람이 잘못 읽습니다. `kind` 를 더했습니다.

```
kind='심평원'   our_name(우리 이름) → hira_name(심평원 이름)        2줄
kind='모기관'   our_name(분원)      → hira_name(명부의 모기관 이름) 13줄
```

칸 이름 `hira_name` 이 심평원 전용처럼 보이지만 **쓰는 함수·뷰가 0개**라
(저장소에서도 기록 문서 두 줄뿐) 이름을 바꾸는 것보다 뜻을 넓히는 쪽이
안전합니다. 주석에 적었습니다.

넣은 13곳 — 근로복지공단 6 · 한국보훈복지의료공단 4(광주보훈요양 포함) ·
대한적십자사 2 · 울진군의료원 1.

### [4] 갈래 밖의 자리 — `자리` 칸

공공보건의료 147곳을 열어보니 성격이 아주 다른 것이 섞여 있었습니다.
그대로 두면 「길 없음」 숫자가 뜻을 잃습니다. `자리` 칸에 적습니다 —
지도에서 눈에 보이고 코드에 안 박힙니다.

```
군 병원 · 나라일터(군무원)     15곳  국군 9 · 해군 2 · 공군 1 · 서울지구 · (종합병원 1)
뒤로 미룸 · 치료사 많음        82곳  시립·도립·군립 노인요양병원
치과·한방 · 뺌                 11곳  우리 직군과 멉니다
```

**노인요양 82곳에 「치료사 많음」을 같이 적었습니다.** 미루는 것이지
없는 셈 치는 게 아닙니다. 나중에 값을 매길 자리입니다.

### 본 대상만 보면

```
갈래          전체   길 없음
종합병원       289     65
공공보건의료    49     37    ← 147 에서 49 로
대학병원        57     27
상급종합        47     12
```

**공공보건의료 「길 없음」이 147 → 37곳입니다.** 「147이 실은 40쯤」이라는
세중님 말씀이 맞았습니다.

---

## 나라일터 본문 소급 · 알리오·워크넷 상세 확인 (2026-09-25 밤)

### 숫자 — 최근 30일 2,335건 전부

```
제목에 걸린 것          3건
본문에만 걸린 것       24건
못 연 것                0건
```

말별 (본문 / 제목)

```
물리치료   본문   5건 · 제목  0건
작업치료   본문   3건 · 제목  1건
언어재활   본문   0건 · 제목  0건
치료사     본문   5건 · 제목  1건
재활       본문  20건 · 제목  2건
```

**「재활」을 빼고 보면 본문 6건 · 제목 2건입니다.**
「재활」만 걸린 21건은 전부 **장애인 제한경쟁 채용**의 「직업재활법」·
「정형재활기기과」 같은 말입니다 — 우리 직군이 아닙니다.

### 본문에 무엇이 있나

```
「모집분야·직종」 줄이 있는 비율   20/27 = 74%
「자격요건」 줄이 있는 비율        18/27 = 67%
```

### 진짜 자리 6건 — 걸린 줄 원문

```
1. 인천광역시교육청 학교교육국 (09-17)   ← 제목에도 있음
   ○ 모집분야 : 기간제 교육공무직(치료사)-작업치료사
   ○ 자격요건 : 작업치료사 면허증

2. 재단법인자동차손해배상진흥원 (09-15)   ← 제목에 없음
   - 간호학, 보건학, 보건행정학, 물리치료학, 작업치료학, 방사선학 … 보건의료 관련
   (의료심사 실무 자리 — 치료사 면허를 쓰는 자리는 아닙니다)

3. 경상남도 통영시 (09-10)                ← 제목에 없음
   ○ 임용분야: 물리치료사
   ○ 직무내용: 도서면(사량면) 건강증진형 보건지소 물리치료실 운영

4. 경상남도 (09-02)                       ← 제목에 없음
   ○ 모집분야 : … 임상병리사, 물리치료사, 비서 등 6개 분야

5. 충청남도 홍성군 보건소 (08-28)          ← 제목에 없음 · 합격자발표
   1. 채용분야: 2026년 의료취약지 원격협진 사업(물리치료사 또는 작업치료사)

6. 경상남도 통영시 (08-27)                ← 제목에 없음
   ○ 임용예정분야: 아동보호전담요원, 물리치료사(도서면(사량면)), 정신건강전문요원
```

**제목만 보면 1건, 본문까지 보면 6건.** 그중 합격자발표 1건·사무직 1건을
빼면 **진짜 새로 얻는 자리는 3건**입니다 (통영 2 · 경남 1).
전부 **지자체 보건소 물리치료사**입니다 — 우리 목표의 한복판입니다.

### 잘못 잡힌 것 10건 — 전부 같은 꼴

```
공무원연금공단        「장애인고용촉진 및 직업재활법」
식약처 의료기기심사부  「정형재활기기과」
한국장애인고용공단     「직업재활」
서울보호관찰소         장애인 제한경쟁
서경초등학교 시설미화원 장애인 제한경쟁
국가아동권리보장원 · 한국출판문화산업진흥원 · 산업연구원 · 인천 제물포구 …
```

**「재활」 하나만으로는 못 가립니다.** 「직업재활」·「재활법」·「재활기기」를
빼거나, 「물리치료·작업치료·언어재활·치료사」만 쓰는 쪽이 맞습니다.
**규칙은 아직 안 만들었습니다** — 세중님이 순서를 정하신 뒤에.

### 알리오 — 본문 칸이 **없습니다**

```
opendata 목록(지금 쓰는 것)  본문스러운 칸 넷
    aplyQlfcCn          73자   지원자격
    prefCondCn          62자   우대조건
    scrnprcdrMthdExpln 171자   전형방법 설명
    disqlfcRsn       1,246자   결격사유 (쓸모없음 — 어느 공고나 같은 법 조문)
opendata detail.do           NODATA_ERROR — 안 됩니다
data.go.kr /detail           HTTP 200 · 3,928자 — 되지만 항목이 목록과 거의 같음
```

**알리오에는 공고 본문이 없습니다.** 직군 단서는 `aplyQlfcCn`(지원자격)과,
앞서 확인한 `steps`(채용전형단계)입니다.

### 워크넷 — 이미 직종 코드로 정확히 받고 있습니다

상세(`callTp=D`)에 **항목이 61개** 있습니다.

```
jobsNm       물리치료사(306501)   ← 직종이 코드까지 찍혀 옵니다
jobCont      141자  업무내용
wantedDtl  2,096자  전체 묶음
certificate · major · pfCond · etcPfCond · submitDoc · selMthd …
```

수집기가 이미 `occupation=306500|306501|306502` 로 **직종을 찍어서**
받고 있습니다. 워크넷은 본문을 볼 이유가 없습니다 — 이미 정확합니다.

### 결론

**본문이 값어치가 있는 곳은 나라일터 하나입니다.**
알리오는 본문이 없고, 워크넷은 이미 직종으로 정확합니다.

---

## 나라일터 본문 담기는 **이미 되어 있었습니다** · 대신 진짜 구멍 (2026-09-25 밤)

### 제 앞 보고가 틀렸습니다

「제목까지만 보면 1건, 본문까지 보면 6건 — 지금 방식으로는 한 건도 못
잡는 자리」라고 말씀드렸습니다. **틀렸습니다.**

`gas/wage.js` 8583줄 — `collectJobs3` 가 **이미 `job3Detail_` 로 상세를
열고** 모든 값을 합쳐 `matchJob_` 을 돌립니다. `contents` 도 거기 들어갑니다.

```js
const d = job3Detail_(x.r.idx);          // 8583줄
const hay = Object.keys(d).map(k => String(d[k] || '')).join(' ');
x.job = matchJob_(hay);
```

DB 를 보니 **그 3건이 전부 담겨 있었습니다.**

```
GJ302625 경상남도         물리치료사   09-08 담음
GJ303244 통영시           물리치료사   09-10 담음
GJ303656 자동차손배진흥원  공통        09-16 담음
GJ303799 인천교육청       작업치료사   09-17 담음
```

빠진 둘은 빠진 게 맞습니다 — 홍성군 보건소는 **합격자발표**,
통영 08-27 은 09-10 재공고의 원공고입니다.

**API 만 보고 gas 코드를 안 읽어서 틀렸습니다.** 있는 것을 먼저 찾아야
했습니다.

### 「재활」도 이미 안 걸립니다

`JOB_WORDS = ['작업치료', '물리치료', '재활치료']` — **「재활」 단독은
처음부터 없었습니다.** 제 스캔이 「재활」을 넣어서 21건이 나온 것이지,
수집기 기준이 아니었습니다.

### 대신 진짜 구멍 — 직군을 못 가린 25건이 회원에게 보였습니다

「재활」만 걸린 20건 중 4건이 DB 에 있었고 **전부 `job_group` 이
비어 있었습니다.** 넓혀 세어보니 —

```
직군 없음 211건 · 보류함 67 · 숨김 119 · **회원에게 보임 25**
```

무엇이 보이고 있었나 —

```
임상병리사 5 · 요양보호사 7 · 간호(조무)사 2 · 사회복지사 2 ·
당직전담사 · 행정9급 전입 · 교도소 공무직 · 자립지원 전담인력 ·
정신건강복지센터 합격자발표 2 · 재활활동보조원 …
```

**「당직전담사」·「행정9급」·「요양보호사」가 치료사 공고 목록에 떴습니다.**

`job_list()` 는 `hidden`·`hold` 로만 거르고 `job_group` 은 안 봅니다
(`p_job is null or p_job = '' or j.job_group = p_job`).
그래서 직군이 비어 있으면 「전체」 탭에 그대로 나옵니다.

### 고친 것 — CLAUDE.md 규칙대로 보류함으로

> 못 가린 공고는 버리지 말고 보류함으로. 관리자가 확인 후 올림

새로 정한 것이 아니라 **이미 적혀 있던 규칙**입니다. 그 규칙이 안
지켜지고 있었습니다.

```
update job_posts set hold = true
 where job_group is null and not hold and not coalesce(hidden,false);
→ 25건
직군 없음 · 회원에게 보임  25 → 0
```

되돌리려면 그 25개 id 의 `hold` 를 false 로 되돌리면 됩니다
(id 는 이 기록과 커밋 메시지에 남아 있습니다).

### 남은 일 — 수집기 쪽

지금은 **이미 담긴 것만** 고쳤습니다. 수집기가 앞으로도 직군 없는 공고를
`hold=false` 로 담으면 같은 일이 되풀이됩니다. 어느 자리에서 그러는지는
아직 안 찾았습니다.

---

## 수집기에 「직군 못 가리면 보류함」 (2026-09-25 밤 · v315)

### 문지기가 이미 있었습니다

새로 만들지 않았습니다. `holdEvents_` 가 **모든 수집기가 시트에 쓰기
직전에 부르는 자리**로 이미 있었습니다 (행사·박람회를 거르던 곳).
거기에 한 갈래를 더했습니다. **규칙은 한 벌입니다.**

```js
function holdEvents_(rows) {
  rows.forEach(function (r) {
    while (r.length < JW) r.push('');
    if (String(r[27] || '') === 'Y') return;      // 이미 보류함이면 그대로
    if (isEvent_(r[3])) { r[27]='Y'; r[28]='행사·박람회로 보여 보류'; return; }
    if (!String(r[12] || '').trim()) {            // 직군(12열)이 비면
      r[27]='Y'; r[28]='직군을 못 가렸습니다';
    }
  });
}
```

### 한 곳이 문지기를 안 거치고 있었습니다

`setValues(rowsToAdd)` 가 다섯 곳인데 **`watchRegionJobs` 만** 안 거쳤습니다.
거기로 들어온 공고는 직군이 비어도 바로 회원에게 갔습니다. 넣었습니다.
지금은 다섯 곳 모두 거칩니다 (`grep -c holdEvents_(rowsToAdd)` = 5).

### 보류 사유 — DB 에 이미 오고 있었습니다

시트의 「보류사유」(28열)가 `tools/copy_jobs.js` 의 `EVIDENCE_COLS` 를 타고
`job_posts.evidence` 에 이미 담겨 있었습니다. 확인한 값 —

```
규칙이 막음 · 제목에 「요양보호사」 → 버림          7건
상세로 판정 · 사람이 볼 것 · ② jobCont · 불확실     6건
병원 게시판 · 예산이 모자라 상세를 못 엶 · 원문 확인 5건
규칙이 막음 · 제목에 「임상병리」 → 버림            4건
…
```

**화면이 안 보여주고 있었을 뿐입니다.** 두 곳에 한 줄씩 넣었습니다.

```
app/admin/jobs/page.tsx      보류함 목록에서 바로
components/admin-job-tools.tsx  공고 상세의 관리자 칸
```

목록이 `evidence` 를 안 받고 있어서 `ADMIN_LIST_COLS` 에 더했습니다.

### [2] 세 갈래 규칙 — 자리만 잡았습니다

`holdEvents_` 주석에 적어뒀습니다. **아직 안 만들었습니다.**

```
받기 단어 있음   → 담기 (회원에게 보임)
버림 단어만 있음 → 쓰레기통 (아예 안 담음)
둘 다 없음       → 보류함        ← 지금은 여기까지만 동작합니다
```

버림 단어는 빈도표를 보고 정하신 뒤 **이 함수 안에** 한 갈래를 더합니다.
다른 곳에 만들지 않습니다.

---

## 받기 단어 「치료사」 · 워크넷 잘린 제목 (2026-09-25 밤 · v316)

### [1] 「치료사」를 보태면 **18건**

받기 단어가 제목에 하나도 없는 **292건**(파일은 292/718, 세중님이 말씀하신
293과 한 건 차이) 중 「치료사」로 새로 걸리는 것이 18건입니다.

```
띄어쓴 꼴    물리 치료사 3 · 작업 치료사 3 · 전문 치료사 1
붙은 꼴      물리(작업)치료사 · 재활(운동)치료사 · 언어치료사 …
```

**조심할 것 — 18건 중 2건이 남의 직군입니다.**

```
국립교통재활병원 계약직 언어치료사 채용공고
<여수강남요양병원-언어치료사 계약직>구인
```

`notOurs_` 에 「언어치료」가 이미 있어 걸러지지만, **받기 단어는 버림보다
먼저 보므로 순서를 확인해야 합니다.** 아직 안 넣었습니다 — 세 갈래 규칙을
한꺼번에 넣을 때 같이 봅니다.

### [2] 워크넷 목록 제목은 **30자에서 잘립니다**

```
100건을 받아보니 40건이 정확히 30자 · 끝이 「…」
시트의 워크넷 공고 426건 중 165건(39%)이 잘려 있습니다
```

상세(`callTp=D`)의 `wantedTitle` 에 온전한 제목이 있습니다.

```
목록 (30자)  [2026년 보령시 취창업 박람회] 주간보호 물리...
상세 (33자)  [2026년 보령시 취창업 박람회] 주간보호 물리치료사 구인

목록 (30자)  [달서새일센터 채용대행]가봄요양원에서 함께 일할 ...
상세 (40자)  [달서새일센터 채용대행]가봄요양원에서 함께 일할 물리치료사님을 구합니다.
```

**잘린 자리에 직군 말이 있으면 제목만으로는 못 잡습니다.**

#### 고친 것

```
wnCut_(t)         30자 이상이거나 「…」 으로 끝나면 잘린 것으로 봅니다
wnFullTitle_(f,t) 상세의 wantedTitle 이 **더 길 때만** 바꿉니다
                  (상세가 빈 채로 와도 제목을 잃지 않게)
```

수집기 두 갈래에 넣었습니다.

```
직종코드로 바로 담는 길   상세를 안 열던 길입니다. 잘렸을 때만 한 번 엽니다
상세를 이미 여는 길       판정(wnJudge_)보다 **먼저** 제목을 바꿉니다 —
                          잘린 자리의 직군 말을 판정이 볼 수 있게
```

#### 소급 — `fixWnTitles()`

밑줄이 없어 실행 목록에 뜹니다. 6분 제한이 있어 **어디까지 했는지 남기고
이어갑니다.** 다시 실행하면 그다음부터 봅니다.
`fixWnTitlesReset()` 으로 처음부터 다시.

끝나면 이렇게 알려줍니다 —

```
잘려 있던 것 N건 · 고친 것 N건 · 못 연 것 N건
잘린 자리에 직군 말이 있던 것 N건   ← 이게 값어치입니다
```

**아직 안 돌렸습니다.** 세중님이 Apps Script 에서 실행하셔야 합니다.

### [3][4][5] — 못 했습니다

**버림 단어 목록이 이 대화에 없습니다.** 「위 목록 그대로」라고 하셨는데
제 쪽에는 안 왔습니다. 저장소의 `버림단어_낱말빈도표.txt` 에는 후보 낱말만
있고, 세중님이 고르신 목록이 아닙니다.

「안전장치 2」와 「쓰레기통 표 · 잘못 버림 단추」 설계도 제 쪽에 없습니다.

**단어를 지어내지 않았습니다.** 목록을 주시면 [4] 시험부터 바로 하겠습니다.

---

## 세 갈래 규칙 — 시험 결과 (2026-09-25 밤 · **네 갈래로 정정됨 · 아래 참고**)

### 규칙은 `tools/sort-rule.mjs` 한 벌

이름은 grep 으로 먼저 봤습니다 (`sortJob` · `받기단어` · `버림단어` ·
`여럿나열` 전부 0건).

```
받기 26개 (기존 24 + 치료사 + 특수체육)
받기예외 6개 (언어·심리·미술·음악·놀이·방사선 치료사)
버림 59개 (직군 32 · 자리 7 · 부서 12 · 꼴 8)
   ※ 특수체육지도자는 버림에서 뺐습니다 (세중님 정정)
```

순서 —

```
① 받기 예외를 제목에서 지웁니다
     「물리치료사, 언어치료사」 → 「물리치료사, 」 → 받기로 남습니다
② 지운 제목에 받기 단어 → 받기        (안전장치 가)
③ 괄호 안 쉼표 둘 이상  → 보류함      (안전장치 나)
④ **원래 제목**에 버림 단어 → 쓰레기통
     지운 제목으로 보면 언어치료사를 못 버립니다
⑤ 둘 다 없음 → 보류함
```

### 292건 시험 결과

```
받기      19건
쓰레기통  101건
보류함    172건
```

### ✗ 안전 검사가 멈췄습니다 — 쓰레기통에 「치료」·「재활」이 4건

지시대로 **고치지 않고 보고합니다.**

```
1. 제26-10회 직원채용 공고(재활치료업무보조,간호업무보조_장애인 제한채용)
     ← 업무보조 (충청북도 청주의료원)
     「재활치료실 업무보조」 자리입니다. 치료사 면허 자리는 아닌 듯합니다
2. 국립교통재활병원 계약직 언어치료사 채용공고        ← 언어치료사
     「재활」은 병원 이름입니다. 의도대로 버려진 것입니다
3. ...랑과 치료로 가족을 돌보는 주간보호센터 간호조무사  ← 간호조무사
     「치료」는 수식어입니다. 의도대로입니다
4. <여수강남요양병원-언어치료사 계약직>구인            ← 언어치료사
     의도대로입니다
```

**4건 중 3건은 의도한 것**이고, 1번만 판단이 필요합니다.

### 꼭 맞아야 하는 칸 — `tools/check-sort.mjs` 16칸 전부 통과

```
○ 홍보팀 및 물리치료사 모집              → 받기      (버림이 같이 있어도 받기)
○ 물리치료사, 언어치료사 모집             → 받기      (예외를 빼도 물리치료사가 남음)
○ 물리 치료사님을 모십니다                → 받기      (공백 빼고 견줌)
○ 국립교통재활병원 계약직 언어치료사 채용공고 → 쓰레기통
○ 일반직(행정, 보건, 간호조무, 원무직)     → 보류함    (괄호 안 쉼표 3개)
○ 일반직(행정, 원무) 채용                 → 쓰레기통  (쉼표 하나면 안 걸림)
○ 요양보호사 · 수련생(체험형) · 원무과 · 보건소장 → 쓰레기통
○ 2026년 제4차 정규직 공개채용 공고        → 보류함
```

받기 예외가 버림 단어에도 들어 있는지도 검사합니다 —
안 그러면 보류함에 쌓입니다 (방사선치료사만 뺐습니다).

### 아직 안 한 것

**[5] 는 시작하지 않았습니다.** 안전 검사가 멈췄고, 세중님이 「멈추고
보고」라고 하셨습니다. 쓰레기통 표 · 관리자 화면 · 719건 소급 전부
그대로입니다. `gas/wage.js` 에도 아직 안 붙였습니다.

---

## 네 갈래로 정정 · 시험 다시 (2026-09-25 밤 · **아직 안 붙였습니다**)

### 세 갈래 → 네 갈래

```
1  직군 확정        → 회원 목록
2  관련 단어 있음   → 보류함
3  아무 단어 없음   → 보류함
4  버림 단어만      → 쓰레기통   (관련 단어가 하나라도 있으면 절대 아님)
```

**「받기 단어」를 「보류 보장 단어」로 바꿨습니다.** 이 말이 있다고 회원에게
보이는 게 아니라 **버려지지 않는 것**이 보장됩니다. 뜻이 그것입니다.

### 1번은 새로 안 만들었습니다

수집기가 직군 칸을 채우는 그 판정을 그대로 씁니다.

```js
// gas/wage.js 9080줄 · notOurs_
if (t.indexOf('작업치료') > -1 || t.indexOf('물리치료') > -1) return false;
```

제목에 **「작업치료」나 「물리치료」가 있으면 먼저 통과**시킵니다.
그래서 「요양보호사 및 작업치료사 모집」도 우리 것입니다.

**「재활치료」는 확정이 아닙니다.** `matchJob_` 는 공통으로 보지만
「재활치료업무보조」 같은 것이 섞입니다. 2번으로 보내 사람이 봅니다.
「치료사」도 마찬가지입니다.

### 292건 시험 — 다시

```
           회원목록  보류함  쓰레기통
세 갈래       19      172     101
네 갈래       17      175     100
```

**「재활치료업무보조」가 쓰레기통 → 보류함으로 옮겨졌습니다.**

### 회원 목록 17건 — 전부 진짜 우리 직군입니다

```
가톨릭대인천성모 재활치료팀(소아물리치료실) 물리치료직
가톨릭대 은평성모 재활의학팀 작업치료직
서울큰나무병원 작업치료…            창원자연주간보호센터 작업치료…
진교나눔 물리 치료사                포항 송도경희요양병원 작업치료…
갤럭시요양원 물리 및 작업 치료사     차오름재활요양원 물리/작업 치료사 2건
사랑재가센터 물리 또는 작업 치료사   금빛재활주간보호센터 물리 치료사
아이드림 특수체육 교사 … 작업치료    충남서부장애인복지관 물리치료…
더헤리티지요양원 작업치료            즐거운우리집 물리치료…
실버공감요양원 작업치료              예사랑실버케어 작업치료
```

띄어쓴 꼴(「물리 치료사」)이 여섯 건 있습니다 — 공백을 빼고 견주지
않았으면 전부 놓쳤을 것들입니다.

### 쓰레기통에 「치료」·「재활」 — 3건으로 줄었고 전부 의도한 것입니다

```
국립교통재활병원 계약직 언어치료사      ← 「재활」은 병원 이름
<여수강남요양병원-언어치료사 계약직>    ← 같음
...랑과 치료로 가족을 돌보는 … 간호조무사 ← 「치료」는 수식어
```

앞 판에서 멈추게 했던 **「재활치료업무보조」는 이제 보류함**입니다.

### 검사 — `tools/check-sort.mjs` 21칸 전부 통과

세중님이 주신 7칸이 전부 들어 있습니다.

```
○ 홍보팀 및 물리치료사 모집              → 회원목록
○ 요양보호사 및 작업치료사 모집           → 회원목록
○ 재활치료업무보조 (장애인 제한채용)      → 보류함
○ 행동발달증진센터 치료사 채용            → 보류함
○ 서울대 2027년 직원 모집                → 보류함
○ 당직전담사 채용 (장애인우선채용)        → 쓰레기통
○ 일반직(행정, 보건, 간호조무, 원무직)    → 보류함
```

### 아직 안 붙였습니다

`gas/wage.js` 에 안 넣었습니다. 서버는 v316 그대로이고 배포도 안 했습니다.
[5](쓰레기통 표 · 관리자 화면 · 719건 소급)도 시작 전입니다.
